<Project Sdk="Microsoft.NET.Sdk">

  <!-- Entities, value objects, pure domain services. References nothing. No EF, no HTTP, no clock. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

</Project>
