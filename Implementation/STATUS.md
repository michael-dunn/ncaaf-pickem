# Status

Live task board. The agent that owns a task updates its row. States: Todo, In Progress, In Review, Blocked, Done.

## Board

| Task | Title | Agent | Tier | State | Branch | Notes |
|---|---|---|---|---|---|---|
| P0-01 | Repo and solution scaffold | opus-p0-01 | Opus | Done | main @ 5e49ac0 | Board verified: 38 task rows, one per task in every phase file, no drift. DECISIONS D-001..D-008 present; D-009 (test stack + central package management), D-010 (.slnx), D-011 (no UseBlazorFrameworkFiles) added. `dotnet build` 0 warnings, `dotnet test` 5/5 green, `dotnet format --verify-no-changes` clean. |
| P0-02 | Database and EF Core | opus-p0-02-03 | Opus | Done | p0-02-03-database-auth | All 26 tables from `02-Data-Model.md` in one migration `Phase0_02_InitialSchema`; applied to LocalDB (27 tables incl. history, 3 filtered indexes, `EndpointHash` computed column). `SqlTestDatabase` + `ApiFactory` + `ApiTestFixture` collection fixture (one DB per run, dropped at the end). `/health/ready` does `CanConnectAsync` and 503s otherwise. D-014..D-018 logged. |
| P0-03 | Google sign-in, users, authorization plumbing | opus-p0-02-03 | Opus | Done | p0-02-03-database-auth | Cookie `ncaaf.auth` (90d sliding, HttpOnly/Secure/Lax) + Google handler; `/auth/login/google`, `/auth/callback/google` (middleware), `/auth/logout`; `/api/me` GET+PUT. `RequireLeagueMember()` / `RequireLeagueCommissioner()` + `HttpContext.GetMembership()`; CSRF filter on the whole `/api` group. Tests: `TestAuth` scheme (`X-Test-User`), `ApiFactory.CreateClientAs`, `AuthMatrix.RunAsync`, and `AuthTests` driving the real Google pipeline through a fake backchannel. D-019..D-022 logged. **Manual pending (operator): real Google login over https://localhost:7092 with a dev OAuth client** — steps are in README "Google OAuth dev setup". |
| P0-04 | Blazor PWA shell and load-time spike | opus-p0-04-05 | Opus | Done | p0-04-05-pwa-calendar | Shell, PWA assets, HttpClient handlers, trimming/Brotli done. Spike recorded in `Implementation/spikes/wasm-load-time.md`: 2.19 MB Brotli, ~0.5 s cold / ~0.3 s warm to rendered page on localhost; PASS, D-025 confirms Blazor WASM. Screenshot `Implementation/screenshots/p0-04-shell-375.png`. **Manual pending (operator)**: iPhone home-screen install, standalone launch, and the phone/Tailscale load numbers (steps in the spike file). Fixed two blockers that made the hosted app never boot: fingerprint placeholder (D-024) and `TrimMode=full` (D-025). |
| P0-05 | Season calendar domain | opus-p0-04-05 | Opus | Done | p0-04-05-pwa-calendar | `Domain/Seasons/SeasonCalendar` (+ `SeasonWeek`, `ISeasonWeekSource`, `SeasonState`, `CurrentWeek`, `LeagueWeekRange`), `FixtureSeasonWeekSource` for 2026 (weeks 0-15, code not JSON, D-026), `GET /api/seasons/{year}/weeks`. `SeasonCalendarTests` 28 tests + `SeasonEndpointTests` 3, all green. Endpoint carries `// TODO P0-03: RequireAuthorization("Authenticated")` - the policy does not exist yet. |
| P0-06 | Job scheduler infrastructure | opus-p0-06 | Opus | Done | p0-06-job-scheduler | `Infrastructure/Jobs/`: `IScheduledJob`, `IOneShotJob` (the card's `IOneShotScheduler`, D-034), `SchedulerTick.TickAsync(nowUtc, ct)` holding all the logic, `JobScheduler : BackgroundService` ticking it every minute off `TimeProvider`, `HeartbeatJob` (`*/5 * * * *`), `AddScheduledJob<T>()` / `AddOneShotJob<T>()`. Cronos 0.13.0 evaluated in `SeasonCalendar.Eastern`; idempotency via `JobRuns(JobName, ScheduledForUtc)` with insert-before-run and duplicate-key-means-skip; 60-minute bounded catch-up (D-033). `GET /api/admin/data-status` + `RequireAnyLeagueCommissioner()` (D-035); P2-04 extends `AdminEndpoints`. 16 new tests (`JobSchedulerTests`, `OneShotTests`, `AdminDataStatusTests`); 86 green overall. Heartbeat verified against LocalDB with `Jobs__Enabled=true`. How to add a job: AGENT-NOTES "Jobs". |
| P1-01 | League and membership service and endpoints | sonnet-p1-01 | Sonnet | Done | p1-01-leagues-backend | `Domain/Leagues/LeagueRules` + `LeagueRuleViolation` + `MemberNameProjection`; `Infrastructure/Services/{LeagueService,InviteService,InviteCodeGenerator}`; `Api/Endpoints/{LeagueEndpoints,InviteEndpoints,LeagueRuleViolationResults}`; `Api/Validation/*` (FluentValidation, added to Directory.Packages.props). Deleted `DiagnosticsEndpoints.cs`/`DiagnosticsPing.cs` per D-021; `AuthMatrixTests` repointed at real routes. `MeEndpoints.ToResponse` now fills `Leagues`. Merged main (P0-06/P2-05/P3-01/P3-02) cleanly except two hot-spot conflicts (`EndpointMapping.cs`, `Infrastructure/DependencyInjection.cs`), resolved by keeping both sides' lines. `dotnet build` 0 warnings, `dotnet test` 236/236 green (143 Domain.Tests + 93 Api.Tests), `dotnet format --verify-no-changes` clean. Opus review pending. |
| P1-02 | League UI | sonnet-p1-02 | Sonnet | Done | p1-02-league-ui | All 7 pages under `Pages/Leagues/` (picker at `/`, create, home, members, invites, join, settings) built against `ILeaguesApi`/`ISeasonsApi` + a Development-only fake (D-043, D-044). Reusable `Components/`: StatusPill, RoleBadge, LockTime (D-045), EmptyState, ErrorBanner, LoadingSkeleton, ConfirmSheet. `wwwroot/js/invites.js` for Web Share. Screenshots for all 9 states at 375px, `document.documentElement.scrollWidth === 375` on every one (`Implementation/screenshots/p1-02-*.png`). `dotnet build`/`test`/`format --verify-no-changes` all clean (0 warnings, 180/180 tests). **Verified against real API by P1-03**: ran the published Api with fixtures + `Seed__DemoLeague=true` against a real LocalDB, dev-logged-in as `michael`/`alyson`, walked `/`, `/leagues/{id}`, `/leagues/{id}/members`, `/leagues/{id}/invites`, `/join/{code}`, `/leagues/{id}/settings` with curl (every P1-02 endpoint call) and headless-Edge screenshots. No integration bugs found: JSON casing, enum serialization, the 409 invite-accept body shape, and every route matched `ILeaguesApi`/`LeaguesApi.cs` exactly. `/leagues/new` was exercised via the API only (`POST /api/leagues`), not screenshotted again (identical to P1-02's own screenshot, which already used the real DTO shape). Lock time (`CurrentWeekLockAtUtc`) is null in this environment because no game set exists yet for the fixture week — expected, `LockTime`'s fallback text renders correctly. |
| P1-03 | Display names | sonnet-p1-03 | Sonnet | Done | p1-03-display-names | `PUT /api/me` gains the global-rename collision check (409, D-058); `MemberRow.IsMe` (additive) used by `Members.razor` to hide self-actions and show a "You" tag. Consolidated `DisplayNameConflictTests` into `DisplayNameTests` (length bounds for both endpoints, global-collision rule, projection helper via the members endpoint). P1-01 review follow-ups: N+1 fix in `LeagueService.ListMineAsync`/`GetMembersAsync` (season-weeks grouped by year, one batched `WeekSubmissions` query); `LeagueRuleViolationCode.InvalidPointValue` (400) and `MembershipNotFound` (404, replaces `InvalidTransferTarget` for an unknown membershipId on remove/promote/demote/transfer); `ILogger` Information lines in `InviteService.AcceptAsync` and `LeagueService`'s role/removal mutations. Web: `Pages/Profile/ProfilePage.razor` at `/me` (global name inline edit, per-league nickname section, `NotificationsSettingsPlaceholder` for P7-02, sign out), `Pages/More.razor` links to it. Screenshots `Implementation/screenshots/p1-03-profile-375.png`, `p1-03-more-375.png`, taken against the real running Api (not the fake). `dotnet build` 0 warnings, `dotnet test` 262/262 (143 Domain.Tests + 119 Api.Tests), `dotnet format --verify-no-changes` clean. Did not add a top-bar Profile link (More tab already links to it; judged not "trivial" without a redesign of the shared top bar). Opus review of the `LeagueService`/`InviteService` changes still pending per 06-Agent-Protocol. |
| P2-01 | Provider spike (real APIs) | opus-p2-01 | Opus | Done | p2-01-provider-spike, p2-01b-cfbd-captures | CFBD captures done (7 requests); only remaining gap is an in-progress ESPN payload (capture on a Saturday afternoon). Spike doc `Implementation/spikes/providers.md`; trimmed real captures (ESPN + CFBD) + verified TeamAliases draft in `tests/NcaafPickEm.Fixtures/Real/`; D-012 and D-013 logged; Feature 12 follow-ups updated. |
| P2-02 | CFBD reference data provider and ingest | sonnet-p2-02 | Sonnet | Done | p2-02-cfbd-ingest | `Infrastructure/Providers/Cfbd/`: `CfbdReferenceDataProvider` (singleton; Kiota `ApiClient`, `CfbdMapping` static mapper, `CfbdAccessTokenProvider`), `CfbdCalendarNormalization`, `DbSeasonWeekSource`. Adopts P2-03's `IProviderCallRecorder`/`ProviderCallRecorder` (singleton, own scope per call) after merging main; added `CountThisMonthAsync(provider, ct)` onto it and folded Kiota's `ApiException.ResponseStatusCode` into its status-code capture alongside `HttpRequestException` (D-062). `Infrastructure/Services/ReferenceDataIngestService`: `IngestTeamsAsync`/`IngestCalendarAsync`/`IngestScheduleAsync`/`IngestRankingsAsync`/`IngestLinesAsync`, each recording `DataRefreshStatus` and returning a small result record; failures clear the in-memory change tracker before committing, so nothing partial ever reaches SQL. Postponement is CFBD-absence-based with a fewer-than-half guard (D-056); calendar normalizes to Sunday-Saturday Eastern via the Saturday inside CFBD's window (D-055); Conferences.Name maps from CFBD `name`, not `shortName` (D-054). `Directory.Packages.props` pins `CollegeFootballData` 5.27.1 plus Kiota 2.1.2 (bumped past the spike's 1.19.0 floor for a security advisory, D-053). `DependencyInjection.cs` adds the `case "Cfbd"` branches for both `IReferenceDataProvider` and `ISeasonWeekSource`, alongside P2-03's domain-events/recorder/live-score registrations. Fixture seeder untouched (still maps fixture JSON directly) to keep `FixtureSeederTests` green with zero risk. Tests: `ReferenceIngestTests` (own throwaway DB — see notes), `ProviderCallRecorderTests` (own DB), `CfbdCalendarNormalizationTests` (no DB), `CfbdMappingTests` (real P2-01 captures via `FixtureLoader.ReadRealText` + plain `System.Text.Json` into the Kiota model types — Kiota's own JSON package exposes only serialize helpers), `CfbdLiveTests` (opt-in, `Category=Live`, needs `CFBD_LIVE=1` + `Cfbd__ApiKey`). Merged `main` (through P2-03, 36b8fcf) mid-task; reconciled the `ProviderCallRecorder` add/add conflict onto P2-03's shape and renumbered D-037..D-041 to D-053..D-062. `dotnet build` 0 warnings, `dotnet test` green, `dotnet format --verify-no-changes` clean; exact post-merge count in the commit. Live verification: PASSED (see end of row). D-062 logged. Live verification: orchestrator ran `CfbdLiveTests` with the real key on 2026-09-18 via the app's Kiota client — passed (1 request; 8 CFBD requests consumed total this month).  Opus review merged (b140522): all-division teams fetch so FBS-vs-FCS games are stored (D-069), empty-payload guard, month-bounded call count, no-null-overwrite, alias repoint guard; live test re-run against `/teams?year=` passed (9 CFBD requests total). |
| P2-03 | ESPN live score provider, matcher, CFBD fallback | opus-p2-03 | Opus | Done | p2-03-espn-live-scores | Domain event infrastructure (`Domain/Events/IDomainEvent`, `Domain/Seasons/Events/GameWentFinal`+`GameScheduleChanged`, `Infrastructure/Events/` collector + dispatcher + `AddDomainEventHandler<TEvent,THandler>()`, no MediatR). `Providers/Espn/` (`EspnScoreboardParser` pure over the P2-01 captures, `EspnStatusMapper`, `EspnLiveScoreProvider` on a named `HttpClient`), `Providers/Cfbd/CfbdLiveScoreProvider` (Scheduled/Final only), `GameMatcher`+`TeamNameIndex`+`TeamNameNormalizer`, `TeamAliasSeed` (P2-04's teams refresh calls it), `ILiveScoreHealth`/`LiveScoreHealth`/`CompositeLiveScoreProvider` (3 ESPN failures -> CFBD for the day + `ScoresMayBeStale`), `Services/LiveScoreApplyService`. **`IProviderCallRecorder`/`ProviderCallRecorder` written here because P2-02 had not landed — reconciled onto this shape by P2-02 at merge (D-052, D-062).** 04 section 9 rewritten and section 10 pointed at the built API; D-046..D-052 logged. 8 new test classes, 70 new tests (`EspnScoreboardParserTests`, `TeamNameNormalizerTests`, `GameMatcherTests`, `LiveScoreSourceSwitchTests`, `LiveScoreProviderSelectionTests`, `DomainEventDispatcherTests`, `LiveScoreApplyTests`, `TeamAliasSeedTests`); `dotnet build` 0 warnings, `dotnet test` 312/312 green (203 Domain, 109 Api), `format --verify-no-changes` clean. Merged `main` (P1-01/P1-02) mid-task.  Hotfix D-172 (2026-09-19): ESPN typed client now sends a User-Agent; without one ESPN answers 403 (seen on the first deployment). |
| P2-04 | Refresh jobs, Saturday poller, data status page | sonnet-p2-04 | Sonnet | Done | p2-04-jobs-poller | `Infrastructure/Jobs/Refresh/`: `TeamsRefreshJob`, `ScheduleRefreshJob`/`ScheduleRefreshDailyJob` (calendar folded in, D-057/D-078), `RankingsRefreshEveningJob`/`RankingsRefreshTuesdayJob`, `LinesRefreshJob`, all thin `IScheduledJob`s over the already-merged `ReferenceDataIngestService` (P2-02), resolving season via `RefreshJobSupport.CurrentSeasonYear` (D-077) and week via `ISeasonWeekSource`. `Infrastructure/Jobs/SaturdayPollerSchedule` (pure window/cadence/fallback decision, 04 section 10) + `SaturdayPoller : BackgroundService` (re-evaluates every minute, calls `ILiveScoreProvider`/`LiveScoreApplyService`). `AdminEndpoints` gains `POST /api/admin/refresh/{dataType}` (audit logged, D-079) and `POST /api/admin/unmatched/{id}/resolve`; `GET /api/admin/data-status` gains `ActiveLiveScoreSource`/`ScoresMayBeStale`/`NeedsReview` (additive, D-080). `Pages/Admin/DataStatusPage.razor` at `/admin/data` + `IAdminApi`/`AdminApi`/`FakeAdminApi`, linked from `More.razor`. This worktree was reassigned from the finished P2-02 agent (branch renamed, P2-02's commits carried forward); merged main once more (P3-05, plus a P1-03/P2-02 decision-numbering conflict cleaned up) before starting new work, so the diff against `main` is P2-04's own. `dotnet build` 0 warnings, `dotnet test` 377/377 green (203 Domain.Tests + 174 Api.Tests: +9 `RefreshJobScheduleTests`, +11 `SaturdayPollerScheduleTests`, +1 `SaturdayPollerIntegrationTests`, +9 `AdminEndpointsTests`), `dotnet format --verify-no-changes` clean. Screenshot `Implementation/screenshots/p2-04-data-status-375.png`, taken against the real published Api via `/auth/dev-login?user=michael`, 375px wide, no horizontal scroll. Opus review of `SaturdayPoller`/`SaturdayPollerSchedule` still pending per 06-Agent-Protocol.  Opus poller review merged: cadence now enforced (was polling every minute), poller asks for the window Saturday after midnight, loop survives provider exceptions, resolve endpoint 409 on alias owned by another team (D-089, D-090). Open: unmatched resolve picker is a raw GUID box (needs a candidates-by-date API; deferred to P8 polish). |
| P2-05 | Fixtures and fixture providers | sonnet-p2-05 | Sonnet | Done | p2-05-fixtures | `Providers/IReferenceDataProvider`, `ILiveScoreProvider` + provider-neutral records shaped against the real P2-01 captures. `FixtureReferenceDataProvider`/`FixtureLiveScoreProvider`/`FixtureSnapshotState` read `tests/NcaafPickEm.Fixtures/Data/Week7_2026` (16 games: 12 Saturday FBS, 2 FCS, 1 Friday excluded, 1 Friday-Pacific-Saturday-Eastern; 3 conference games; 4 ranked incl. Michigan/Texas and Maryland/Rutgers; lines on 10/12; 6 score snapshots incl. a Final tie and a post-midnight-ET finish) plus `influence-example.json` (Overview worked example). `FixtureSeeder`/`FixtureSeederHostedService` seed reference data + `SeasonWeeks` (from the merged P0-05 `ISeasonWeekSource`) and the "Family League" demo league (5 fixture users, Michael commissioner) when `Seed__DemoLeague=true`. `GET /auth/dev-login?user=<name>` and `/api/admin/fixture/snapshot` (Dev/Testing only). Merged `main` (P0-04/P0-05) mid-task per orchestrator instruction; D-036 logged. `dotnet build` 0 warnings, `dotnet test` 93/93 green (48 Domain, 45 Api), `dotnet format --verify-no-changes` clean, manual `dotnet run` smoke test verified seeded rows via `/api/me` after dev-login. |
| P3-01 | Game set generator (domain) | opus-p3-01 | Opus | Done | p3-01-gameset-generator | Pure `GameSetGenerator.Generate/Preview` + input records; 23 tests. Corrected `04` section 2 (eligibility gates manual adds, lock is a result flag); D-030..D-032. No P0-05 dependency: `IsSaturdayEastern` is an input flag. |
| P3-02 | Point value resolver (domain) | opus-p3-02 | Opus | Done | p3-02-point-value-resolver | `Domain/Points/`: `PointValueResolver` (`Resolve`, `IsElevated`), `PointRuleValidation` (`Validate` -> `PointRuleError[]`), records `PointGameInfo` / `PointRuleInfo` / `PointResolution`, `PointValueLimits`. New enum `Shared/Enums/PointValueSource` (D-014). 48 new tests in `PointValueResolverTests` + `PointRuleValidationTests`, `dotnet test` 51/51 green, build 0 warnings, `format --verify-no-changes` clean. D-028, D-029 logged; `04` section 3 updated to the real signature; corrected the "at or below" comment on `PointRuleType.CloseSpread` (strictly below). P3-03 consumes the API in D-028. |
| P3-03 | Configuration endpoints and services | sonnet-p3-03 | Sonnet | Done | p3-03-config-endpoints | `Infrastructure/Services/{GameSetService,PointRuleService,GameSetGameDtoMapper,WeekRangeGuard,PointValueRecalculator,GameSetRuleConfigurationException,PointRuleValidationException}`; `Domain/GameSets/{GameSetRuleViolation,Events/GameAddedToSet,Events/GameRemovedFromSet}`; `Api/Endpoints/{GameSetEndpoints,PointRuleEndpoints,ReferenceEndpoints,GameSetRuleViolationResults}`; `Api/Validation/{AddGameRequestValidator,SetPointOverrideRequestValidator}`. `LeagueService.UpdateSettingsAsync` re-resolves unlocked weeks on a `DefaultPointValue` change. Merged main (P2-03's domain events) mid-task; events raised through the real `DomainEventCollector`/`IDomainEventDispatcher`, no handler yet (D-057). D-053..D-057 logged. 34 new tests (`GameSetRulesEndpointsTests`, `GameSetGenerateTests`, `ManualAddRemoveTests`, `PointRulesEndpointsTests`, `ReferenceEndpointsTests`, `GameSetAuthMatrixTests`); narrowed `FixtureSeederTests`' game-count assertion to the fixture's own season/week since the 409-over-cap tests insert synthetic games under other years. `dotnet build` 0 warnings, `dotnet test` 346/346 green (203 Domain.Tests + 143 Api.Tests), `dotnet format --verify-no-changes` clean. Opus review pending. |
| P3-04 | Auto-regeneration job and schedule-change handling | sonnet-p3-04 | Sonnet | Done | p3-04-regeneration-job | `Infrastructure/Jobs/{RegenerateGameSetsJob,EnsureCurrentWeekSetsJob}` (Tuesday 03:30 ET, Sunday 00:05 ET): both call `GameSetService.GetOrCreateWeekSetAsync`+`GenerateAsync` per league/week, catching `Locked` (Information, skip) and `ExceedsMax` (Warning, skip) so one league/week never stops the run; `DbUpdateException` from a concurrent writer is caught the same way. `Infrastructure/Services/ScheduleChangeHandler : IDomainEventHandler<GameScheduleChanged>` (registered via `AddDomainEventHandler`): before lock, removes/restores the affected `WeekGameSetGames` row (`RemovedReason = "Schedule change"`, only restoring rows it removed itself) and recomputes `LockAtUtc`, raising `GameRemovedFromSet`/`GameAddedToSet`; after lock it leaves the row untouched and raises the new `Domain/GameSets/Events/GameNeedsVoidReview`. `GameSetService.ListNeedsVoidReviewAsync(leagueId?)` is the query P2-04's data page and P5-02's void flow can call. D-082: `PUT .../weeks/{week}/gameset-rules` also regenerates in the same request when `{week}` is the league's current week (superseding D-065 for that one route; `WeekOverrideRegenerateTests` proves the second `generate` call the UI still makes is then a no-op). Folded in the orchestrator's P3-03 review follow-ups (D-083): batched `Games` lookup in `GenerateAsync`, `GetWeekGameSetAsync`'s `.ThenBy(GameId)` tie-break, `ILogger<T>` Information logging on `GameSetService`/`PointRuleService`'s mutating methods, and the `DbUpdateException` handling above. Merged `main` through the P2-02 review-fixes commit mid-task (clean, no conflicts); renumbered D-069→D-082/D-083. Tests: `RegenerationJobTests`, `ScheduleChangeTests`, `AutoCreateWeekSetTests`, `WeekOverrideRegenerateTests` (17 new), driven via `ApiTestFixture.PinnedFactory`/`Factory` and `ActivatorUtilities.CreateInstance<TJob>` or the real `GameScheduleChanged`/`LiveScoreApplyService` pipeline. `dotnet build` 0 warnings, `dotnet test` 427/427 green (203 Domain + 224 Api), `dotnet format --verify-no-changes` clean. Opus review pending. |
| P3-05 | Commissioner configuration UI and member week view | sonnet-p3-05 | Sonnet | Done | p3-05-config-ui | Four pages under `Pages/GameSets/` (`Rules` at `/leagues/{id}/config/games`, `WeekGames` at `/leagues/{id}/config/week/{week}`, `MemberWeek` at `/leagues/{id}/weeks/{week}/games`) and `Pages/Points/Points` (`/leagues/{id}/config/points`), built against new `IGameSetsApi`/`GameSetsApi` and `IPointRulesApi`/`PointRulesApi` (D-059) plus Development-only fakes `FakeGameSetsApi`/`FakePointRulesApi` sharing one `FakeGameSetStore` (D-060), seeded from the Week 7 2026 fixture data (Michigan #8/Texas #5, Maryland/Rutgers Big Ten, etc.) with a `?locked=1` flag for the locked state. New reusable `Components/`: `GameRow`, `KickoffTime`, `NumberStepper`, `BottomSheet`. `LeagueHome.razor` got a small "Configure" section for commissioners (hot-spot table below — P1-03 may also touch `Pages/Leagues/`). Screenshots at 375px for all four pages plus the preview and locked states (6 total), `scrollWidth === 375` on every one, shot against the Development-only fake per D-060 (P3-03's real endpoints have not merged yet) via a temporarily-loosened, never-committed `USE_FAKE_API_SCREENSHOT_TEMP` constant exactly like D-044's recipe: `Implementation/screenshots/p3-05-{rules,rules-preview,week-games,week-games-locked,points,member-week}-375.png`. Found and fixed a real layout bug while screenshotting: `GameRow`'s single-row layout clipped team names on narrow screens once rank/kickoff/point-badge/trailing action all competed for space (D-061). `dotnet build` 0 warnings (with and without `USE_FAKE_API`), `dotnet test` 242/242 green (143 Domain, 99 Api), `dotnet format --verify-no-changes` clean. `PointRuleDto` carries no `ConferenceName`/`TeamName` echo (unlike `GameSetRuleDto`); the Points page resolves display names client-side from `/api/reference/{conferences,teams}` instead — flagged to P3-03/orchestrator as a possible additive DTO change, not made here since `Shared/Contracts` is P3-03's file. |
| P4-01 | Picks service and endpoints | opus-p4-01 | Opus | Done | p4-01-picks-backend | `Domain/Picks/{SubmissionStatusCalculator,ActiveSetGame,SubmissionState,PickRuleViolation}`; `Infrastructure/Services/PickService`; `Api/Endpoints/{PickEndpoints,PickRuleViolationResults}`; `Api/Validation/SetPickRequestValidator`; `Shared/Contracts/Picks/*` (7 records). Schema change: `WeekGameSetGames.AddedUtc` + migration `Phase4_01_GameSetGameAddedUtc` (D-085) — the recorded "last time TotalCount increased", written by `GameSetService` on insert and re-add. Guard order on mutations: `WeekRangeGuard` 404 → current week 409 `WeekNotCurrent` (past **and** future, D-088) → lock 409 `Locked` at/after `LockAtUtc` even before P4-02's job runs (D-086). `MyPickGameDto` composes `GameSetGameDto` (D-084); `{gameId}` on the pick route is the `Games.Id` (D-087). **For P4-04**: `PickService.RecomputeStatusAsync(weekGameSetId, membershipId, markUnseenGameChanges, ct)` and `RecomputeWeekStatusesAsync(weekGameSetId, markUnseenGameChangesFor, ct)`, both no-ops on a locked week. New tests: `SubmissionStatusTests` (Domain, 15), `SetPickTests` (12), `SubmitTests` (7), `PicksVisibilityTests` (7), `LockEnforcementTests` (6), `AckChangesTests` (6), `PicksAuthMatrixTests` (1), plus `Infrastructure/{PickWeekScenario,ClockedApp}`. `dotnet build` 0 warnings, `dotnet test` 536/536 green post-merge (218 Domain.Tests + 318 Api.Tests), `dotnet format --verify-no-changes` clean. Merged `main` (through 0e4e54d: P2-02 review, P2-04, P3-03 review, P7-01) mid-task and renumbered D-069..D-073 to D-084..D-088. D-084..D-088 logged. Opus review: self (Opus card). |
| P4-02 | Lock job | opus-p4-02 | Opus | Done | p4-02-lock-job | `Domain/Picks/{WeekLocker,WeekLockRequest,WeekLockGame,WeekLockMember,WeekLockResult,LockedGameSnapshot,LockedMemberStatus}`; `Domain/GameSets/WeekGameSetLockGuard`; `Domain/GameSets/Events/WeekLocked` (D-112); `Infrastructure/Jobs/LockWeekJob : IOneShotJob` (`Name = "LockWeek"`, key `WeekGameSetId:N`, `DueUtc = LockAtUtc`). No schema change, no migration. **Orchestrator decision implemented (D-110)**: configuration mutations are refused from `LockAtUtc`, not from `LockedUtc`, through one guard — `WeekGameSetLockGuard.IsFrozen(set, nowUtc)` / `IsNotFrozen(nowUtc)` — now used by `GameSetService` (generate, manual add/remove, week rules), `PointRuleService` (override, plus `ReResolveUnlockedWeeksAsync`'s query; it gained a `TimeProvider`) and `PickService`. `ScheduleChangeHandler` deliberately left on `LockedUtc` (P5-02 owns it). D-111: "active at lock" = `RemovedUtc == null` **and** `JoinedUtc <= LockAtUtc`. The card's "swept every minute Fri 18:00–Sun 03:00 ET" needs no window: `GetDueAsync` answers `LockAtUtc <= now && LockedUtc == null`, so an outage catches up on the next tick. `PointValueRecalculator.LoadLatestSpreadsAsync` extracted so `SpreadAtLock` and the recalculator agree on "current spread". New tests: `WeekLockerTests` (Domain, 16), `LockWeekJobTests` (12), `PostLockMutationTests` (14). `dotnet build` 0 warnings, `dotnet test` 595/595 green (234 Domain.Tests + 361 Api.Tests), `dotnet format --verify-no-changes` clean. **For P4-03/P6/P7**: `WeekLocked(LeagueId, Week, WeekGameSetId, LockedUtc)`; a locked week's `WeekSubmissions.Status` is `Locked` or `Incomplete` and `WeekGameSetResponse.IsLocked` still means only "the job has run" (D-086 unchanged). Opus review: self (Opus card). |
| P4-03 | Picks UI | sonnet-p4-03 | Sonnet | Done | p4-03-picks-ui | `Pages/Picks/PicksPage.razor` at `/leagues/{id}/weeks/{week}/picks`: header ("Week N", `StatusPill` extended with `PickedCount`/`TotalCount` for "In Progress n of m", `LockTime`), prev/next week chips via new `ILeaguesApi.GetLeagueWeeksAsync`, game cards ordered by kickoff with optimistic pick/PUT/revert (per-`GameId` sequence counter guards a stale response, re-tapping the same team is a no-op), "Saved" tick, inline ProblemDetails-title error on failure, ack-changes fired from `OnAfterRenderAsync`, sticky `SubmitFooter` (Submit / "N picks left" / Locked / Submitted), read-only past/locked weeks with `WinnerTeamId`-derived correct/incorrect coloring. New `Components/`: `TeamPickButton`, `PickGameCard`, `SubmitFooter`, `PickButtonOutcome` enum. New `Web/Services/IPicksApi`/`PicksApi` (+ `Fakes/FakePicksApi`, registered the same guarded way as the other typed clients) and an additive `Count` on `LeaguesApiException`/`ProblemDetailsBody` for the `IncompletePicks` 409 (D-120). D-120..D-123 logged (D-121: why every screenshot is fake-API, not real; D-123: the `DefineConstants` global-property trap that silently drops `DEBUG` from a raw `-p:DefineConstants=USE_FAKE_API` build/publish — corrects the recipe P1-02/P3-05 documented). Merged `main` through `d02598a` (P4-02 lock job, P4-04 pick-change handlers, P7-02/P7-03) — two additive hot-spot conflicts (`Web/Services/DependencyInjection.cs`'s two `#if` branches, both sides' `IPicksApi`/`IPushApi` lines kept), no other conflict markers found. `dotnet build` 0 warnings (both with and without `USE_FAKE_API`), `dotnet test` 676/676 green post-merge (270 Domain.Tests + 406 Api.Tests), `dotnet format --verify-no-changes` clean. Screenshots (375px, `scrollWidth === clientWidth === 375` on every one): `Implementation/screenshots/p4-03-{empty,in-progress,submitted,locked,past-week,offline-revert}-375.png`. Timing recorded in `Implementation/spikes/wasm-load-time.md` ("P4-03: Picks page repeat load"): cold 310-477 ms, warm 337-352 ms on localhost, well inside the 2 s budget (Debug/unoptimized build, so a conservative upper bound). **All six screenshots and the real-vs-fake tradeoff (D-121)**: the real, already-merged P4-01 endpoints only accept picks for the league's actual current week, which is derived from the real wall clock (today is week 3 of the fixture calendar) while the fixture schedule's games are anchored to week 7 (2026-10-17) — confirmed `GET .../picks/me` works read-only against a real, freshly-generated week-7 set (dev-login + curl), but every mutation 409s `WeekNotCurrent` regardless of P4-02's lock job. `FakePicksApi` adds a fixed `PastWeek = 6` (three `Final` games) for the past-week screenshot and a `?failNextPick=1` flag for the offline-revert one. P4-04's `IsNewSinceSubmit`/ack-changes wiring was already built against the real contract and needs no follow-up; whoever revisits this page after the real calendar reaches week 7 should re-take the in-progress/submitted/offline screenshots against the real API. |
| P4-04 | Game added/removed handling for picks | sonnet-p4-04 | Sonnet | Done | p4-04-game-change-handlers | `Infrastructure/Services/PickGameChangeHandlers.cs`: `GameAddedPickHandler`/`GameRemovedPickHandler` (`IDomainEventHandler<GameAddedToSet>`/`IDomainEventHandler<GameRemovedFromSet>`), both thin wrappers over P4-01's `PickService.RecomputeWeekStatusesAsync` — see AGENT-NOTES "Picks" for exactly which membership ids each passes as `markUnseenGameChangesFor`. No new migration; no endpoint changes; every event-raising site (`GameSetService.GenerateAsync`/`AddGameAsync`/`RemoveGameAsync`, `ScheduleChangeHandler`) already dispatched correctly post-save, so no fix was needed there. New tests: `GameAddedTests` (6: stored-row revert + flag + roster/`GET /api/leagues` reflect immediately, NotStarted unaffected, InProgress stays flagged, ack-changes clears the flag, locked-week no-op via direct handler invocation, one regeneration event adding two games writes exactly one row per member), `GameRemovedTests` (6: pick row retained, Submitted member stays Submitted and flagged, a member with no pick on the removed game is not flagged, roster counts update immediately, a re-add flags again, locked-week no-op). `dotnet build` 0 warnings, `dotnet test` 565/565 green (218 Domain.Tests + 347 Api.Tests), `dotnet format --verify-no-changes` clean. D-108/D-109 logged. Order relative to P7-03's (not-yet-merged) notification handlers for the same two events does not matter — see AGENT-NOTES and the doc comments on both handlers. |
| P5-01 | Week scorer (domain + service) | opus-p5-01 | Opus | Done | p5-01-week-scorer | `Domain/Scoring/{WeekScorer,WeekScoringRequest,ScoringGame,ScoringMember,ScoringPick,WeekScoreResult,MemberWeekScore}` — pure, no clock, no week number; filters active rows and settled memberships itself (D-128) and delegates every winner question to `WinnerResolver` (D-098). `Domain/Scoring/Events/{ResultOverridden,GameVoided}` defined and already subscribed, for P5-02 to raise (league-scoped, naming `WeekGameSetId`, D-132). `Infrastructure/Services/ScoringService` (`RescoreWeekAsync`/`RescoreGameAsync`, full recompute, upsert `WeekResults`, stamp `WeekGameSets.IsComplete`), its three handlers, and `Infrastructure/Jobs/NightlyRescoreJob` (`30 4 * * *` ET; locked-non-complete weeks plus sets whose results disagree with them, D-134). **Contract for P5-03**: `Infrastructure/Scoring/IStandingsSnapshotWriter.WriteSnapshotAsync(leagueId, throughWeek, ct)`, called once on the not-complete→complete transition after `SaveChangesAsync`; the `NoOpStandingsSnapshotWriter` is a `TryAddScoped`, so P5-03's real writer wins by registering on an earlier line (D-129). No schema change, no migration (`WeekResults`/`SeasonStandingsSnapshots` already existed). D-128..D-134 logged; `04` section 7 rewritten to the real API; AGENT-NOTES "Scoring"; Feature 06 traceability rows repointed. New tests: `WeekScorerTests` (Domain, 22, incl. the fixture's post-midnight finish read out of snapshot 6), `ScoringSnapshotWalkTests` (Api, 1 — the Phase 5 exit criterion: snapshots 1..6, totals 10→50→90/20/10→+35, the Iowa State/Kansas tie earning nobody anything and holding `IsComplete` false until a real `ResultOverridden` settles it, snapshot writer called exactly once, two further rescores change nothing), `ScoringServiceTests` (Api, 10 — void, unlocked weeks, orphan rows, the nightly sweep). Merged `main` at d02598a cleanly (no conflicts). `dotnet build` 0 warnings, `dotnet test` 709/709 green (292 Domain + 417 Api), `dotnet format --verify-no-changes` clean. Opus review: self (Opus card). |
| P5-02 | Corrections: override result and void | sonnet-p5-02 | Sonnet | Done | p5-02-corrections | `Domain/Scoring/CorrectionRuleViolation` (`NotLocked`/`AlreadyVoided` 409, `TeamNotInGame`/other 400, `GameNotFound` 404) + `Api/Endpoints/CorrectionRuleViolationResults`. `Infrastructure/Services/CorrectionService` (`OverrideResultAsync`, `VoidGameAsync`, `GetAuditAsync`): both mutations load the locked row (`WeekGameSetLockGuard.IsFrozen`), write the `WeekGameSetGames` column + an `AuditLog` row (before/after/reason/scores, plus `gameId` in `Details` so the audit reader can resolve team names without a second table), `SaveChangesAsync`, then raise `ResultOverridden`/`GameVoided` (P5-01's handlers already subscribed, rescoring happens there — no scoring code added here). `GetAuditAsync` builds each `AuditEntry.Summary` per `AuditAction` (batched game-name and member-name lookups, JSON-parsed `Details`), covering every action type written so far (`ResultOverride`/`GameVoided`/`GameManuallyAdded`/`GameManuallyRemoved`/`MemberRemoved`/`RolePromoted`/`RoleDemoted`/`RoleTransferred`/`ManualRefresh`), "System" when the actor membership is missing. `Api/Endpoints/CorrectionEndpoints.cs` (`POST .../gameset/games/{gameId}/override-result`, `/void`, `GET /api/leagues/{leagueId}/audit`) with `OverrideResultRequestValidator`/`VoidGameRequestValidator` (Reason 1..200). Decision (documented, not a bug): a second identical override is not a no-op — it writes a fresh audit row and re-raises the event, since the rescore it triggers is idempotent anyway (D-143). `GameNeedsVoidReviewHandler : IDomainEventHandler<GameNeedsVoidReview>` added purely to quiet the dispatcher's "no handler" log — P3-04's `ListNeedsVoidReviewAsync`/`GET /api/admin/data-status` remain the mechanism, unchanged. `NeedsReviewGameDto` gained additive optional `WeekGameSetId`/`GameSetGameId` (both branches of `AdminEndpoints.GetDataStatusAsync`'s `NeedsReview` list now populate them) so P5-05's one-tap Void has the ids it needs without a second lookup. New tests: `OverrideTests` (8), `VoidTests` (7), `CorrectionsAuthMatrixTests` (3) — all against real HTTP endpoints, `ScoredWeekScenario` + `SaturdayPoller` snapshot 6 (own throwaway `SqlTestDatabase`, like `ScoringServiceTests`). `dotnet build` 0 warnings, `dotnet test` 737/737 green post-merge (292 Domain.Tests + 445 Api.Tests), `dotnet format --verify-no-changes` clean. Merged `main` through 559cebb (P5-04 leaderboard UI, P6-02 review fixes) — two conflicts, both append-only (`DECISIONS.md`/`STATUS.md` row/decision numbering), resolved by keeping both sides' content. Three hot-spot rows below. Opus review pending.  **Opus review folded into P8-01** (branch `p8-01-security-review`, report §8). Two defects found and fixed (D-156): `CorrectionService.GetAuditAsync`'s summary builder used `JsonElement.GetProperty`/`TryGetProperty` unguarded, so one audit row with an unexpected `Details` shape 500'd the whole league's audit page for every member; and `GameSetService.ListNeedsVoidReviewAsync` did not exclude `IsRemoved` rows, so the data-status "needs review" list offered a void/override that answers 404 `GameNotFound` — which P5-05's one-tap Void reads. Everything else verified correct: override/void refused before lock, winner must be home or away, a voided row cannot be overridden, audit rows carry the actor plus `gameId`/`before`/`after`/`reason`/scores, events raised after `SaveChangesAsync` with P5-01's handlers registered so rescoring happens, audit member-only, needs-review updates. D-143 (a repeated identical override re-audits and re-raises) reviewed and upheld. |
| P5-03 | Standings and leaderboard queries | opus-p5-03 | Opus | Done | p5-03-leaderboard | `Domain/Leaderboard/{StandingsCalculator,StandingsMember,StandingsWeekResult,StandingsSnapshot,StandingsSnapshotRow,GridGame,GridPick}` (pure, `04` section 8: competition ranking, points behind, weekly wins, trend, mid-season joiners, former members, grid precedence, `ComputeSnapshot`). `Infrastructure/Services/LeaderboardService` + `Api/Endpoints/LeaderboardEndpoints` for the three contract routes (season = 3 queries; grid 403 `PicksNotVisible` before lock). `Infrastructure/Scoring/StandingsSnapshotWriter` implements P5-01's `IStandingsSnapshotWriter` and is registered above its `TryAddScoped` no-op. `HasGameSet` on `GET .../weeks` now means generated (D-140). Tests: `StandingsCalculatorTests` (14), `LeaderboardEndpointsTests` (10), `StandingsSnapshotWriterTests` (4), `LeaderboardAuthMatrixTests` (3), `LeaderboardPerfTests` (50 members x 15 weeks x 20 games under season 2096; season endpoint well under the 1 s budget). D-139..D-142. Merged `main` @ 5fe3182 (P5-01; add/add on `IStandingsSnapshotWriter` resolved in favour of main's file) and @ ee8693c (P6-02 review). Per the orchestrator's request, D-135's roster rule is now also applied to the grid's columns and to `PickService.GetAllPicksAsync`, with `PickWeekScenario.MarkLockedAsync` settling lock statuses to match (D-142); `PicksVisibilityTests` gained the removed-before-lock case and two handler tests now read their "before" row after the lock. |
| P5-04 | Leaderboard UI | sonnet-p5-04 | Sonnet | Done | p5-04-leaderboard-ui | `Pages/Leaderboard/{Season,WeekPage,Grid}.razor` (routes: `/leagues/{id}/leaderboard`, `/leagues/{id}/weeks/{week}/leaderboard`, `/leagues/{id}/weeks/{week}/grid`) built against new `Web/Services/ILeaderboardApi`/`LeaderboardApi` (+ `Fakes/FakeLeaderboardApi`, D-136) — **P5-03's real endpoints have not merged**, so all screenshots are against the fake, same as P4-03/P6-03's own notes for their pages. New reusable `Components/`: `TrendArrow`, `WeekNav`, `PickGrid` (pinned first column + sticky header, D-137). Reused `ILeaguesApi.GetLeagueWeeksAsync` (P4-03's route) for week nav; `FakeLeaguesApi`'s implementation now delegates to `FakeLeaderboardApi.BuildLeagueWeeks()` (resolved a merge conflict against P4-03's simpler version — see D-136 for why it is safe). `BottomTabBar`'s Leaderboard tab already routed to `/leagues/{id}/leaderboard` from P1-02, so no wiring change was needed there. `dotnet build` 0 warnings (with and without `USE_FAKE_API`), `dotnet test` 719/719 green post-merge (292 Domain.Tests + 427 Api.Tests), `dotnet format --verify-no-changes` clean. Merged `main` through `5fe3182` (P5-01 week scorer, P6-02/P6-03 dashboard, P4-03 picks) mid-task; four additive-shaped conflicts (`DependencyInjection.cs`'s two `#if` branches, `_Imports.razor`'s two `@using` lines, `ILeaguesApi.cs`'s doc comment, `FakeLeaguesApi.GetLeagueWeeksAsync`'s body — the one real semantic choice, D-136) resolved and re-tested; no leftover conflict markers. Screenshots (375px, `document.documentElement.scrollWidth === 375` on every one): `Implementation/screenshots/p5-04-{season,week,grid,grid-scrolled,grid-prelock}-375.png` — `grid-scrolled` shows the pinned "Game" column and matchup text still in place while the member columns scroll underneath (headless Chromium, D-137). **Manual pending (operator): iOS Safari pinned-column check** — no physical device or simulator in this environment; the sticky-positioning CSS includes the `-webkit-sticky` fallback and no ancestor `overflow` sits between the scroll container and the pinned cells, and the headless-Chromium mid-scroll screenshot above is the best available substitute. Steps for the operator: open the grid page on an iPhone in Safari, scroll the grid horizontally, and confirm the "Game" column (matchup + result) stays pinned on the left edge throughout. |
| P5-05 | Commissioner corrections UI and audit view | sonnet-p5-05 | Sonnet | Done | p5-05-corrections-ui | New `Web/Services/ICorrectionsApi`/`CorrectionsApi` (+ `Fakes/FakeCorrectionsApi`, D-149) for the override-result/void/audit routes in `03-API-Contracts.md` (Feature 06). Built initially against the fake (P5-02's server endpoints had not merged yet — same pattern as P4-03/P5-04/P6-03's own notes), then **P5-02 merged mid-task and this branch switched to verifying the real endpoints** (see the open item below and D-148/D-150). `Pages/GameSets/WeekGames.razor` (commissioner, P3-05's page): once `WeekGameSetResponse.IsLocked`, each game's trailing slot swaps to either a "Voided" pill, a "Result set: <winner>" pill, or a "Correct" button opening a `BottomSheet` menu with "Set result" (two `TeamPickButton`s + a 1..200-char reason, `TeamPickButton` reused as-is) and "Void game" (reason only, destructive). Both actions refetch the week set on success so the row updates immediately without a page reload. New `Pages/Audit/AuditPage.razor` at `/leagues/{id}/audit` (all members, not commissioner-gated): newest-first cards with a humanised action label (`AuditAction` enum name -> "Result set"/"Game voided"/etc.), relative time (absolute on hover via `title`), actor, and the server-built summary; empty state when there is nothing yet. `Pages/Admin/DataStatusPage.razor`'s needs-review rows get a one-tap "Void" action (`BottomSheet`, reason required, red confirm) plus a "View activity log" link to the league's audit page (D-150: calls straight through on `row.GameId`, the route's actual param, not P5-02's additive `WeekGameSetId`/`GameSetGameId`); `FakeAdminApi` now takes `FakeGameSetStore` so its seeded needs-review row (Iowa State/Kansas tie) shares the same `GameId` the store's `VoidGame` operates on, and disappears from the list once voided. `LeagueHome` gets an "Activity" link next to Invites/Members. Friendly 409/400/404 messages (`NotLocked`/`AlreadyVoided`/`TeamNotInGame`/`GameNotFound`) on both the week view and Data status page. `dotnet build` 0 warnings both configs (plain and `-p:DefineConstants=DEBUG%3BTRACE%3BUSE_FAKE_API`), `dotnet test` 771/771 green post-merge (306 Domain.Tests + 465 Api.Tests — no Web test project exists in this repo, matching every prior Blazor task), `dotnet format --verify-no-changes` clean. **Real-endpoint verification of "override changes the leaderboard immediately"**: published `NcaafPickEm.Api` plain-Release against fixtures, dev-logged-in as `michael`, hand-built week 7's set (default rules were empty; `POST .../gameset/games` once), seeded two members' picks and settled their `WeekSubmissions.Status=Locked` via `sqlcmd` (the real pick-mutation routes 409 `WeekNotCurrent` today per D-121's clock-drift note — real wall clock is week 3, fixture week 7 is anchored to 2026-10-17), set `WeekGameSets.LockedUtc` via `sqlcmd`, then called the real `POST .../override-result` three times in a row (Kentucky, then Georgia, then Kentucky again) and re-read `GET .../weeks/7/leaderboard` after each: the ranking flips **Alyson 10/Michael 0 <-> Michael 10/Alyson 0** every time, immediately, with no separate rescore step — confirms P5-01's `ResultOverriddenScoringHandler` really does fire synchronously off the domain event P5-02 raises. Screenshotted the real `/leagues/{id}/weeks/7/leaderboard` page before and after (`p5-05-leaderboard-{before,after}-override-375.png`) and the real audit trail (`p5-05-audit-375.png`, all real `ResultOverride`/`GameManuallyAdded` rows) and the real week view showing "Result set: Kentucky" (`p5-05-week-result-set-real-375.png`). The interactive-sheet screenshots (`p5-05-week-corrections-375.png` with the Correct menu open, `p5-05-set-result-sheet-375.png`, `p5-05-needs-review-void-375.png`) are against the fake, taken against a published, hosted real Api with a temporarily fake-enabled Web build per D-044/D-127's `USE_FAKE_API_SCREENSHOT_TEMP` escape hatch (reverted before committing) — a second real game could not be added to demonstrate the "Correct" button's pre-override state against real data because the week was already locked by the time P5-02 landed (adding a game to a locked week is correctly refused, 409). Merged `main` twice (through 095238e for P5-03/P5-04 leaderboard, then through 7e57e26 for P5-02 corrections); all conflicts additive (`DependencyInjection.cs`'s `#if` branches, `_Imports.razor`'s usings), both kept, no leftover conflict markers. Hot-spot rows below (`_Imports.razor`, `DependencyInjection.cs`, both own commits). Also performed the P6-03 real-endpoint dashboard verification hand-off — see that row's added note below. D-148..D-150 logged. |
| P6-01 | Influence calculator (domain) | opus-p6-01 | Opus | Done | p6-01-influence-calculator | `Domain/Dashboard/`: `InfluenceCalculator.Calculate` + records `InfluenceRequest`/`InfluenceGame`/`InfluenceMember`/`InfluencePick` in, `InfluenceResult`/`InfluenceGameResult` out; new `Shared/Enums/InfluenceOutcome` (D-014). **Shared `Domain/Scoring/WinnerResolver`** (override > higher score when Final > none) written here because P5-01 has not started; `GameSetGameDtoMapper` and `LiveScoreApplyService.DetermineWinner` refactored to delegate to it, behaviour-preserving, whole Api suite still green — **P5-01/P5-02 must call it, not re-derive** (D-098, AGENT-NOTES "Game sets and points"). 36 new tests (`InfluenceCalculatorTests` 18 incl. the Overview worked example asserted from `influence-example.json` for both Dance and Alyson, `WinnerResolverTests` 18). `dotnet build` 0 warnings, `dotnet test` 518/518 green (239 Domain in 0.2 s + 279 Api), `dotnet format --verify-no-changes` clean. `04` section 6 rewritten to the real API; `03` `DashboardGameDto` line gains `HomePickers`/`AwayPickers` + the enum's home; D-095..D-099 logged; AGENT-NOTES "Dashboard" documents how P6-02 builds the request (the "active at lock" query is the whole rule). No merge of `main` needed (branched at d177a5c, nothing else touched these files). |
| P6-02 | Dashboard endpoint | sonnet-p6-02 | Sonnet | Done | p6-02-dashboard-endpoint | `Infrastructure/Services/DashboardService.GetAsync(viewer, week, ct)`: reuses `GameSetService.GetWeekGameSetAsync` for games (also runs `WeekRangeGuard`), one query for members with a `WeekSubmissions` row for the set (`MembersActiveAtLock`), one query for `Picks` on the set; builds `InfluenceRequest`, calls `InfluenceCalculator.Calculate`, joins the result back onto the `GameSetGameDto`s by `GameSetGameId`. "Locked" = `WeekGameSets.LockedUtc != null` only, never the `LockAtUtc`-passed instant (D-116) — before that, `IsAvailable=false` with `LockAtUtc`/`LockAtEasternDisplay` and empty lists, including the no-set-yet case (`GetWeekGameSetAsync` already answers that shape). `Api/Endpoints/DashboardEndpoints.cs` (`MapDashboardEndpoints`, `GET /api/leagues/{leagueId}/weeks/{week}/dashboard`, `RequireLeagueMember()`) 400s any query string at all (title `UnsupportedQueryParameter`, D-119) rather than accepting/ignoring `asMember`. New `DashboardTests` (10: no-set-yet, past-`LockAtUtc`-but-job-hasn't-run, the Overview worked example for Dance and Alyson via a new `DashboardWeekScenario` test helper that seeds all five fixture members and the two fixture games (700001/700002) and locks through the real `LockWeekJob` — not by hand-setting `LockedUtc` — stale-scores propagation via a new `FakeLiveScoreHealth`, Won/Lost + `PointsSoFar` after a Final score, a post-lock joiner's own dashboard (no pick anywhere, D-118), a former member still appearing flagged, `?asMember=` 400, and the member/non-member/anonymous triple via `AuthMatrix.RunAsync` with no commissioner route). `dotnet build` 0 warnings, `dotnet test` 686/686 green (270 Domain.Tests + 416 Api.Tests) after merging `main` (through d02598a: P4-02 lock job, P4-04, P7-03 review fixes — `DashboardService`'s "locked" semantics were already correct against the real `LockWeekJob`, no logic change needed), `dotnet format --verify-no-changes` clean. D-116..D-119 logged. Two hot-spot rows below. Opus review pending.  Opus review merged (D-135): roster limited to Locked/Incomplete WeekSubmissions rows (a member who left before lock was being named); tests de-tautologised. Open: same roster gap in PickService.GetAllPicksAsync handed to P5-03. |
| P6-03 | Dashboard UI | sonnet-p6-03 | Sonnet | Done | p6-03-dashboard-ui | `Pages/Dashboard/DashboardPage.razor` at `/leagues/{id}/weeks/{week}/dashboard` (built and screenshotted before P6-02 landed on `main`, against the frozen contract in `03-API-Contracts.md`/`Shared/Contracts/Dashboard/*` and the fake, per orchestrator guidance; P6-02 has since merged — see the real-endpoint verification note below, added after this branch merged `main` through 69b100e): pre-lock countdown card (`LockTime`'s new `ShowCountdown` mode, D-045 extended not duplicated) + "Dashboard opens at kickoff"; post-lock header (`StatCard` x2, `StaleBanner` when `ScoresMayBeStale`), `DashboardGameCard` per game in server order (matchup with the viewer's pick underlined, status line - kickoff/live-with-clock/final -, Won/Lost tint, `MemberChips` for opposite/no-pick/both-sides), collapsed `CollapsibleSection` "Everyone agrees (N)". New client `Web/Services/IDashboardApi`/`DashboardApi` + `Fakes/FakeDashboardApi` (D-044/D-059 gate), the fake seeded with the Overview worked example (Michael/Alyson/Dance/Alex/Daniel, Michigan-Texas, Maryland-Rutgers) plus two games this task added for "everyone agrees" and "no pick, both sides" (the worked example alone doesn't exercise either); `?viewer=alyson|michael|alex|daniel` (default Dance) and `?state=prelock|live|final|stale` fake flags, read client-side only and never sent to the real endpoint (D-119's note). Auto-refresh: a `PeriodicTimer` ticks every 60 s for the page's life once any game is not Final; new `wwwroot/js/visibility.js` (Page Visibility API, `DotNetObjectReference` callback) reports tab visibility, and a tick while hidden is skipped outright (no fetch/re-render/reload) rather than tearing the timer down and recreating it - loop stops for good once every game (incl. "Everyone agrees") is Final; a small "Updated h:mm" line shows the last refresh. `dotnet build` 0 warnings both configs (plain and `-p:DefineConstants=USE_FAKE_API`), `dotnet test` green post-merge, `dotnet format --verify-no-changes` clean. **Screenshots taken against a published, hosted (real) Api, fake data layer**: `dotnet publish src/NcaafPickEm.Web -c Release -p:DefineConstants=USE_FAKE_API_SCREENSHOT_TEMP` (a temporary `\|\| USE_FAKE_API_SCREENSHOT_TEMP` added to `DependencyInjection`'s guard, reverted in the same working session before committing - D-044's escape-hatch pattern) then `dotnet publish src/NcaafPickEm.Api -c Release -p:BuildProjectReferences=false` so the Api's publish step reuses that exact Web build instead of silently rebuilding it plain-Release (confirmed by diffing `_framework/dotnet.js` byte sizes between the two publish outputs - **`-p:BuildProjectReferences=false` is the fix; copying `wwwroot` after the fact is not enough** because `MapStaticAssets()`'s `*.staticwebassets.endpoints.json` manifest is generated at Api-publish time and pins Content-Length/ETag per file, so a same-named-but-different file dropped in afterward serves truncated at the old length, D-123, folded into AGENT-NOTES). Ran the published Api against LocalDB, dev-logged-in as `michael`, drove headless Edge via `puppeteer-core`/CDP for all four states plus a click-to-expand check on "Everyone agrees" (confirmed the hidden game appears). Found and fixed one real bug this way: `Q@period` rendered as the literal text "Q@period" instead of interpolating (needed `@(period)`). D-120..D-123 logged. Merged `main` twice — first through d02598a (P4-02 lock job, Leaderboard/Scoring contracts, no conflicts), then through 69b100e (P6-02 landing; conflicts only in `DECISIONS.md`/`STATUS.md` from both tasks numbering/rowing independently, resolved by renumbering this branch's decisions to D-120..D-123 after P6-02's D-116..D-119 and combining the two P6-03/P6-02 board rows). Hot-spot rows below (`_Imports.razor`, `DependencyInjection.cs`, both small/own commits). **Real-endpoint verification by P5-05 (2026-09-19): done.** Published `NcaafPickEm.Api` plain-Release (real, non-fake client), ran it against a fresh LocalDB with `Providers__ReferenceData=Fixture`/`Seed__DemoLeague=true`, dev-logged-in as `michael`. Today's real wall clock is week 3 (D-121's clock-drift note applies again: the fixture schedule's only games are anchored to 2026-10-17, which is week 7), so week 7 had no game set yet — generated one by hand (`POST .../gameset/games` x4, since the league's default rules were empty) and set `WeekGameSets.LockedUtc` directly via `sqlcmd` (the orchestrator's suggested shortcut for a job that will not naturally tick until the real clock reaches October). `GET .../weeks/7/dashboard` then answered `IsAvailable=true` against the real endpoint; loaded `/leagues/{id}/weeks/7/dashboard` in headless Edge (real Web build, real Api) and confirmed it rendered live picks correctly (`Implementation/screenshots/p6-03-real-endpoint-live-375.png`). `POST /api/admin/refresh/Scores` did not update scores (it polls *today's* Eastern date, 2026-09-19, not the fixture's 2026-10-17 — the same clock-drift gap, not a client bug), so to exercise the Final render, two games' `Games.Status`/scores were set to Final directly via `sqlcmd`; the real endpoint then reported `Final`, and the page rendered "Final 20-27" / "Final 17-14" correctly, both teams and scores in the right order (`p6-03-real-endpoint-final-375.png`) — no repeat of the P6-03 "`Q@period`" bug and no new client bug found in `DashboardApi`/`DashboardPage` against the real payload shape. No client code changes were needed. Opus review still pending. |
| P7-01 | Push subscriptions and sender | opus-p7-01 | Opus | Done | p7-01-push-sender | `Infrastructure/Push/`: `IPushSender` + `WebPushSender` (WebPush 1.0.13) + `NullPushSender`, `PushPayload` (`{title,body,url,tag}` camelCase), `PushSendResult`, `PushTtl` (1 h reminders / 4 h events, D-072), `VapidConfiguration`/`PushOptions`/`VapidKeyGenerator`, `NotificationService.SendToUserAsync`, `PushRetryJob : IOneShotJob` over a new `PushRetries` table (migration `Phase7_01_PushRetries`, D-070; +1/+5/+15 min then Failed). `Api/Endpoints/PushEndpoints.cs`: vapid-public-key (503 when unconfigured, D-073), subscription upsert by endpoint hash (D-074) with re-owning (D-075), idempotent delete, status, Commish notification log, and a Development/Testing-only `POST /api/push/test`. DTOs in `Shared/Contracts/Push/`, two FluentValidation validators. D-070..D-076 logged; 02/03/AGENT-NOTES/README/both appsettings templates updated. 46 new tests (`PushSubscriptionTests` 19, `PushDeliveryTests` 13, `WebPushSenderTests` 14, plus a fixed-up assert in `HealthEndpointTests` that hard-coded "exactly one migration"); `dotnet build` 0 warnings, `dotnet test` 378/378 green (203 Domain + 175 Api), `format --verify-no-changes` clean. `deploy/generate-vapid.ps1` run locally; keys printed to console only, never committed. **Manual pending (operator): a real device push** — that is P7-02's job and needs a phone. |
| P7-02 | Service worker, settings page, notification routing | sonnet-p7-02 | Sonnet | Done | p7-02-service-worker | `wwwroot/service-worker.js` and `.published.js` both implement `push` (`showNotification` from `event.data.json()`, `icon: icon-192.png`, `badge: badge-96.png`, `renotify: true`, `data.url`) and `notificationclick` (focus + `navigate` an existing window resolved against `self.registration.scope`, else `clients.openWindow`) — identical handlers, kept in sync by hand, no shared module. New `wwwroot/js/push.js` ES module: `getSupport()`, `subscribe(vapidPublicKey)`, `getExistingSubscription()`, `unsubscribe()`; every returned shape uses explicit camelCase keys matched by `[JsonPropertyName]` on the C# side (`Web/Services/PushInterop.cs`). New `Web/Services/IPushApi`/`PushApi` (+ `Fakes/FakePushApi`, same `DEBUG && USE_FAKE_API` switch, registered in `DependencyInjection.AddWebServices`) wrapping `/api/push/*`; 503 vapid-key surfaces as `null`, not an exception. `Components/NotificationSettings.razor` replaces P1-03's `NotificationsSettingsPlaceholder` on `/me`: unsupported / iOS-not-installed (Home Screen steps) / server-unavailable (503) / permission-denied / off / "off on this device" (re-posts the existing browser subscription) / on (Turn off + `#if DEBUG` commissioner-gated "Send test notification", D-107); a `?simulate=ios\|off\|on\|unavailable` query flag (`#if DEBUG` only, D-106) forces a state for screenshots without depending on the capturing browser's real Push API support. `manifest.webmanifest` gains a generated 96x96 `badge-96.png` (navy ring / white disc, `purpose: monochrome`). D-106, D-107 logged. `dotnet build` 0 warnings (with and without `USE_FAKE_API`), `dotnet test` 456/456 green (203 Domain + 253 Api — this task added no new .NET tests, only client code the existing suites do not cover), `dotnet format --verify-no-changes` clean. Screenshots (fake API, dev-login-authenticated so the background `GET /api/me` cascading-auth check does not 401-redirect the page out from under the shot — see AGENT-NOTES): `Implementation/screenshots/p7-02-notifications-off-375.png`, `p7-02-notifications-ios-install-375.png`, `p7-02-notifications-on-375.png`, all `scrollWidth === 375`. **Verified for real, desktop-equivalent**: with real VAPID keys (`deploy/generate-vapid.ps1`) and headless Edge (Chromium engine) with `Notification` permission pre-granted via CDP `overridePermissions`, dev-logged-in against the real (non-fake) Api on fixtures — clicking "Turn on notifications" did a real `PushManager.subscribe` + `POST /api/push/subscriptions`, and "Send test notification" did a real `POST /api/push/test` that returned `NotificationResult.Sent`. **Manual pending (operator): iPhone home-screen install** — add to Home Screen from Safari, open from the icon (standalone), turn on notifications, receive the test push, tap it and confirm it opens the picks page in standalone rather than a new Safari tab. Exact steps: (1) On the iPhone, browse to the deployed app's URL in Safari and sign in; (2) tap Share -> Add to Home Screen; (3) open the app from its new Home Screen icon (not from Safari) so `navigator.standalone` is true; (4) go to Profile, tap "Turn on notifications", accept the permission prompt; (5) as commissioner, tap "Send test notification" (or have another commissioner trigger `POST /api/push/test`); (6) confirm the notification banner appears and that tapping it brings the already-open standalone app to the foreground (or opens it) at the target URL instead of a new Safari tab.  Hotfix D-163 (2026-09-19): the worker no longer intercepts /auth, /api, /health; found on the first real deployment because the OAuth callback was being served from the offline cache. |
| P7-03 | Reminder jobs and event notifications | sonnet-p7-03 | Sonnet | Done | p7-03-reminders | New `Infrastructure/Notifications/`: `NotificationMessages` (Feature 11 catalog texts, one method per entry — title is a short label, body is the exact catalog sentence with placeholders filled), `ReminderRecipients` (current-week unlocked-set lookup per league via `ISeasonWeekSource`/`SeasonCalendar.CurrentWeekAt`, and unsubmitted-member/picks-left queries, all evaluated at call time), `FridayMemberReminderJob`/`FridayCommissionerSummaryJob : IScheduledJob` (`0 20 * * 5`/`0 21 * * 5` ET), `SaturdayReminderOneShot : IOneShotJob` (due = `LockAtUtc - 1h`, re-read every tick so a moved lock produces a new occurrence per D-034), `GamesAddedNotificationHandler`/`GameRemovedNotificationHandler : IDomainEventHandler<...>`. `GameAddedToSet` already carries every added row's id per regeneration (P3-03), so the coalescing the card asks for needed no event-shape change. Recipients for GamesAdded are computed order-independently of P4-04's own handler on the same event (D-11x). New tests: `ReminderJobTests` (9, incl. one real-cron test through `SchedulerHarness`/`SchedulerTick`), `SaturdayOneShotTests` (5), `EventNotificationTests` (3) — 17 new, all against real fixture/HTTP flows with `FakePushSender`. `dotnet build` 0 warnings, `dotnet test` 569/569 green post-merge (218 Domain.Tests + 351 Api.Tests), `dotnet format --verify-no-changes` clean. Merged `main` (through 15514b1: P3-04 regeneration, P4-01 picks) — one hot-spot conflict in `Infrastructure/DependencyInjection.cs` (two `using` lines added on both sides), resolved by keeping both. Opus review pending.  Opus review merged (D-113..D-115): per-league try/catch in Friday jobs, Saturday one-shot skips once LockAtUtc passed, GameRemoved excludes removed members. Open for P8-01: GamesAdded re-fires on a second add because SubmittedUtc is never cleared (compare against AddedUtc of prior games); off-season guard on Friday jobs; catalog #2 pluralisation.  **P8-01 closed the first two** (branch `p8-01-security-review`, report §9): D-155 — `GamesAddedNotificationHandler` now compares `SubmittedUtc` against the newest `AddedUtc` among the rows already in the set, and reads `WeekSubmissions.Status` not at all, so it is more order-independent of P4-04 than before and a member who never re-submitted is told once; D-154 — `ReminderRecipients.CurrentUnlockedSetsAsync` skips a season year whose `SeasonCalendar.CurrentWeekAt` state is not `InSeason`. Catalog #2 pluralisation is copy, not correctness: **left for P8-04**. |
| P8-01 | Security and correctness review | opus-p8-01 | Opus | Done | p8-01-security-review | Report: `Implementation/reviews/security-review.md`, one section per check with its evidence; linked from `00-README.md`'s table. **Route inventory is now executable** (D-157): `RouteInventoryTests` reads the booted app's `EndpointDataSource` and asserts every `/api` endpoint has a policy and is not anonymous, every `/api/leagues/{leagueId}/**` route carries the membership filter, every mutation is under the CSRF group, only `/health`, `/health/ready`, `/auth/login/google` and the Development-only `/auth/dev-login` are anonymous, no mutation lives outside `/api` bar `POST /auth/logout`, and every route table row in `03-API-Contracts.md` is mapped (`Infrastructure/ApiContractRoutes.cs`). The scoping helpers stamp `EndpointScopeMetadata`/`CsrfProtectedMetadata` next to their filters to make that possible, since an `IEndpointFilter` is invisible in metadata. `GeneratedAuthMatrixTests` then probes that same inventory over HTTP (401 anonymous everywhere, 404 stranger on every league route, 403 member on every commissioner route, 403 non-commissioner on every admin route, 400 for a mutation with no `X-Requested-With`) — no auth-matrix group was missing; this adds the groups that only had indirect coverage. New: `CookieSecurityTests` (cookie flags from options **and** from the wire after a real Google sign-in; `/api` 401 ProblemDetails with no redirect; `/auth` 302 to `/login`), `ProductionBehaviourTests` (real host at `ASPNETCORE_ENVIRONMENT=Production`: a throwing endpoint returns 500 `application/problem+json` with no message, type name, stack frame or `stackTrace`; `/auth/dev-login`, `/api/admin/fixture/*` and `/api/push/test` are not mapped), `RateLimitTests`, `NeverRulesTests` (one unlocked week, all five pick-reading surfaces), `CorrectionEdgeCaseTests`. **Rate limiting (D-153)**: fixed one-minute window per client IP, 30/min on `/auth/*` and 20/min on `/api/invites/*`, 429 ProblemDetails + `Retry-After`; `RateLimiting__*` keys documented in `deploy/.env.example` and the Production template; off in the test suite (`ApiFactory`) because every in-memory request shares one partition. **Three "never" rules all hold**; the `DateTime.UtcNow` grep is **zero hits** in Domain, Infrastructure, Api and Shared — the only 13 hits are in `NcaafPickEm.Web`, all rendering the viewer's own wall clock or inside `DEBUG && USE_FAKE_API` fakes. **Dependency audit clean**: `dotnet list package --vulnerable --include-transitive` reports no vulnerable package in any of the 8 projects, so `Directory.Packages.props` is untouched; xunit 2.x is `Legacy`-deprecated (pinned on purpose) and the ASP.NET packages sit one patch behind the installed runtime deliberately. **Secrets clean**: no the key's first characters, no bearer tokens, no VAPID keys, no tracked `.env`/`*.key`/`appsettings.{Development,Production}.json`. Fixes: `ReturnUrl.Sanitize` now refuses a control character (a mid-string CR/LF reached the `Location` header); plus the P5-02 and P7-03 fixes in their own rows. D-153..D-157 logged. Three hot-spot rows below. `dotnet build` 0 warnings, `dotnet test` **822/822** green post-merge (306 Domain + 516 Api; 821 before merging main @ 969c461, which brought P5-05 and P8-03), `dotnet format --verify-no-changes` clean. Open for P8-03/P8-04: see the report's §10 accepted risks and §11 findings 8-10. |
| P8-02 | Home server deployment | sonnet-p8-02 | Sonnet | Done with notes | p8-02-deploy | `deploy/`: `install-service.ps1` (publish Release trimmed/Brotli, create the `NcaafPickEm` Windows service via `sc.exe create`, write `.env`'s keys into the service's own registry `Environment` (not machine-wide), `sc.exe failure` auto-restart, start + poll `/health/ready`; idempotent re-runs), `deploy.ps1` (Saturday-window guard via `[TimeZoneInfo]::ConvertTime`/"Eastern Standard Time", `-Force` override, `dotnet ef database update` before stop/republish/start, `/health/ready` poll with last-20-log-lines on failure, `-WhatIf` throughout), `renew-cert.ps1` + `register-renew-cert-task.ps1` (monthly), `backup.sql` + `backup.ps1` (nightly, 30-day prune) + `register-backup-task.ps1` (daily 03:45), `restore-verify.ps1` (restore latest `.bak` into `<db>_RestoreCheck`, `DBCC CHECKDB`, row counts, drop), `.env.example` (every key from `01-Architecture.md` plus deploy-only `BACKUP_*`; `.env` already covered by the existing `.gitignore` `.env`/`.env.*`/`!.env.example` rules), `appsettings.Production.template.json` completed (added `Kestrel:Certificates:Default`, `AllowedHosts`). Added `Microsoft.Extensions.Hosting.WindowsServices` + one `Program.cs` line, `builder.Host.UseWindowsService()` (D-103, hot-spot row below) — required for the SCM to see start/stop status at all, not an enhancement. D-101..D-105 logged (dropped `WITH COMPRESSION` from `backup.sql` — Express-only rejection, confirmed against LocalDB; routed `backup.ps1` around a real `sqlcmd -v`-plus-colon parsing bug on this machine's sqlcmd 15.0.1300, confirmed with half a dozen quoting variants; `UseWindowsService()`; registry-environment vs. machine-wide; PEM `Kestrel:Certificates:Default` vs. `.pfx`). README gained "Deploy" (first-time setup walkthrough, redeploy, Tailscale HTTPS, backups) and "Operate" (data status page, corrections, backup/Saturday/logs/service-control) sections. All 8 `.ps1` files parse clean (`[System.Management.Automation.Language.Parser]::ParseFile`); `install-service.ps1 -WhatIf` and `deploy.ps1 -WhatIf` both dry-run cleanly end to end (including the Saturday-window branch and a missing-service warning path) against a scratch `.env`. `dotnet publish -c Release` to a temp folder confirmed `NcaafPickEm.Api.exe`, 59 `.br` framework files, `appsettings.json` present; ran it with `ASPNETCORE_ENVIRONMENT=Production` + a LocalDB connection string + placeholder Google/Push/Cfbd keys — `/health`, `/health/ready`, and `/` all returned 200 — then re-ran with `-WorkingDirectory C:\Windows\System32` (mimicking the SCM) to confirm content-root resolution does not depend on the process's working directory; stopped and deleted the temp output afterward. `backup.ps1` and `restore-verify.ps1` run end to end against the real LocalDB `NcaafPickEm` database into a temp folder (backup succeeded 3.9 MB; restore + `DBCC CHECKDB` + row counts + drop all succeeded); temp output deleted. `dotnet build` 0 warnings, `dotnet test` 482/482 green (203 Domain.Tests + 279 Api.Tests), `dotnet format --verify-no-changes` clean. **Manual pending (operator)** — cannot be done by an agent (no home server, no phone, no tailnet access): app reachable from a phone on the tailnet over HTTPS (date: ____), Google login round-trips on the deployed server (date: ____), service survives a reboot (date: ____), backup file appears the next morning (date: ____). Exact commands for each are in README "Deploy" and the OPEN ITEMS list in this task's final report. |
| P8-01 | Security and correctness review | opus-p8-01 | Opus | Done | p8-01-security-review | Report: `Implementation/reviews/security-review.md`, one section per check with its evidence; linked from `00-README.md`'s table. **Route inventory is now executable** (D-157): `RouteInventoryTests` reads the booted app's `EndpointDataSource` and asserts every `/api` endpoint has a policy and is not anonymous, every `/api/leagues/{leagueId}/**` route carries the membership filter, every mutation is under the CSRF group, only `/health`, `/health/ready`, `/auth/login/google` and the Development-only `/auth/dev-login` are anonymous, no mutation lives outside `/api` bar `POST /auth/logout`, and every route table row in `03-API-Contracts.md` is mapped (`Infrastructure/ApiContractRoutes.cs`). The scoping helpers stamp `EndpointScopeMetadata`/`CsrfProtectedMetadata` next to their filters to make that possible, since an `IEndpointFilter` is invisible in metadata. `GeneratedAuthMatrixTests` then probes that same inventory over HTTP (401 anonymous everywhere, 404 stranger on every league route, 403 member on every commissioner route, 403 non-commissioner on every admin route, 400 for a mutation with no `X-Requested-With`) — no auth-matrix group was missing; this adds the groups that only had indirect coverage. New: `CookieSecurityTests` (cookie flags from options **and** from the wire after a real Google sign-in; `/api` 401 ProblemDetails with no redirect; `/auth` 302 to `/login`), `ProductionBehaviourTests` (real host at `ASPNETCORE_ENVIRONMENT=Production`: a throwing endpoint returns 500 `application/problem+json` with no message, type name, stack frame or `stackTrace`; `/auth/dev-login`, `/api/admin/fixture/*` and `/api/push/test` are not mapped), `RateLimitTests`, `NeverRulesTests` (one unlocked week, all five pick-reading surfaces), `CorrectionEdgeCaseTests`. **Rate limiting (D-153)**: fixed one-minute window per client IP, 30/min on `/auth/*` and 20/min on `/api/invites/*`, 429 ProblemDetails + `Retry-After`; `RateLimiting__*` keys documented in `deploy/.env.example` and the Production template; off in the test suite (`ApiFactory`) because every in-memory request shares one partition. **Three "never" rules all hold**; the `DateTime.UtcNow` grep is **zero hits** in Domain, Infrastructure, Api and Shared — the only 13 hits are in `NcaafPickEm.Web`, all rendering the viewer's own wall clock or inside `DEBUG && USE_FAKE_API` fakes. **Dependency audit clean**: `dotnet list package --vulnerable --include-transitive` reports no vulnerable package in any of the 8 projects, so `Directory.Packages.props` is untouched; xunit 2.x is `Legacy`-deprecated (pinned on purpose) and the ASP.NET packages sit one patch behind the installed runtime deliberately. **Secrets clean**: no the key's first characters, no bearer tokens, no VAPID keys, no tracked `.env`/`*.key`/`appsettings.{Development,Production}.json`. Fixes: `ReturnUrl.Sanitize` now refuses a control character (a mid-string CR/LF reached the `Location` header); plus the P5-02 and P7-03 fixes in their own rows. D-153..D-157 logged. Three hot-spot rows below. `dotnet build` 0 warnings, `dotnet test` **822/822** green post-merge (306 Domain + 516 Api; 821 before merging main @ 969c461, which brought P5-05 and P8-03), `dotnet format --verify-no-changes` clean. Open for P8-03/P8-04: see the report's §10 accepted risks and §11 findings 8-10. |
| P8-02 | Home server deployment | sonnet-p8-02 | Sonnet | Done with notes | p8-02-deploy | `deploy/`: `install-service.ps1` (publish Release trimmed/Brotli, create the `NcaafPickEm` Windows service via `sc.exe create`, write `.env`'s keys into the service's own registry `Environment` (not machine-wide), `sc.exe failure` auto-restart, start + poll `/health/ready`; idempotent re-runs), `deploy.ps1` (Saturday-window guard via `[TimeZoneInfo]::ConvertTime`/"Eastern Standard Time", `-Force` override, `dotnet ef database update` before stop/republish/start, `/health/ready` poll with last-20-log-lines on failure, `-WhatIf` throughout), `renew-cert.ps1` + `register-renew-cert-task.ps1` (monthly), `backup.sql` + `backup.ps1` (nightly, 30-day prune) + `register-backup-task.ps1` (daily 03:45), `restore-verify.ps1` (restore latest `.bak` into `<db>_RestoreCheck`, `DBCC CHECKDB`, row counts, drop), `.env.example` (every key from `01-Architecture.md` plus deploy-only `BACKUP_*`; `.env` already covered by the existing `.gitignore` `.env`/`.env.*`/`!.env.example` rules), `appsettings.Production.template.json` completed (added `Kestrel:Certificates:Default`, `AllowedHosts`). Added `Microsoft.Extensions.Hosting.WindowsServices` + one `Program.cs` line, `builder.Host.UseWindowsService()` (D-103, hot-spot row below) — required for the SCM to see start/stop status at all, not an enhancement. D-101..D-105 logged (dropped `WITH COMPRESSION` from `backup.sql` — Express-only rejection, confirmed against LocalDB; routed `backup.ps1` around a real `sqlcmd -v`-plus-colon parsing bug on this machine's sqlcmd 15.0.1300, confirmed with half a dozen quoting variants; `UseWindowsService()`; registry-environment vs. machine-wide; PEM `Kestrel:Certificates:Default` vs. `.pfx`). README gained "Deploy" (first-time setup walkthrough, redeploy, Tailscale HTTPS, backups) and "Operate" (data status page, corrections, backup/Saturday/logs/service-control) sections. All 8 `.ps1` files parse clean (`[System.Management.Automation.Language.Parser]::ParseFile`); `install-service.ps1 -WhatIf` and `deploy.ps1 -WhatIf` both dry-run cleanly end to end (including the Saturday-window branch and a missing-service warning path) against a scratch `.env`. `dotnet publish -c Release` to a temp folder confirmed `NcaafPickEm.Api.exe`, 59 `.br` framework files, `appsettings.json` present; ran it with `ASPNETCORE_ENVIRONMENT=Production` + a LocalDB connection string + placeholder Google/Push/Cfbd keys — `/health`, `/health/ready`, and `/` all returned 200 — then re-ran with `-WorkingDirectory C:\Windows\System32` (mimicking the SCM) to confirm content-root resolution does not depend on the process's working directory; stopped and deleted the temp output afterward. `backup.ps1` and `restore-verify.ps1` run end to end against the real LocalDB `NcaafPickEm` database into a temp folder (backup succeeded 3.9 MB; restore + `DBCC CHECKDB` + row counts + drop all succeeded); temp output deleted. `dotnet build` 0 warnings, `dotnet test` 482/482 green (203 Domain.Tests + 279 Api.Tests), `dotnet format --verify-no-changes` clean. **Manual pending (operator)** — cannot be done by an agent (no home server, no phone, no tailnet access): app reachable from a phone on the tailnet over HTTPS (date: ____), Google login round-trips on the deployed server (date: ____), service survives a reboot (date: ____), backup file appears the next morning (date: ____). Exact commands for each are in README "Deploy" and the OPEN ITEMS list in this task's final report. **Superseded by P8-05 for the Linux/Docker target** (D-158): the home server is a headless Linux box running Docker, so the primary deployment is now `Dockerfile` + `deploy/docker/compose.yaml`. These scripts are kept, still work, and are documented as the alternative (README "Deploy" -> "Alternative: Windows service"); D-101..D-105 remain valid for that path. Two settings must stay as they are here and are inverted only on Docker: `App__BehindProxy` unset (Kestrel binds HTTPS itself, so trusting `X-Forwarded-*` would be a spoofing hole) and `Database__MigrateOnStartup` false (D-015 - `deploy.ps1` migrates as its own step). The operator-checklist items that were this row's are now items 3-5 in Docker form, with the Windows walkthrough kept as item 5a. |
| P8-03 | End-to-end season simulation | opus-p8-03 | Opus | Done with notes | p8-03-simulation | `tests/NcaafPickEm.Api.Tests/Simulation/FullWeekSimulationTests`: one `[Fact]` over its own `SqlTestDatabase`/`ApiFactory` with a movable `FixedTimeProvider` and `FakePushSender`. Week 6: Michael creates the league through `POST /api/leagues`, one invite code, Alyson/Dance/Alex/Daniel/Sam accept (`JoinedWeek` 6), `[Top25]` saved, and a prior completed week seeded over three bespoke week-6 `Games` rows and closed by the real `ScoringService` (snapshot `ThroughWeek=6`). Week 7: the real `SchedulerTick` at Tue 03:30 ET runs `RegenerateGameSetsJob` (3 Top 25 games), commissioner manually adds Maryland/Rutgers, Iowa State/Kansas (15 pts via the point-override route), Oregon/Washington and San Jose State/Hawai'i; picks reproduce the Overview exactly, Alex leaves 2 unpicked, Sam never starts (D-151). Ticks at Fri 20:00/21:00 ET and Sat 11:00 ET assert reminder recipients and catalog body text; the tick at Sat 12:00 ET (the set's `LockAtUtc`) locks the week Locked x4 / Incomplete x2, picks 409 afterwards, and Dance's and Alyson's dashboards match `WorkItems/Overview.txt` character for character. Six `SaturdayPoller.PollOnceAsync` snapshots walk the scores (tie on the data-status needs-review list at 4; the post-midnight finish counts at 6), then a real void and a real override close the week: 65/50/45/30/20/0, `IsComplete`, 12 snapshot rows, season ranks 1-6, grid cells (Voided/Correct/Incorrect/NoPick) and Up/Down/Same trend arrows. Operator CLI `dotnet run --project src/NcaafPickEm.Api -- simulate [--week n] [--snapshot 1-6] [--lock] [--tick <instant>] [--league <name>]` (D-152, `Api/Simulation/`), Development + Fixture only, run against LocalDB for all four modes. Docs: README "Operate" -> "Dry-run a week before the season", AGENT-NOTES "End-to-end simulation", traceability rows for Features 01/02/04/06/07/11. **Manual pending (operator)**: the iPhone walkthrough on the deployed server - 21 numbered steps with the screenshot names to save are in `Implementation/screenshots/e2e/README.md`. |
| P8-04 | Traceability audit and docs | sonnet-p8-04 | Sonnet | Done | p8-04-traceability | Every row in `07-Traceability.md` now has a `Result` column (106 AC-group rows across the 13 feature tables): **101 PASS** (grep-verified against `tests/` — real class/method confirmed to exist; 12 rows corrected a stale/never-real class or method name from the Proof column to the real one, e.g. `InviteTests`/`StandingsTests`/`MembershipTests`/`WeekLeaderboardTests`/`MeTests` never existed, `TiesShareRank`/`LockAtIsEarliestKickoff`/`NoPickScoresZero`/`RefreshTwice_NoDuplicates`/`FinalOnlyOnFinal`/`PointRulesChange_KeepsPicks` were never the actual method names), **5 rows MANUAL PENDING (operator)** (iOS standalone e2e walkthrough, iOS Safari grid pinned-column scroll, physical iPhone manifest/install, first real nightly backup landing, real receipt-and-tap-opens-standalone push — each points at the doc with the exact steps), **0 GAP** (every stale reference resolved to real coverage, nothing left unproven). Cross-checked every acceptance-criteria group in all 13 `WorkItems/*.txt` stories against the table; no missing rows found. Root `README.md` rewritten into the required structure (what it is / architecture summary with stack table, solution layout, runtime-shape diagram, provider hybrid, job schedule with Eastern times / run locally / run tests incl. opt-in `CfbdLiveTests` and the fake-API build-symbol recipe / deploy / operate incl. corrections, audit page, needs-review, stale-scores guidance / where the plan lives), every prior fact preserved. `Implementation/00-README.md`'s index now lists `AGENT-NOTES.md`, `reviews/operator-checklist.md`, `spikes/providers.md`, `spikes/wasm-load-time.md`, `screenshots/`, `screenshots/e2e/README.md`. New `Implementation/reviews/operator-checklist.md` collects every "Manual pending (operator)" item from this board into one ordered, numbered checklist. Final sweep clean: no merge-conflict markers in `Implementation/README.md/src/tests/deploy`, no duplicate `D-NNN` ids, every board row Done except this one. `dotnet build` 0 warnings, `dotnet test` 822/822 green (306 Domain.Tests + 516 Api.Tests), `dotnet format --verify-no-changes` clean. No new decisions needed (next free slot D-158 confirmed unused). |
| P8-05 | Docker deployment and CI | opus-p8-05 | Opus | Done | p8-05-docker | Root `Dockerfile` (multi-stage: layer-cached restore from global.json + both `Directory.*.props` + the slnx + every csproj, then `dotnet publish src/NcaafPickEm.Api -c Release`; Debian `aspnet:10.0` runtime for tzdata+ICU, non-root `app` uid 1654, `/app/keys` + `/app/logs` volumes, curl `HEALTHCHECK` on `/health/ready`, OCI source label) and `.dockerignore`. **No `wasm-tools` workload needed** - the SDK image publishes the hosted Blazor client with the existing Release settings unchanged; `tests/NcaafPickEm.Fixtures` must be copied into the build because Infrastructure takes a real `ProjectReference` on it (P2-05). App changes, each a no-op unless its key is set: `Api/Hosting/ForwardedHeadersSetup` (`App__BehindProxy`, `XForwardedFor|Proto|Host`, `KnownIPNetworks`/`KnownProxies` cleared, first in the pipeline - D-160, and note `KnownIPNetworks` because `KnownNetworks` is `ASPDEPR005`-obsolete in .NET 10), `Api/Hosting/DataProtectionSetup` (`DataProtection__KeysPath`, key ring on a volume so a redeploy does not sign everyone out - D-161), `Serilog__LogDirectory`, and `DatabaseMigratorHostedService` now waits for SQL Server with backoff up to `Database__StartupTimeoutSeconds` (default 120) plus `DatabaseStartupState` so `/health/ready` answers 503 while migrating (D-159). **Real bug found by running the stack**: the wait originally used `Database.CanConnectAsync()`, which opens the *application* database and answers false when it does not exist yet - i.e. on a brand-new container, the exact deploy it exists to support - so it now probes `master` with a raw `SqlConnection`; covered by a regression test. `deploy/docker/`: `compose.yaml` (db mssql 2022 `MSSQL_PID=Developer` + 2 GB cap + uid-10001 bind mount + sqlcmd healthcheck; api on the GHCR image with `depends_on: service_healthy`, `ConnectionStrings__Default` - not `DefaultConnection` - `Database__MigrateOnStartup=true`, `App__BehindProxy=true`, keys/logs volumes; watchtower scoped to `ncaaf-api` with `DOCKER_API_VERSION=1.44`; every port published `127.0.0.1:` only with the UFW/iptables reason), `.env.example`, `backup.sh` (host cron 03:45, `WITH CHECKSUM, INIT` into the mssql bind mount so the host sees the `.bak`, 30-day prune, sa password read from the container's own environment so it never reaches the host process list), `restore-verify.sh` (restore as `NcaafPickEm_RestoreCheck`, `DBCC CHECKDB`, row counts, drop - including on failure), `compose.dev.yaml` (local offline full-stack: builds from the tree, fixtures, demo league, Development so dev-login works). CI: `.github/workflows/ci.yml` (build + `dotnet format --verify-no-changes` + both test projects against an `mssql:2022` service container via `TEST_SQL_CONNECTION`, which `SqlTestDatabase` reads as a **server-level** string) and `.github/workflows/ghcr-build.yml` (buildx -> `ghcr.io/<owner>/<repo>`, tags `latest`/`sha-<short>`/semver, `GITHUB_TOKEN` with `packages: write`, GHA cache, `linux/amd64` only with the reason in the file - the Dockerfile builds inside the SDK image, so arm64 would mean QEMU; nothing in the app is arch-specific). **Local Docker validation done for real** (Docker Desktop 29.6.2, linux/x86_64): `docker build` clean; `compose.dev.yaml` up -> both containers `(healthy)`; logs show `Applying 3 pending migration(s)` -> `Database schema is now current` -> fixture seeder -> `Job scheduler heartbeat for occurrence 2026-09-19T14:55:00-04:00` (the `-04:00` proves tzdata+ICU resolve `America/New_York` in the container); `/health` and `/health/ready` both 200; `GET /auth/dev-login?user=michael` 302 + `ncaaf.auth` cookie -> `GET /api/me` 200 with the demo league; `GET /` 200 and `/_framework/blazor.webassembly.js` 200 (hosted WASM served); `docker run --rm ncaaf-pickem:local generate-vapid` printed a pair and exited (the hidden CLI argument works as a container argument); `id` = `uid=1654(app)`; the key ring and the rolling log file both landed on their volumes; image 549 MB; torn down with `down -v` and the local image removed. Also confirmed `builder.Host.UseWindowsService()` is a genuine no-op on Linux (`WindowsServiceHelpers.IsWindowsService()` short-circuits off `RuntimeInformation.IsOSPlatform(OSPlatform.Windows)`; the app booted, served and shut down normally with the call in place). New tests: `ForwardedHeadersTests` (4), `DataProtectionKeyRingTests` (2), `DatabaseMigratorStartupTests` (4), plus one readiness-gate case in `HealthEndpointTests` - 11 new. `dotnet build` 0 warnings, `dotnet test` **833/833** green (306 Domain.Tests + 527 Api.Tests), `dotnet format --verify-no-changes` clean. Docs: README "Deploy" rewritten Docker-first (prereqs, directory prep with both uids, `.env`, `docker compose up -d`, `tailscale serve`, Google redirect URI, VAPID via `docker run`, first-run checks, updating via watchtower or `pull && up -d`, Saturday no-deploy rule, backups cron, SQL access via `tailscale serve --tcp 1433`, local full-stack check, Windows service kept as "Alternative"), `01-Architecture.md` runtime shape + configuration, `AGENT-NOTES.md` "Docker", `reviews/operator-checklist.md` items 3-5 replaced with the Docker steps (5a keeps the Windows path), `deploy/appsettings.Production.template.json` (new `Database:StartupTimeoutSeconds`, `DataProtection:KeysPath`, `App:BehindProxy`, `Serilog:LogDirectory` with comments). D-158..D-162 logged. Four hot-spot rows below. **Manual pending (operator)** - cannot be done by an agent (no home server, no phone, no tailnet): app reachable from a phone on the tailnet over HTTPS (date: ____), Google login round-trips on the deployed server (date: ____), host reboot brings both containers back healthy (date: ____), an image update leaves a signed-in phone signed in (date: ____), first backup file appears the next morning (date: ____). CI itself is unproven until the first push to GitHub - no remote exists in this environment.  Verified on GitHub: CI green at 25901ec (306 Domain + 527 Api tests on ubuntu against a mssql service container) and image published to ghcr.io/michael-dunn/ncaaf-pickem:latest. **Deployed to `dunn-home-server` 2026-09-19** from image rev `b56ac7d` (checklist item 3): both containers `(healthy)`, 3 migrations applied, `-04:00` heartbeat, `/health` + `/health/ready` 200, key ring + log on their volumes; `backup.sh` + `restore-verify.sh` both passed by hand the same day and the 03:45 cron is installed (item 5 - first cron-driven `.bak` still to be confirmed 2026-09-20). Host-specific deviations (api `127.0.0.1:5001`, db `127.0.0.1:1434`, Tailscale `:8443`, shared `pennypulse-watchtower`, compose under `/srv/dunn-home-server/services/ncaaf-pickem/`) are recorded in the home-server repo's `services/ncaaf-pickem/README.md` and in checklist item 3. Still operator-pending: `sudo tailscale serve --bg --https=8443 http://127.0.0.1:5001`, the `:8443` redirect URI on the Google client, `CFBD_API_KEY`, and every phone-side check (item 4). |
| P8-06 | Reference-data bootstrap on fresh deployment | opus-p8-06 | Opus | Done | p8-06-bootstrap | Hotfix for the first production deployment: with `Providers__ReferenceData=Cfbd` the calendar comes from the `SeasonWeeks` table, which only the Tuesday 03:10 ET job ever fills, so a container started any other day had no calendar, no teams and no games until the following Tuesday — `GET /api/seasons/2026/weeks` empty, the create-league page on its "weeks default to 1..14" hint, `POST /api/leagues` answering 400 `InvalidWeekRange` (the report said 409; the mapping is 400), and `POST /api/admin/refresh/*` locked behind a commissioner that cannot exist before the first league. Three fixes. **(1) D-164** `Infrastructure/Jobs/Refresh/ReferenceDataBootstrap` + `ReferenceDataBootstrapHostedService`: a `BackgroundService` registered after `DatabaseMigratorHostedService` that, when the season has no `SeasonWeeks` rows **or** `Teams` is empty, runs calendar -> teams -> this week's and next week's schedule -> rankings -> lines through the existing `ReferenceDataIngestService` (~7 provider calls) and logs one `Reference data bootstrap ...` summary. Gated on `Providers:BootstrapOnStartup` if set, else `Providers:ReferenceData=Cfbd` **and** `Jobs:Enabled` — a no-op in Fixture mode and in every test host, twice over. Yields immediately so `/health/ready` stays migration-only; a failure is a Warning plus the `DataRefreshStatus.LastError` the ingest already writes, never an exception; overlapping the scheduler's first tick is harmless because the ingest is idempotent. The "already populated?" check reads the tables, not `ISeasonWeekSource`, whose cache is process-static. **(2) D-165** `LeagueService.CreateAsync` with zero calendar rows now falls back to the new `SeasonCalendar.DefaultLeagueRangeWithoutCalendar` (1-14), logs a Warning and uses the first week as `JoinedWeek`, instead of refusing — the API finally agrees with what the page promises; with a calendar on file the strict `ValidateWeekRange` is untouched. **(3) D-166** `GET /api/admin/data-status` and `POST /api/admin/refresh/{dataType}` are now `RequireAnyLeagueCommissioner(allowWhenNoLeaguesExist: true)` (commissioner of any league **or** the `Leagues` table is empty), so `/api/admin/*` is scoped per route; `unmatched/{id}/resolve` keeps the strict scope. `EndpointScopeMetadata` is unchanged, so `RouteInventoryTests`/`GeneratedAuthMatrixTests` needed no edit (their scenario always has a league). UI: the create-league hint now reads "No season calendar for {year} yet - the server is fetching it; refresh this page in a minute, or create the league now with the default weeks 1..14." New `ReferenceDataBootstrapTests` (4: empty database ingests calendar/teams/games/rankings/lines and sets every `DataRefreshStatus`, 6-8 provider calls; a second start makes zero calls; a failing provider leaves `/health/ready` 200 with `LastError` recorded; Fixture mode never runs it) against their own `SqlTestDatabase`/`ApiFactory` with `Providers:ReferenceData=Cfbd` and a counting provider that serves the Week 7 2026 fixture payloads plus the CFBD-shaped calendar the fixture set lacks; `ApiFactory` gained a `settings` parameter for that. Plus 2 `LeagueEndpointsTests` (empty-calendar create succeeds 1..14; out-of-range on a season that has a calendar still 400) and 2 `AdminEndpointsTests` (zero-leagues access allowed and writes no audit row; a member with a league present still 403). `dotnet build` 0 warnings, `dotnet test` **841/841** green (306 Domain + 535 Api), `dotnet format --verify-no-changes` clean. Docs: README "Operate" first-start bullet + jobs table row, AGENT-NOTES "Reference data ingest", 03-API-Contracts "Data admin" bootstrap-exception note, D-164..D-166. One hot-spot row below. **Operator note**: the deployed instance needs `CFBD_API_KEY` set before a restart can bootstrap anything; the first start after that spends ~7 of the month's free-tier calls. |
| P8-07 | CFBD ingest hardening for live data | opus-p8-07 | Opus | Done | p8-07-ingest-live-data | Hotfix for the first live CFBD ingest, which failed both of its reference-data steps on the real 2026 payload. **Calendar (D-167)**: CFBD's `/calendar?year=2026` returns 16 rows — 15 regular weeks plus one `Postseason` row that it also numbers **1**, so the ingest tried to track two `SeasonWeeks` with the same `(SeasonYear, Week)` and EF threw before anything was saved. New `CfbdCalendarNormalization.NormalizeSeason` (returning `CalendarNormalizationResult`) drops non-regular rows (counted into `CalendarIngestResult.SkippedNonRegular`, logged at Information), keeps the first row of any repeated week number (Warning), and flags the highest surviving week `IsRegularSeason = false` because it is conference-championship week — which is what 04 section 1 has always required and what makes a 2026 league default to weeks 1..14. Season-type matching is one case-insensitive `IsRegularSeasonType` helper (the Kiota client says `"Regular"`, the raw JSON `"regular"`). **Aliases (D-168)**: `IX_TeamAliases_Source_Alias` is unique under a case-insensitive collation, and the real 682-team payload hands the same alias to two schools (`Tiffin`) and the same text in two casings (`Alma`/`ALMA`); P2-02 only guarded against repointing an *existing* row, so the collisions sat inside one `SaveChangesAsync` and SQL Server rejected the whole teams ingest. `UpsertAliasesAsync` now claims aliases through an `OrdinalIgnoreCase` map spanning existing rows **and** the whole batch, first claimant wins, skips an alias that only repeats the team's own `School` (`TeamNameIndex` indexes `School` directly, so it is pure noise), and emits one summary Warning with up to 5 examples instead of one line per collision. **Lengths (D-169)**: `Team`/`Conference` gained `*MaxLength` consts (School/Name 100, Mascot 100, Abbreviation 20, LogoUrl 300), the EF configurations now read them, and every provider string is truncated before it is stored. **Abbreviations (D-171)**: no schema change was needed — `Conferences.Abbreviation` is NOT NULL with empty allowed and `CfbdMapping` already maps null to empty, `Teams.Abbreviation` is already nullable — so the fix is in the read models: `GameSetGameDtoMapper.ToTeamDto`/new `ToConferenceDto` fall back to the school/conference name when the abbreviation is blank (46 of 72 live conferences have none), and `ReferenceEndpoints` now goes through the mapper instead of projecting inline. **Bootstrap (D-170)**: verified, not changed — the teams step already runs after a failed calendar step, and a partially failed run repeats on the next start because the guard is `hasCalendar && hasTeams`. New `CfbdLiveTests.GivenTheRealCfbdApi_WhenBootstrappingTheCurrentSeason_ThenEveryStepSucceeds` (`Category=Live`, no-op unless `CFBD_LIVE=1` and `Cfbd__ApiKey`) boots `ApiFactory` with `Providers:ReferenceData=Cfbd` plus the key, lets the real bootstrap run against a throwaway database, and asserts every step, exactly one non-regular week, and no `DataRefreshStatus.LastError`. Other tests: 3 new + 1 rewritten in `ReferenceIngestTests` (15 regular weeks + a postseason week 1 stored as 15 rows twice over, week 15 not regular, `DefaultLeagueRange` 1..14; the two-teams-one-alias/self-School/repeat-casing case; an over-length `School` truncated rather than rejected) and 4 new in `CfbdCalendarNormalizationTests` (case-insensitive season type as a Theory, the real 2026 shape, a repeated week number, an all-postseason payload). `dotnet build` 0 warnings, `dotnet test --filter "Category!=Live"` **853/853** green (306 Domain + 547 Api), `dotnet format --verify-no-changes` clean. Docs: AGENT-NOTES "Reference data ingest", 04 section 1 (postseason rows and the championship-week flag), D-167..D-171. Live verification: the orchestrator re-runs the bootstrap against the real API. |
| P9-01 | Remove the Windows deployment path | sonnet-p9-01 | Sonnet | Done | p9-01-remove-windows | Deleted the ten Windows deploy scripts/templates (`install-service.ps1`, `deploy.ps1`, `renew-cert.ps1`, `register-renew-cert-task.ps1`, `backup.ps1`, `backup.sql`, `restore-verify.ps1`, `register-backup-task.ps1`, `.env.example`, `appsettings.Production.template.json`) plus `deploy/generate-vapid.ps1` per Q5, leaving `deploy/docker/` as the only deployment path. `Program.cs` lost `builder.Host.UseWindowsService()` and its P8-02 comment; `NcaafPickEm.Api.csproj` and `Directory.Packages.props` lost the `Microsoft.Extensions.Hosting.WindowsServices` reference/version and its comment. Docs updated to match: README.md (deleted "Alternative: Windows service", the `deploy.ps1` Saturday-guard sentences in "Updating"/"Operate", every `deploy/generate-vapid.ps1` reference now points at `dotnet run --project src/NcaafPickEm.Api -- generate-vapid` or `docker run --rm <image> generate-vapid`, the `install-service.ps1` layout note, and the stale "On Windows" logs/service-control asides), `01-Architecture.md` (dropped the Windows-service paragraph and the deleted-file tree lines, fixed the two "Windows-service path" mentions and the `deploy/.env.example` reference in Configuration), `reviews/operator-checklist.md` (dropped item 5a), `screenshots/e2e/README.md` (step 1 now `docker compose pull api && docker compose up -d api`), `AGENT-NOTES.md` (VAPID bullets point at `dotnet run -- generate-vapid`), `07-Traceability.md` (the backup and no-Saturday-deploys rows now cite `deploy/docker/backup.sh`/`restore-verify.sh` and the watchtower Saturday note instead of the deleted `.ps1` files), `appsettings.Development.template.json`'s Push comment, and the `Program.cs` comment above the `generate-vapid` command. Added a "Superseded by Phase 9 (P9-01)" note at the top of P8-02's section in `Phase-8-Hardening-Deploy.md`. D-173 logged (supersedes D-101..D-105). `dotnet build -c Release` 0 warnings; `dotnet format --verify-no-changes` clean; `dotnet test tests/NcaafPickEm.Domain.Tests` 306/306; `dotnet test tests/NcaafPickEm.Api.Tests` 550/550 (LocalDB). Done-when grep (`install-service\|deploy.ps1\|renew-cert\|register-.*-task\|UseWindowsService\|WindowsServices`) confirmed hits only in `DECISIONS.md`, `STATUS.md`, `Phases/Phase-8-Hardening-Deploy.md`, and this phase's own `Phase-9-Tailscale-Auth.md`. No functional change; nothing Google- or auth-related touched (P9-03's scope). |

## Gates

| Phase | Passed at commit | Date | Notes |
|---|---|---|---|
| 0 | fd084d1 (P0-01..P0-05), 72c2c0e (P0-06) | 2026-09-18 | All six Phase 0 tasks merged; 157 tests green. Manual pending (operator): real Google login with a dev OAuth client; iPhone PWA install and load timing over Tailscale (steps in README and Implementation/spikes/wasm-load-time.md). |
| 1 | 76661d5 | 2026-09-18 | P1-01 (Opus-reviewed), P1-02, P1-03 merged; create/invite/join/roles/display names proven by LeagueEndpointsTests, InviteAcceptTests, RoleTests, DisplayNameTests and the auth matrix; league pages verified against the real API by P1-03 with dev-login. 332 tests green. |
| 2 | a05f134 | 2026-09-18 | P2-01..P2-05 merged (P2-02 Opus-reviewed with all-division fix; P2-03 Opus; P2-04 poller review pending post-merge). Exit criteria: offline fixtures + demo league (FixtureSeederTests, DevLoginTests); real CFBD ingest verified via CfbdLiveTests with the operator key (9 requests consumed); SaturdayPollerIntegrationTests applies snapshots 1-6 with GameWentFinal once per game; data status page screenshot p2-04-data-status-375.png. 482 tests green. |
| 3 | 0e0dbad | 2026-09-18 | P3-01..P3-05 merged (P3-01/P3-02 Opus; P3-03 Opus-reviewed with LockAtUtc fix; P3-04 review pending post-merge). Exit criteria: default Top25 rules generate the fixture week 7 set (GameSetGenerateTests), preview/override and elevated badge (PointRulesEndpointsTests, p3-05 screenshots), Tuesday regeneration keeps manual rows and removes postponed games (RegenerationJobTests), auto-create week set (AutoCreateWeekSetTests). 499 tests green. UI verified against the real API pending (P3-05 used the fake; P4-03 will exercise the real routes). |
| 4 | (this merge) | 2026-09-19 | P4-01..P4-04 merged. Exit criteria: pick/submit/change (SetPickTests, SubmitTests), lock job locks at earliest kickoff and freezes values (LockWeekJobTests), all mutations 409 after lock and from LockAtUtc (PostLockMutationTests), all-picks opens after lock (PicksVisibilityTests), game add/remove handling (GameAddedTests, GameRemovedTests); picks UI screenshots p4-03-*. Note: UI screenshots used the fake API because the wall clock is week 3 while fixtures are week 7 (D-121); P8-03 simulation pins the clock. |
| 6 | (this merge) | 2026-09-19 | P6-01..P6-03 merged (P6-02 Opus review post-merge in flight). Exit criteria: Dance and Alyson dashboards match the Overview exactly (InfluenceCalculatorTests, DashboardTests), stale flag propagation, Won/Lost at Final; dashboard UI screenshots p6-03-* (prelock, live, final, stale). |
| 7 | 7a1bf9f | 2026-09-19 | P7-01..P7-03 merged (P7-03 Opus-reviewed). Exit criteria: reminder recipients and once-per-week (ReminderJobTests, SaturdayOneShotTests), add-game push to submitted members (EventNotificationTests), subscribe + test push verified in headless Edge against the real API (P7-02). Manual pending (operator): notifications on the installed iPhone PWA and the time-shifted Friday reminder on the deployed server (steps in README "Notifications on iPhone"). |
| 5 | (this merge) | 2026-09-19 | P5-01..P5-05 merged (P5-01/P5-03 Opus; P5-02 review folded into P8-01). Exit criteria: snapshots 1-6 score the week (ScoringSnapshotWalkTests), leaderboard and grid (LeaderboardEndpointsTests, StandingsCalculatorTests), void and override rescore with audit (OverrideTests, VoidTests; real-API leaderboard flip screenshots p5-05-leaderboard-before/after-override), season trend arrows after a second complete week (StandingsCalculatorTests trend cases; FullWeekSimulationTests in P8-03 exercises it end to end). 771 tests green. |
| 8 | 9deb095 | 2026-09-19 | P8-01 (security review, 7 fixes, route inventory tests), P8-02 (deploy scripts; server verification operator-pending), P8-03 (FullWeekSimulationTests + `simulate` CLI; iPhone run operator-pending), P8-04 (traceability: 106 rows, 101 PASS, 5 MANUAL PENDING, 0 GAP; README final; operator checklist). 822 tests green. Project DoD: automated proof complete; operator items in `Implementation/reviews/operator-checklist.md` (Google dev login, iPhone install + load timing, notifications on iPhone, deploy steps, first backup, iOS grid check, e2e run book). |

## Hot-spot edits (Program.cs, DbContext, app.css, service-worker.js)

| Date | Task | File | What |
|---|---|---|---|
| 2026-09-19 | P8-01 | `src/NcaafPickEm.Api/Program.cs` | Own commit (`d2ac749`). One line: `app.UseRateLimiter();`, placed after `app.UseAuthorization()` and therefore inside `UseStatusCodePages()`'s scope, so the limiter's bare 429 comes back as `ProblemDetails` the same way the cookie handler's 401 and 403 do. Routing is auto-inserted first, so the endpoint-aware limiter sees the route's `RequireRateLimiting` metadata. D-153. |
| 2026-09-19 | P8-01 | `src/NcaafPickEm.Api/DependencyInjection.cs` | Own commit (`fcced81`). Two lines: `services.AddAppRateLimiting(configuration);` next to `AddAppAuthentication`, plus its comment. All the policy configuration lives in `Api/Auth/RateLimitingSetup.cs`. D-153. |
| 2026-09-19 | P8-01 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Own commit (`e995251`). One line changed, none added: `.AddEndpointFilter<CsrfEndpointFilter>()` on the `/api` group becomes `.RequireCsrfHeader()`, which adds the identical filter plus the `CsrfProtectedMetadata` marker `RouteInventoryTests` reads. No behaviour change. D-157. |
| 2026-09-19 | P8-03 | `src/NcaafPickEm.Api/Program.cs` | Own commit. One `using` (`NcaafPickEm.Api.Simulation`) and one four-line block next to P7-01's `generate-vapid` one: `if (args is [SimulateCommand.CommandName, ..]) { Environment.ExitCode = await SimulateCommand.RunAsync(args); return; }` - the hidden Development-only `simulate` operator command (D-152). Nothing in the serving path changed; the `await` makes the generated `Main` async, which nothing else in the file depends on. |
| 2026-09-19 | P5-03 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Own commit. One line: `api.MapLeaderboardEndpoints();`, and the now-redundant `//   api.MapLeaderboardEndpoints(); (P5-03)` placeholder removed (the merge with P6-02's identical edit left the "later phases add" comment with nothing under it, so the comment is now one line). |
| 2026-09-19 | P5-03 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. One `using` (`NcaafPickEm.Infrastructure.Scoring`) and two lines: `AddScoped<IStandingsSnapshotWriter, StandingsSnapshotWriter>()` and `AddScoped<LeaderboardService>()`, both placed with the other application services and therefore **above** P5-01's `TryAddScoped<IStandingsSnapshotWriter, NoOpStandingsSnapshotWriter>()`, which is what makes the real writer win. |
| 2026-09-19 | P5-01 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. Three `using` lines and one block: `AddScoped<ScoringService>()`, `TryAddScoped<IStandingsSnapshotWriter, NoOpStandingsSnapshotWriter>()` (P5-03 registers its real writer on an earlier line and this then no-ops, D-129), the three `AddDomainEventHandler` lines for `GameWentFinal`/`ResultOverridden`/`GameVoided`, and `AddScheduledJob<NightlyRescoreJob>()`. No `AppDbContext` or migration change: `WeekResults` and `SeasonStandingsSnapshots` already existed. |
| 2026-09-19 | P5-04 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Own commit, then a merge-conflict resolution against P6-03's `IDashboardApi` line (additive, both kept). One line per branch: `ILeaderboardApi` -> `Fakes.FakeLeaderboardApi` / `LeaderboardApi`. |
| 2026-09-19 | P5-04 | `src/NcaafPickEm.Web/_Imports.razor` | Own commit, then a merge-conflict resolution against P6-03's `Shared.Contracts.Dashboard` using (both kept). One line: `@using NcaafPickEm.Shared.Contracts.Leaderboard`. |
| 2026-09-19 | P5-02 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. One `using`, `AddScoped<CorrectionService>()`, and `AddDomainEventHandler<GameNeedsVoidReview, GameNeedsVoidReviewHandler>()`, right after P5-01's scoring block. |
| 2026-09-19 | P5-02 | `src/NcaafPickEm.Api/DependencyInjection.cs` | Own commit. One `using` (`NcaafPickEm.Shared.Contracts.Scoring`) and two validator registrations (`OverrideResultRequest`/`VoidGameRequest`). |
| 2026-09-19 | P5-02 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Own commit. One line, `api.MapCorrectionEndpoints();`, alphabetically before `MapDashboardEndpoints()`. |
| 2026-09-19 | P7-01 | `Directory.Packages.props` | Own commit. Added a "Web push" ItemGroup with `WebPush` 1.0.13 (web-push-libs, latest stable, ships a net10.0 target), referenced version-less from `NcaafPickEm.Infrastructure`. |
| 2026-09-19 | P7-01 | `src/NcaafPickEm.Infrastructure/Data/AppDbContext.cs` | One line: `public DbSet<PushRetry> PushRetries => Set<PushRetry>();`, with `Data/Configurations/PushRetryConfiguration.cs` alongside it. Migration `Phase7_01_PushRetries` (D-070). |
| 2026-09-19 | P7-01 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | One line: `services.AddPush(configuration);` (plus its `using`). `AddPush` binds `Push:*`, chooses `WebPushSender` or `NullPushSender` from whether the VAPID pair validates, registers `NotificationService` and `AddOneShotJob<PushRetryJob>()`. |
| 2026-09-19 | P7-01 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | One line: `api.MapPushEndpoints();`, and removed the now-redundant `//   api.MapPushEndpoints(); (P7-01)` placeholder. The Development-only `POST /api/push/test` is mapped inside `MapPushEndpoints` (it reads `IHostEnvironment` off the group's `ServiceProvider`), so the Dev/Testing block in this file is untouched. |
| 2026-09-19 | P7-01 | `src/NcaafPickEm.Api/Program.cs` | Own commit, 7 lines at the very top: `if (args is [VapidKeyGenerator.CommandName, ..]) { VapidKeyGenerator.WriteNewKeyPair(Console.Out); return; }` before the bootstrap logger, so `dotnet run --project src/NcaafPickEm.Api -- generate-vapid` prints a key pair and exits without building the host (D-076). Nothing else in the file changed. |
| 2026-09-19 | P7-01 | `src/NcaafPickEm.Api/DependencyInjection.cs` | Two `IValidator<T>` registrations for `PushSubscriptionRequest` and `DeletePushSubscriptionRequest`. |
| 2026-09-19 | P7-01 | `tests/NcaafPickEm.Api.Tests/Infrastructure/ApiFactory.cs` | Additive: an optional `configureServices` constructor parameter applied in `ConfigureTestServices`, so a test class can replace an app service (`PushDeliveryTests` swaps `IPushSender` for `FakePushSender`). Existing call sites are unchanged. |
| 2026-09-19 | P7-01 | `tests/NcaafPickEm.Api.Tests/HealthEndpointTests.cs` | Fixed an assertion that hard-coded "exactly one migration has been applied" and would fail for every future phase that adds one: it now asserts the first applied migration is the initial schema and that nothing is pending. |
| 2026-09-18 | P3-04 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. Three lines: two `services.AddScheduledJob<T>()` (`RegenerateGameSetsJob`, `EnsureCurrentWeekSetsJob`) and one `services.AddDomainEventHandler<GameScheduleChanged, ScheduleChangeHandler>()`. |
| 2026-09-18 | P4-04 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. One `using` (`NcaafPickEm.Domain.GameSets.Events`) and two lines: `services.AddDomainEventHandler<GameAddedToSet, GameAddedPickHandler>();` / `services.AddDomainEventHandler<GameRemovedFromSet, GameRemovedPickHandler>();`, right after P3-04's `ScheduleChangeHandler` line. |
| 2026-09-18 | P2-03 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. `Providers:LiveScores` now parses to a `LiveScoreSource`: `Fixture` registers `FixtureLiveScoreProvider` directly, `Espn`/`Cfbd` register the named ESPN `HttpClient`, `CfbdLiveScoreProvider` and `CompositeLiveScoreProvider`; `ILiveScoreHealth` is a singleton built from the configured value; an unknown value still throws at startup. Plus one line each for `AddDomainEvents()`, the `IProviderCallRecorder` singleton and the scoped `LiveScoreApplyService`. Later phases add event handlers here with `AddDomainEventHandler<TEvent, THandler>()`. |
| 2026-09-18 | P2-03 | `src/NcaafPickEm.Infrastructure/Providers/Models/LiveScoreUpdate.cs` | Added a trailing `ProviderSource Source = ProviderSource.Espn` (D-047). P2-05's `FixtureLiveScoreProvider` and its snapshot JSON are untouched; the CFBD fallback needs it because it carries no team names and is matched by `CfbdGameId`. |
| 2026-09-18 | P2-03 | `tests/NcaafPickEm.Fixtures/NcaafPickEm.Fixtures.csproj` and `FixtureLoader.cs` | Embedded `Real\**\*.json` under its own resource prefix and added `FixtureLoader.ReadRealText`/`ReadReal<T>` (D-051), so the P2-01 captures are test and runtime inputs without being copied into `Data/`. `FixtureLoader.Names` is unchanged. |
| 2026-09-18 | P1-02 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Registers `ILeaguesApi`/`ISeasonsApi`: the real `LeaguesApi`/`SeasonsApi` by default, the Development-only fakes only when both `DEBUG` and `USE_FAKE_API` are defined (D-044). |
| 2026-09-18 | P1-02 | `src/NcaafPickEm.Web/_Imports.razor` | Added `@using` for `NcaafPickEm.Shared.Contracts.{Leagues,Invites,Seasons}` and `NcaafPickEm.Shared.Enums`, needed by every page under `Pages/Leagues/`. |
| 2026-09-18 | P3-05 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Added `IGameSetsApi`/`GameSetsApi` and `IPointRulesApi`/`PointRulesApi` registrations (plus `FakeGameSetStore`/`FakeGameSetsApi`/`FakePointRulesApi` in the existing `#if DEBUG && USE_FAKE_API` branch), alongside P1-02's existing `ILeaguesApi`/`ISeasonsApi` lines. |
| 2026-09-18 | P3-05 | `src/NcaafPickEm.Web/_Imports.razor` | Added `@using` for `NcaafPickEm.Shared.Contracts.{GameSets,Points,Reference}`, needed by every page under `Pages/GameSets/` and `Pages/Points/`. |
| 2026-09-18 | P3-05 | `src/NcaafPickEm.Web/Pages/Leagues/LeagueHome.razor` | Added a 6-line "Configure" `<div class="card">` for commissioners linking to the three new config pages, right after the existing "Manage members" card. Not a listed hot-spot file, but flagged here since P1-03 (display names) may touch `Pages/Leagues/` concurrently. |
| 2026-09-18 | P1-02 | `src/NcaafPickEm.Web/wwwroot/css/app.css` | Added one missing primitive, `.btn-danger`, for `ConfirmSheet`'s destructive action (remove member, revoke invite). |
| 2026-09-18 | P1-02 | `src/NcaafPickEm.Web/Auth/ApiAuthenticationStateProvider.cs` | Added a `JsonException` catch alongside the existing `HttpRequestException` one, so a non-JSON 200 from `api/me` degrades to signed-out instead of crashing the app (found running the Web project standalone for screenshots; not a hot-spot file per the ownership table but a one-line defensive fix). |
| 2026-09-18 | P2-05 | `src/NcaafPickEm.Api/Program.cs` | One line: `AddInfrastructure(builder.Configuration, builder.Environment)` — added the `IHostEnvironment` argument so provider registration can tell Development/Testing (default to Fixture) from everywhere else (throw if unset). |
| 2026-09-18 | P2-05 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Added a Development/Testing-only block: `app.MapDevAuthEndpoints()` and `api.MapFixtureAdminEndpoints()`, mirroring the existing Diagnostics-probe pattern so their tests can run through `ApiFactory`. |
| 2026-09-18 | P2-05 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Added `FixtureSnapshotState` singleton, `IReferenceDataProvider`/`ILiveScoreProvider` registration switched on `Providers:ReferenceData`/`Providers:LiveScores` (throws outside Dev/Testing when unset), `FixtureSeeder` + `FixtureSeederHostedService` registration. P2-02/P2-03 add their `case` branches in the two private switch methods. |
| 2026-09-18 | P2-05 | `src/NcaafPickEm.Infrastructure/NcaafPickEm.Infrastructure.csproj` | Added a `ProjectReference` to `tests/NcaafPickEm.Fixtures` so the fixture providers can read the embedded JSON at runtime, not only in tests (D-036). |
| 2026-09-18 | P0-05 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | One line: `api.MapSeasonEndpoints();`, and removed the `_ = api;` placeholder now that the group has a real member. |
| 2026-09-18 | P0-05 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Two lines: `TryAddSingleton<SeasonCalendar>()` and `TryAddSingleton<ISeasonWeekSource, FixtureSeasonWeekSource>()`. P2-02 swaps the week source by `Providers:ReferenceData`. |
| 2026-09-18 | P3-03 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Own commit. Three lines, alphabetical: `api.MapGameSetEndpoints()`, `api.MapPointRuleEndpoints()`, `api.MapReferenceEndpoints()`. |
| 2026-09-18 | P3-03 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. Two lines: `services.AddScoped<PointRuleService>()` then `services.AddScoped<GameSetService>()`, placed before `LeagueService` (which now takes `PointRuleService`). |
| 2026-09-18 | P0-04 | `src/NcaafPickEm.Web/wwwroot/css/app.css` | Replaced the template stylesheet with the design tokens (colours, spacing, 16px base, 44px tap target, safe-area insets), the reset, the shell layout, small primitives, and the loading splash. Add feature styles in a co-located `.razor.css`, not here. |
| 2026-09-18 | P0-04 | `src/NcaafPickEm.Web/wwwroot/service-worker.js` and `service-worker.published.js` | Kept the template's asset-manifest offline caching unchanged; appended empty `push` and `notificationclick` listeners with TODOs for P7-02. |
| 2026-09-18 | P0-04 | `src/NcaafPickEm.Web/Program.cs` | One registration line: `builder.Services.AddWebServices(new Uri(builder.HostEnvironment.BaseAddress))`. Add client services in `Web/Services/DependencyInjection.cs`, not here. |
| 2026-09-18 | P0-04 | `src/NcaafPickEm.Web/_Imports.razor` | Added `@using NcaafPickEm.Web.Components` and `@using NcaafPickEm.Web.Services`. |
| 2026-09-18 | P0-04 | `src/NcaafPickEm.Web/wwwroot/index.html` | PWA head (manifest, theme colour, apple-mobile-web-app meta, `viewport-fit=cover`), branded splash, bootstrap link removed, and the stable `_framework/blazor.webassembly.js` script path (D-024). |
| 2026-09-18 | P0-04 | `src/NcaafPickEm.Api/NcaafPickEm.Api.csproj` | Comment only: why the hosted `index.html` must not use the SDK fingerprint placeholders (D-024). No behaviour change. |
| 2026-09-18 | P0-01 | `src/NcaafPickEm.Api/Program.cs` | Created it. Thin composition root only: `AddInfrastructure(configuration)`, `AddApiServices()`, `MapApiEndpoints()`, Serilog, and Blazor hosting. Add services in `Infrastructure/DependencyInjection.cs` or `Api/DependencyInjection.cs` and endpoints in `Api/Endpoints/EndpointMapping.cs`, not here. |
| 2026-09-18 | P0-03 | `src/NcaafPickEm.Api/Program.cs` | Two edits: `AddApiServices(builder.Configuration)` now takes configuration (Google client id/secret), and explicit `app.UseAuthentication(); app.UseAuthorization();` after `UseStatusCodePages()`. The explicit calls are deliberate — `WebApplication` would auto-insert both *before* all user middleware, so a 401 from the authorization middleware would come back with an empty body instead of ProblemDetails. Do not delete them. |
| 2026-09-18 | P0-02 | `src/NcaafPickEm.Infrastructure/Data/AppDbContext.cs` | Created it, with every table from `02-Data-Model.md`. Adding a table = one `DbSet` here plus one file in `Data/Configurations/`; no mapping logic in the context itself. |
| 2026-09-18 | P0-03 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Added `app.MapAuthEndpoints()` at the root, `.AddEndpointFilter<CsrfEndpointFilter>()` on the `/api` group, `api.MapMeEndpoints()`, and a Development/Testing-only `api.MapDiagnosticsEndpoints()` (P1-01 deletes that block, D-021). |
| 2026-09-18 | P0-03 | `src/NcaafPickEm.Web/Program.cs` | Added `AddAuthorizationCore()`, `AddCascadingAuthenticationState()`, and `ApiAuthenticationStateProvider` as the `AuthenticationStateProvider`. `App.razor` is untouched — the cascading state comes from DI, not a wrapper component. |
| 2026-09-18 | P0-03 | `src/NcaafPickEm.Web/_Imports.razor` | Appended four usings: `NcaafPickEm.Web.Auth`, `NcaafPickEm.Web.Components`, `Microsoft.AspNetCore.Components.Authorization`, `Microsoft.AspNetCore.Authorization`. |
| 2026-09-18 | P0-06 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | One line, `services.AddJobScheduler(configuration)`, after the season-calendar registrations so the migrator is registered before the scheduler and the schema exists by the first tick. Later phases add their jobs with `AddScheduledJob<T>()` / `AddOneShotJob<T>()` on the next line, not in `Program.cs`. `Program.cs` itself is untouched. |
| 2026-09-18 | P0-06 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | One line, `api.MapAdminEndpoints();`, and removed the now-redundant `//   api.MapAdminEndpoints(); (P0-06)` placeholder comment. |
| 2026-09-18 | P0-06 | `Directory.Packages.props` | Added a "Background jobs" ItemGroup with `Cronos` 0.13.0 (latest stable), referenced version-less from `NcaafPickEm.Infrastructure`. |
| 2026-09-18 | P1-01 | `Directory.Packages.props` | Added a "Validation" ItemGroup with `FluentValidation` 11.11.0, referenced version-less from `NcaafPickEm.Api`. |
| 2026-09-18 | P1-01 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Two lines: `services.AddScoped<LeagueService>(); services.AddScoped<InviteService>();`. Merged with P0-06/P2-05's additions to the same method (jobs, providers, fixture seeder) — no further conflict expected since each phase's block is a self-contained region with a comment header. |
| 2026-09-18 | P1-01 | `src/NcaafPickEm.Api/DependencyInjection.cs` | Four `IValidator<T>` registrations for the four new request validators; removed the now-obsolete "Later phases add" remarks block. |
| 2026-09-18 | P1-01 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Added `api.MapInviteEndpoints();` and `api.MapLeagueEndpoints();`; removed the Development/Testing-only diagnostics block and `DiagnosticsEndpoints.cs`/`DiagnosticsPing.cs` (D-021). Merged with P0-06's `api.MapAdminEndpoints();` and P2-05's dev-login/fixture-admin block, which both landed on `main` first — resolved by keeping every side's mapping line, alphabetizing the feature lines, and keeping the P2-05 dev-tools block as the last thing mapped. |
| 2026-09-18 | P2-02 | `Directory.Packages.props` | Added a "Providers" ItemGroup: `CollegeFootballData` 5.27.1 plus its five transitive `Microsoft.Kiota.*` packages, pinned at 2.1.2 rather than the spike's 1.19.0 floor (NU1903 security advisory, D-053). |
| 2026-09-18 | P2-02 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Read `Providers:ReferenceData` once, up front, so the `ISeasonWeekSource` registration can add `DbSeasonWeekSource` (scoped) before the fixture `TryAddSingleton` when it is `Cfbd`; `RegisterReferenceDataProvider` gained an `IConfiguration` parameter and a `case "Cfbd"` branch that binds `CfbdOptions`, builds the Kiota `ApiClient` from a named `HttpClient`, and registers `ReferenceDataIngestService` scoped and `CfbdReferenceDataProvider` singleton. Merged with P2-03's domain-events/`IProviderCallRecorder`/`LiveScoreApplyService` additions to the same method at the P2-02/P2-03 reconciliation — kept both sides' registrations; dropped P2-02's own scoped `ProviderCallRecorder` in favor of P2-03's singleton `IProviderCallRecorder` (D-062). |
| 2026-09-18 | P1-03 | `src/NcaafPickEm.Web/_Imports.razor` | Added `@using NcaafPickEm.Shared.Contracts.Auth`, needed by `Pages/Profile/ProfilePage.razor` for `MeResponse`/`UpdateMeRequest`. |
| 2026-09-18 | P1-03 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Two lines: registers `IMeApi` as the real `MeApi` (or `Fakes.FakeMeApi` under `DEBUG && USE_FAKE_API`), alongside the existing `ILeaguesApi`/`ISeasonsApi` registrations. |
| 2026-09-19 | P4-01 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Own commit. One line, alphabetical: `api.MapPickEndpoints();`, and removed its "later phases add" comment line. |
| 2026-09-19 | P4-01 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. One line: `services.AddScoped<PickService>();`, after the P3-03 game-set/point registrations (it takes `GameSetService`). |
| 2026-09-19 | P4-01 | `src/NcaafPickEm.Api/DependencyInjection.cs` | One `IValidator<SetPickRequest>` registration next to the existing six. |
| 2026-09-19 | P4-01 | `src/NcaafPickEm.Infrastructure/Data/Migrations/` (append-only) | Second migration in the repo, `Phase4_01_GameSetGameAddedUtc`: adds `WeekGameSetGames.AddedUtc` (D-085). Existing rows backfill to `0001-01-01`. `HealthEndpointTests` stopped asserting "exactly one applied migration" as a result. |
| 2026-09-19 | P4-02 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. One line: `services.AddOneShotJob<LockWeekJob>();`, after the two P3-04 `AddScheduledJob` lines. |
| 2026-09-19 | P4-01 | `Implementation/STATUS.md` | Merge housekeeping only. This branch removed three leftover merge-conflict markers from this hot-spot table before `main` landed the same fix (`5512f97`); the `git merge main` resolution keeps every row from both sides and changes no row's content. |
| 2026-09-18 | P2-04 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. Added `services.TryAddScoped<ReferenceDataIngestService>()` unconditionally (D-081, was Cfbd-only), then the six refresh-job `AddScheduledJob<T>()` lines and `services.AddHostedService<SaturdayPoller>()`, all after the existing provider registration block. No conflict: appended to the end of `AddInfrastructure`, past every earlier phase's region comment. |
| 2026-09-18 | P2-04 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Two lines: registers `IAdminApi` as the real `AdminApi` (or `Fakes.FakeAdminApi` under `DEBUG && USE_FAKE_API`), alongside the existing typed clients. |
| 2026-09-19 | P7-03 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. One `using` plus five registration lines: `AddScheduledJob<FridayMemberReminderJob>()`/`AddScheduledJob<FridayCommissionerSummaryJob>()`/`AddOneShotJob<SaturdayReminderOneShot>()` right after `AddPush(configuration)`, and two `AddDomainEventHandler<GameAddedToSet/GameRemovedFromSet, ...>()` lines. Merge with `main` conflicted only on the `using` block (P2-04's `Jobs.Refresh` line landed on the same line range); resolved by keeping both usings. |
| 2026-09-18 | P8-02 | `src/NcaafPickEm.Api/Program.cs` | Own commit. One line, `builder.Host.UseWindowsService();`, right after `WebApplication.CreateBuilder(args)`. Required for `deploy/install-service.ps1`'s Windows service to respond to `Start-Service`/`Stop-Service` at all (D-103); a documented no-op under `dotnet run`, tests, and Docker. |
| 2026-09-18 | P8-02 | `src/NcaafPickEm.Api/NcaafPickEm.Api.csproj`, `Directory.Packages.props` | Same commit as the `Program.cs` line above. Added the `Microsoft.Extensions.Hosting.WindowsServices` 10.0.11 `PackageReference`/`PackageVersion` pair the new line needs. |
| 2026-09-19 | P7-02 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Two lines: registers `IPushApi` as the real `PushApi` (or `Fakes.FakePushApi` under `DEBUG && USE_FAKE_API`), alongside the existing registrations in the same guarded block. |
| 2026-09-19 | P4-03 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Own commit. Two lines: registers `IPicksApi` as the real `PicksApi` (or `Fakes.FakePicksApi` under `DEBUG && USE_FAKE_API`), alongside the existing typed clients. Merge with `main` conflicted against P7-02's own two `IPushApi` lines landing in the same two spots; resolved by keeping both sides' lines in both `#if`/`#else` branches. |
| 2026-09-19 | P4-03 | `src/NcaafPickEm.Web/_Imports.razor` | Own commit. One `@using NcaafPickEm.Shared.Contracts.Picks` line, needed by `PicksPage.razor` for `MyPicksResponse`/`MyPickGameDto`. |
| 2026-09-19 | P7-02 | `src/NcaafPickEm.Web/_Imports.razor` | One line: `@using NcaafPickEm.Shared.Contracts.Push`, needed by `Components/NotificationSettings.razor` for `PushSubscriptionRequest`/etc. |
| 2026-09-19 | P7-02 | `src/NcaafPickEm.Web/wwwroot/manifest.webmanifest` | One icon entry added: `badge-96.png`, 96x96, `purpose: monochrome`. Every existing entry unchanged. |
| 2026-09-19 | P7-02 | `src/NcaafPickEm.Web/wwwroot/service-worker.js` | Filled in the `push`/`notificationclick` stubs P0-04 left (same handlers as `.published.js` below); the dev worker's `fetch` no-op passthrough is untouched. |
| 2026-09-19 | P7-02 | `src/NcaafPickEm.Web/wwwroot/service-worker.published.js` | Filled in the `push`/`notificationclick` stubs P0-04 left; the offline-shell install/activate/fetch caching logic above them is untouched. |
| 2026-09-19 | P6-02 | `src/NcaafPickEm.Api/Endpoints/EndpointMapping.cs` | Own commit. One line, alphabetical: `api.MapDashboardEndpoints();`, and removed its "later phases add" comment line. |
| 2026-09-19 | P6-02 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. One line: `services.AddScoped<DashboardService>();`, right after the P4-01 `PickService` registration (it takes the same `GameSetService`). |
| 2026-09-19 | P6-03 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Two lines: registers `IDashboardApi` as the real `DashboardApi` (or `Fakes.FakeDashboardApi` under `DEBUG && USE_FAKE_API`), alongside the existing registrations in the same guarded block. Own commit. |
| 2026-09-19 | P6-03 | `src/NcaafPickEm.Web/_Imports.razor` | One line: `@using NcaafPickEm.Shared.Contracts.Dashboard`, needed by the dashboard page/components for `DashboardResponse`/`DashboardGameDto`/`MemberRef`. Own commit. |
| 2026-09-19 | P5-05 | `src/NcaafPickEm.Web/Services/DependencyInjection.cs` | Own commit, then a merge-conflict resolution against P5-04's `ILeaderboardApi` lines (additive, both kept). One line per branch: `ICorrectionsApi` -> `Fakes.FakeCorrectionsApi` / `CorrectionsApi`. |
| 2026-09-19 | P5-05 | `src/NcaafPickEm.Web/_Imports.razor` | Own commit, then a merge-conflict resolution against P5-04's `Shared.Contracts.Leaderboard` using (additive, both kept). One line: `@using NcaafPickEm.Shared.Contracts.Scoring`. |
| 2026-09-19 | P8-05 | `src/NcaafPickEm.Api/Program.cs` | Own commit (`c3b8f2a`). One `using` (`NcaafPickEm.Api.Hosting`) and one line: `app.UseAppForwardedHeaders();`, first in the pipeline - before `UseSerilogRequestLogging`, auth, and the rate limiter, all of which read the scheme/host/client address it rewrites. A no-op unless `App__BehindProxy` is true, so `dotnet run`, the test suite, and the Windows service are unchanged. D-160. |
| 2026-09-19 | P8-05 | `src/NcaafPickEm.Api/DependencyInjection.cs` | Own commit (`e7ca0ed`). One `using` and two lines next to the existing `AddAppRateLimiting`: `services.AddAppForwardedHeaders(configuration);` and `services.AddAppDataProtection(configuration);`. Both return immediately when their configuration key is unset; all the policy lives in `Api/Hosting/ForwardedHeadersSetup.cs` and `Api/Hosting/DataProtectionSetup.cs`. D-160, D-161. |
| 2026-09-19 | P8-05 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit (`5a2d506`). One line plus its comment, immediately before the existing `services.AddHostedService<DatabaseMigratorHostedService>();` it belongs to: `services.TryAddSingleton<DatabaseStartupState>();`. Appended nothing and moved nothing, so no merge surface beyond that one line. D-159. |
| 2026-09-19 | P8-05 | `.gitattributes` | Two small blocks, both about line endings on files Linux reads: `*.sh`/`*.yaml`/`*.yml`/`Dockerfile`/`.dockerignore` and `.env.example` pinned to `eol=lf`. A CRLF shebang is a "bad interpreter" failure on the server, and Compose's env-file parser keeps a trailing `\r` inside the value, which would silently corrupt `SA_PASSWORD`. No existing rule changed. |
| 2026-09-19 | P8-06 | `src/NcaafPickEm.Infrastructure/DependencyInjection.cs` | Own commit. Two lines plus their comment, immediately after `AddHostedService<SaturdayPoller>()`: `services.TryAddScoped<ReferenceDataBootstrap>();` and `services.AddHostedService<ReferenceDataBootstrapHostedService>();`. Registration order matters only in that it is after `DatabaseMigratorHostedService`, so migrations have applied before the bootstrap starts; the service itself is a no-op unless `Providers:ReferenceData=Cfbd` with jobs on, or `Providers:BootstrapOnStartup` says otherwise. D-164. |

## Escalations

| Date | Tasks | Question | Decision (DECISIONS id) |
|---|---|---|---|
| 2026-09-18 | P1-01, P1-03 | Concurrency findings from the P1-01 review (last-commissioner race between two concurrent demote/remove calls; `Invite.Uses` incremented without an optimistic-concurrency check) | Accepted at family scale: leagues here are 2-50 people who are not adversarial and rarely act within the same second. Not fixed in P1-03; revisit only if a future phase adds untrusted multi-tenant leagues. |
