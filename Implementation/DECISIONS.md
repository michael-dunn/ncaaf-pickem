# Decisions

Append only. Format: `D-NNN  YYYY-MM-DD  <task id or "plan">  <one-line decision>` followed by an indented rationale. Never edit or delete an entry; supersede it with a new one that references the old ID.

D-001  2026-09-18  plan   Frontend is Blazor WebAssembly PWA hosted by the API project, pending the P0-04 load-time spike.
       Rationale: C# end to end, shared DTOs, PWA template. Fallback if the spike fails thresholds in 01-Architecture.md: Razor Pages plus vanilla JS modules for picks and dashboard.

D-002  2026-09-18  plan   Multiple commissioners per league; authorization checks Membership.Role == Commissioner, never a single owner column.
       Rationale: Feature 01 as amended by the product owner (share and demote commissioner role). Invariant: at least one active commissioner at all times.

D-003  2026-09-18  plan   Data provider is the hybrid: CFBD free tier for reference data, ESPN scoreboard for live scores, CFBD games endpoint as fallback.
       Rationale: Feature 12, decided by the product owner. Switchable by configuration.

D-004  2026-09-18  plan   Auth is ASP.NET Core cookie authentication with the Google handler and no Identity.
       Rationale: Feature 08 Option A, decided by the product owner.

D-005  2026-09-18  plan   Background jobs run in-process with Cronos cron expressions evaluated in America/New_York; no Hangfire or Quartz.
       Rationale: Feature 10 requires in-app scheduling; keeps schema and operations small. Idempotency via JobRuns(JobName, ScheduledForUtc).

D-006  2026-09-18  plan   Week results are materialized (WeekResults) and fully recomputed on every scoring event rather than incrementally updated.
       Rationale: Feature 06 requires idempotent scoring; full recompute from source rows is the simplest way to guarantee it and supports overrides and voids without special cases.

D-007  2026-09-18  plan   Season rank trend uses SeasonStandingsSnapshots written when a week becomes Complete, comparing the latest two snapshots.
       Rationale: Feature 07 trend arrows need a stable "rank after the week before" that does not shift when a later week is rescored.

D-008  2026-09-18  plan   Removed games (before lock) are soft-flagged on WeekGameSetGames rather than deleted, and picks on them are retained.
       Rationale: Feature 02 sticky manual removal, Feature 11 "game removed" notification targeting, and history in grids all need the row to exist.

D-009  2026-09-18  P0-01  Test stack is xUnit 2.9.3 + FluentAssertions pinned to 7.2.2; versions are governed centrally by Directory.Packages.props.
       Rationale: FluentAssertions 8.x moved to a paid commercial licence (Xceed) for non-OSS use, so 7.2.2 is the last freely usable line and is pinned rather than floated. xUnit stayed on v2 (the .NET 10 `dotnet new xunit` default) because v3 changes the runner model and Microsoft.AspNetCore.Mvc.Testing integration for no benefit this project needs. Central Package Management (`ManagePackageVersionsCentrally`, transitive pinning on) means parallel agents add a versionless PackageReference plus one PackageVersion line and can never disagree on a version.

D-010  2026-09-18  P0-01  Solution file is `NcaafPickEm.slnx`, not `NcaafPickEm.sln` as named in 01-Architecture.md.
       Rationale: .NET 10's `dotnet new sln` emits the XML slnx format by default and groups src/ and tests/ into solution folders automatically. Supported by the CLI, Visual Studio 2022 17.13+, and Rider 2025.1+. Supersedes the file name in 01-Architecture.md only; the layout it describes is unchanged.

D-011  2026-09-18  P0-01  The Api hosts the Blazor WASM app through `app.MapStaticAssets()` alone. `UseBlazorFrameworkFiles()` must not be added.
       Rationale: verified by Release publish and a live smoke test. In .NET 10 the host's static-asset endpoint manifest already contains the referenced WASM project's `_framework` payload with fingerprinting and Brotli/gzip negotiation. `UseBlazorFrameworkFiles()` inserts a private StaticFileMiddleware branch that bypasses the endpoint middleware, so every `/_framework/*` request 500s with "The request reached the end of the pipeline without executing the endpoint". P0-04 owns PWA and trimming tuning but must not reintroduce that call.

D-012  2026-09-18  P2-01  ESPN status mapping in 04 section 9 gains a `status.type.state` fallback and diacritic-folding normalization; CFBD's live scoreboard is confirmed Tier 1 ($1/mo), not Tier 2; `groups=80` is not an FBS/FCS classifier.
       Rationale: verified live in the P2-01 spike (`Implementation/spikes/providers.md`) against real ESPN payloads for 2026-09-12 (80 events, all Final), 2026-09-19 (71 events, all Scheduled, all with odds) and the rankings endpoint, plus the public CFBD tiers page and the public CFBD OpenAPI document.
       Four changes follow. (1) `04` section 9 enumerates ESPN `status.type.name` values; ESPN adds names without notice and the spelling `STATUS_END_PERIOD` is probably `STATUS_END_OF_PERIOD`. Any unrecognized name must fall back to `status.type.state` ("pre" to Scheduled, "in" to InProgress, "post" to Final only when `type.completed` is true, otherwise leave unchanged) and be logged once, rather than throwing or stalling. `state` has three stable values and is the safe backstop.
       (2) `04` section 9's normalization must fold diacritics: ESPN ships "San Jose State" with U+00E9, so lowercase-plus-strip-punctuation leaves a string that never equals CFBD's. Also match ESPN `team.location` (school name, no mascot) rather than `team.displayName`, and treat `Teams.Abbreviation` as an exact-match last resort only, because ESPN's abbreviations are its own (USA / USM / USF collide).
       (3) Feature 12 says the CFBD tiers page is self-contradictory about the real-time scoreboard tier. It is not, as of 2026-09-18: Live Scoreboard is Tier 1, $1/month, 5,000 calls; Tier 2 ($5) adds live play-by-play. Feature 12's Option 1 cost line and Option 3 fallback line should both say Tier 1 / $1. D-003 (hybrid) is unchanged and reaffirmed - ESPN is still free, keyless and richer - but the escape hatch is a dollar, not five.
       (4) Feature 12 says "FBS/FCS classification has to be inferred from the groups parameter". It cannot be. `groups=80` selects games involving an FBS team, not games where both teams are FBS; the two captures contain 185 distinct teams across 22 conference groups, including Howard, Portland State, Gardner-Webb and Cal Poly. Classification must come from CFBD `Team.classification`, which D-003 already provides. Consequence for P2-03: an unmatched ESPN event is ignored silently when either team is a known FCS school and written to `UnmatchedGames` only when both look FBS.
       Not superseded, flagged for the owning tasks rather than decided here: CFBD `Game` carries no status field (only `completed`), so P2-02 must model postponement as the game disappearing from or moving within the week payload, and P2-03's CFBD fallback is Scheduled-or-Final only; and CFBD `/games` takes `classification`, not the `division` named in the P2-01/P2-02 cards.

D-013  2026-09-18  P2-01  CFBD `CalendarWeek.startDate`/`endDate` are Eastern-local-time week boundaries anchored to Monday 03:00 ET, not the Sunday 00:00 ET to Saturday 23:59:59 ET boundary `02-Data-Model.md` assumes for `SeasonWeeks`; CFBD `lines[].spread` is confirmed home-relative and needs no conversion.
       Rationale: verified against the real, live CFBD `/calendar?year=2025` and `/lines?year=2025&week=3&seasonType=regular` responses captured in the P2-01 spike (`Implementation/spikes/providers.md`; trimmed captures in `tests/NcaafPickEm.Fixtures/Real/cfbd-calendar-2025.json` and `cfbd-lines-2025-week3.json`). The 2025 season's 17 `CalendarWeek` rows all start and end at 03:00/02:59 America/New_York (the UTC offset shifts from 07:00 to 08:00 exactly at the Nov 2025 DST-end transition while the local time stays 03:00), i.e. each week runs Monday 03:00 ET to the following Monday 02:59 ET - one full week, but shifted roughly three days later than the Sunday-to-Saturday boundary `02-Data-Model.md` describes for `SeasonWeeks.StartUtc`/`EndUtc`. This does not change `02-Data-Model.md` here; it flags for whoever owns `SeasonWeeks` (P0-05 per the P2-01 doc, P2-02 for the ingest) to decide whether `SeasonWeeks` stores CFBD's boundaries verbatim or normalizes them to the ET Sunday-Saturday week the lock algorithm in `04` section 1 was written against. Separately, `lines[].spread` matches `GameLines.Spread`'s convention exactly (negative = home favoured) - confirmed across multiple 2025 week 3 games including away-favoured ones (e.g. Tennessee home vs Georgia away, spread +3.5, `formattedSpread` "Georgia -3.5"); no sign conversion needed in the CFBD ingest.

D-014  2026-09-18  P0-02  Enums that both entities and DTOs need live in `NcaafPickEm.Shared/Enums`, and `Domain` takes a project reference on `Shared`.
       Rationale: `MembershipRole`, `GameStatus`, `SubmissionStatus` and friends are part of the persisted schema *and* part of the wire contract. Defining them twice guarantees they drift and forces a mapping layer nobody wants; defining them in Domain would force `Shared` (and therefore the Blazor client) to reference Domain. `Shared` has no logic and no dependencies, so `Domain -> Shared` keeps the graph acyclic and Domain still has no EF, HTTP, or clock dependency. `01-Architecture.md`'s dependency-direction line is updated in the same commit. Enums that only a DTO needs (`Trend`, `MyOutcome`, `GridCell.Outcome`, `InvitePreview.State`) still belong to `Shared/Contracts` and are added by the phase that defines them.

D-015  2026-09-18  P0-02  The app applies pending EF migrations at startup when `Database__MigrateOnStartup` is true; the default is true in Development and false in every other environment.
       Rationale: a developer should never have to remember `dotnet ef database update` after pulling a migration, and a home-server deploy should never migrate itself during an unattended restart. `DatabaseMigratorHostedService` reads the flag and falls back to `IHostEnvironment.IsDevelopment()`. API tests set it to false: `SqlTestDatabase` migrates the throwaway database before the host boots, so the two must not race. P8-02's deploy script runs `database update` as an explicit step.

D-016  2026-09-18  P0-02  Entity timestamps are `DateTime` in UTC mapped to `datetime2`, with a global value converter that stamps `Kind = Utc` on read; DTOs keep `DateTimeOffset` per `03-API-Contracts.md`.
       Rationale: `02-Data-Model.md` specifies `datetime2` UTC columns, and `datetimeoffset` would store an offset the app never uses (all logic converts through `SeasonCalendar`). SQL Server returns `DateTimeKind.Unspecified`, which silently behaves as local time in arithmetic and in JSON; `UtcDateTimeConverter` removes that whole class of bug at the mapping layer instead of trusting every caller. Columns that are genuinely a calendar date (`Games.KickoffEasternDate`, `UnmatchedGames.GameDate`) use `DateOnly`/`date` and bypass the converter.

D-017  2026-09-18  P0-02  `PushSubscriptions.Endpoint` keeps its `nvarchar(2048)` column; the unique constraint the data model asks for is enforced on a persisted computed `EndpointHash binary(32)` (SHA-256 of the endpoint).
       Rationale: SQL Server caps a nonclustered index key at 1700 bytes and `nvarchar(2048)` is 4096, so a unique index on the column itself cannot be created. Truncating the column would risk mangling a real push endpoint. The computed column is maintained by the database, so upsert code just writes `Endpoint`, and P7-01 looks a device up by hash rather than by a 2 KB string comparison. `02-Data-Model.md` records the extra column.

D-018  2026-09-18  P0-02  Every foreign key in `AppDbContext` uses `DeleteBehavior.Restrict`.
       Rationale: nothing in this app hard-deletes a principal row — memberships and game-set games are soft-flagged so history survives (D-008) — and the schema has several diamond paths (`Picks -> Memberships -> Leagues` and `Picks -> WeekGameSetGames -> WeekGameSets -> Leagues`) that SQL Server rejects outright under cascade. Restrict everywhere makes an accidental delete fail loudly instead of quietly taking picks and results with it.

D-019  2026-09-18  P0-03  The Google handler is always registered; when `Google:ClientId` / `Google:ClientSecret` are absent it is configured with placeholder credentials rather than skipped.
       Rationale: `GoogleOptions` validates that both are non-empty, so skipping registration would mean `/auth/login/google` route-not-found on a fixtures-only dev box and a different pipeline shape in tests than in production. With placeholders the app boots identically everywhere and a challenge simply fails at Google, which is the honest outcome for a machine with no OAuth client. Tests never reach Google at all: `AuthTests` replaces `GoogleOptions.BackchannelHttpHandler` with `FakeGoogleBackchannel` and drives the real challenge, state, correlation cookie, code exchange, and `OnTicketReceived` path offline.

D-020  2026-09-18  P0-03  API tests authenticate through a `TestAuth` scheme that reads `X-Test-User`; the handler looks nothing up and creates nothing.
       Rationale: the card asks for a test scheme so no test needs Google. Making the handler upsert users would give tests a second, divergent way to create accounts; instead tests seed rows with `TestUsers` and pass the id, so the database stays the one place a user comes into existence. `ApiFactory` registers the scheme as the default in `ConfigureTestServices`, which runs after the app's own `AddAuthentication`, so the cookie scheme stays registered and `ApiTestFixture.CookieFactory` can still exercise it for the Google tests. A request with no header stays anonymous and gets the same 401 as production.

D-021  2026-09-18  P0-03  `/api/leagues/{leagueId}/ping` and `/ping/commish` exist as league-scoping probes, mapped only in Development and Testing. P1-01 deletes them.
       Rationale: the authorization matrix is mandatory per endpoint group (05-Conventions.md) and `AuthMatrix` had to be proven against something, but no real league-scoped route exists until P1-01. Two do-nothing GETs cost nothing, are unreachable in Production, and let the harness ship exercised rather than untested. P1-01 must delete `DiagnosticsEndpoints.cs`, `DiagnosticsPing.cs`, and the `if (IsDevelopment || IsEnvironment("Testing"))` block in `EndpointMapping`, and point `AuthMatrixTests` at the real routes.

D-022  2026-09-18  P0-03  The CSRF header filter is applied to the `/api` group only, not to `/auth`.
       Rationale: `03-API-Contracts.md` scopes the rule to `/api`, and `POST /auth/logout` is already covered by the session cookie being `SameSite=Lax` — a cross-site form post never carries it, so there is nothing for a forged logout to act on. Adding the filter to `/auth` would also break any future plain-form fallback for sign-out without buying protection. `LogoutButton` sends the header anyway, so nothing changes if a later task widens the rule.
D-023  2026-09-18  P0-04  The Blazor client publishes with `InvariantGlobalization=true` and `BlazorWebAssemblyLoadAllGlobalizationData=false`; the Api keeps `InvariantGlobalization=false`.
       Rationale: ICU data is ~1.5 MB of raw first-load payload for a US-English family app. All Eastern-time logic and its display strings (`LockAtEasternDisplay`, week windows) are computed server-side by `SeasonCalendar`, which needs the real time zone database and therefore keeps ICU in the Api. The client only renders the browser's local time, which still works: `TimeZoneInfo.Local` and `DateTimeOffset.Now` were verified in a published, trimmed build. Consequence for UI tasks: .NET on the client cannot produce a zone *abbreviation* ("MDT"); when Feature 13's "show the zone next to lock time" needs one, get it from the browser (`Intl.DateTimeFormat(undefined, { timeZoneName: 'short' })`) or show the UTC offset.

D-024  2026-09-18  P0-04  `index.html` references `_framework/blazor.webassembly.js` by its stable path; `OverrideHtmlAssetPlaceholders` is false and the SDK's `#[.{fingerprint}]` placeholders must not be reintroduced.
       Rationale: the placeholder rewrite runs only when the Blazor project is published on its own. When the Api publishes it as a hosted reference (D-011), the placeholder survives into the published `index.html` and `/_framework/blazor.webassembly#[.{fingerprint}].js` 404s, so the app never boots - while curl against any real `/_framework/*` URL still returns 200, which is how P0-01's smoke test missed it. Setting the property on the host does not help; the host's publish never rewrites the referenced project's HTML. The stable path is served from the fingerprinted file by the static-asset manifest. Cache impact is limited to `blazor.webassembly.js` and `dotnet.js`: the multi-MB runtime files are named by the boot manifest, not by the page, and stay fingerprinted and immutably cached.

D-025  2026-09-18  P0-04  Blazor WebAssembly stands (D-001 confirmed); no Razor Pages fallback. Trimming stays at the SDK default trim mode, not `TrimMode=full`.
       Rationale: the measured Release payload is 2.19 MB over the wire (Brotli), rendering the home page in ~0.5 s cold and ~0.3 s warm on localhost; repeat loads fetch zero bytes because the service worker serves the shell. Estimated 1.0-1.5 s first load on home Wi-Fi and 2.6-3.2 s on LTE, well inside the 5 s / 2 s thresholds in 01-Architecture.md. Details and the operator's remaining phone measurement are in `Implementation/spikes/wasm-load-time.md`; the fallback trigger stays open until those numbers are recorded. `TrimMode=full` was tried and rejected: it removed component constructors that are only reached by reflection, so the router rendered `NotFound` and threw `CtorNotLocated` in a published build, for a negligible size gain.

D-026  2026-09-18  P0-05  `ISeasonWeekSource` is async and lives in Domain; the 2026 fixture calendar is code, not JSON; `GET /api/seasons/{year}/weeks` answers 404 for a season the calendar does not know.
       Rationale: the interface is pure (no I/O in Domain) but its implementations are I/O - P2-02's reads the ingested CFBD calendar from SQL - so `ValueTask<IReadOnlyList<SeasonWeek>> GetWeeksAsync` avoids a sync-over-async wrapper later. The fixture weeks are built in `FixtureSeasonWeekSource` from `SeasonCalendar.WeekWindow(saturday)` rather than a JSON file, so P0-05 and P2-05 never touch the same fixture data. 2026 dates: Week 1 is the week of Saturday 5 September 2026, Week 0 the week before, weeks 0-14 regular season, Week 15 championship week (`IsRegularSeason = false`). 404 rather than an empty array distinguishes "no such season" from "a season with no weeks yet", which the client needs when a commissioner types a year.

D-027  2026-09-18  merge  `Domain/Seasons/SeasonWeek` is the P0-05 domain record (DateTimeOffset window, `Contains`) and doubles as the EF entity; `SeasonWeekConfiguration` converts StartUtc/EndUtc to the data model's `datetime2` columns.
       Rationale: P0-02 and P0-05 each created a `SeasonWeek` in parallel (entity class vs domain record). One type avoids a mapping layer between the calendar and the DB-backed `ISeasonWeekSource` P2-02 adds; the value converter keeps the `Phase0_02_InitialSchema` migration untouched. `GET /api/seasons/{year}/weeks` now requires the `Authenticated` policy per 03-API-Contracts (the P0-05 TODO), and `SeasonEndpointTests` moved onto `ApiTestFixture` with a signed-in client plus an anonymous-401 case.

D-028  2026-09-18  P3-02  `PointValueResolver` and `PointRuleValidation` work over slim input records in `Domain/Points` (`PointGameInfo`, `PointRuleInfo`, returning `PointResolution`), not over the `Game`/`WeekGameSetGame`/`League`/`PointRule` entities `04-Domain-Algorithms.md` section 3 names. The resolution carries a `PointValueSource` (Override / Rule / Default) and the matched rule id; `PointValueSource` lives in `Shared/Enums` per D-014. `Resolve` sorts the rules by `Priority` itself and never clamps a value to 1..100.
       Rationale: the same resolution has to run over a commissioner's unsaved candidate rules (the preview in P3-03/P3-05) as over saved ones, and over a game the caller has already loaded; taking entities would force the preview path to build throwaway `PointRule` rows with fake ids and would drag EF-shaped navigation properties into a pure function. Records keep the Domain project's "no EF, no clock, no I/O" rule honest and make every test a one-liner. The defensive sort costs nothing (an already-ascending list is returned untouched) and removes a whole class of caller bug where a list came back from the database or a request body in display order rather than priority order; ties keep input order because `OrderBy` is stable. `Source` is kept even though `WeekGameSetGames` stores only the number, because the config UI has to explain *why* a game is worth 40 (an override the commissioner forgot, or the conference rule) and recomputing that in the UI would duplicate the algorithm. Range enforcement stays in `PointRuleValidation` alone rather than being silently clamped in `Resolve`: a value outside 1..100 is a configuration bug that must reach the commissioner as a 400, not be rounded away where nobody sees it. `PointValueLimits.Min`/`Max` alias `League.MinPointValue`/`MaxPointValue` so the default, the rules, and the overrides can never drift to different ranges.
       Consumers (P3-03, P3-05): `PointValueResolver.Resolve(game, pointValueOverride, leagueDefault, rules, currentSpread)` and `PointValueResolver.IsElevated(resolved, leagueDefault)`; `PointRuleValidation.Validate(rules, leagueDefault)` returns `IReadOnlyList<PointRuleError>` with `Field` = `DefaultPointValue` or `Rules[i].<Field>`, ready to map onto a validation ProblemDetails.

D-029  2026-09-18  P3-02  A close-spread rule matches only when `Math.Abs(spread) < threshold`, strictly; a spread exactly equal to the threshold does not match. The XML comment on `PointRuleType.CloseSpread` said "at or below" and is corrected to match.
       Rationale: `04-Domain-Algorithms.md` section 3 and the Feature 03 story ("below a threshold (e.g., under 3)") both say strictly below; the enum comment was the only place saying otherwise, and it is the thing a later agent reads first. With half-point lines a "under 3" rule is meant to catch 2.5, not 3, which is exactly what strict comparison gives. `PointValueResolverTests` pins the boundary at 3.0 and -3.0 so the ambiguity cannot come back.

D-030  2026-09-18  P3-01  The Saturday/FBS/not-postponed eligibility gate applies to manual adds too, so `GenerationResult` carries a third diff list, `RemovedIneligible`, alongside `Added` and `Removed`.
       Rationale: `04` section 2 step 4 reads as if a manual row survives generation unconditionally, but Feature 02 says only Saturday games are "ever included, regardless of rule", FCS games are "never included, regardless of rule", and cancelled or postponed games are "excluded when generated and removed if already in the set". The story wins, so the step 1 pool gates every source and `04` section 2 is corrected in the same commit. That leaves a manual row that can legitimately leave the set, which step 7's "manual rows are never auto-removed" must not swallow, and which carries a different `RemovedReason` ("Schedule change", the vocabulary P3-04 already uses) from a rule-driven removal ("Rule regeneration"). Splitting the two lists keeps the invariant that matters — a manual add is never dropped because the rules changed — while letting `GameSetService` write the right reason without re-deriving eligibility from the entities. `Removed` and `RemovedIneligible` are disjoint.

D-031  2026-09-18  P3-01  Generation after lock is signalled by a result flag (`GenerationResult.Locked`, `IsLocked = true`, empty games and empty diff), not by an exception.
       Rationale: `04` section 2 step 8 says "no-op and returns 409", but 409 is an HTTP concern and the Domain project has no HTTP. Two callers want different behaviour from the same no-op: the generate endpoint turns it into a 409, and P3-04's Tuesday regeneration job simply skips the week — and a job that has to catch an exception per league to do that is worse code than one that checks a bool. `Preview` ignores the flag entirely, because looking at what a configuration would have produced is harmless after lock; only saving is refused.

D-032  2026-09-18  P3-01  The generator picks the AP poll to rank against from every poll the caller passes, rather than the caller pre-selecting current-or-fallback.
       Rationale: `04` section 2 step 2 makes poll selection part of the algorithm (latest `FetchedUtc` for the week, else the most recent prior week, flagging `UsedFallbackRankings`), so it belongs in the pure function where it is testable without a database. `GameSetService` passes the `Rankings` rows it already loaded as `RankingSet` records and gets the flag back. Polls for a later week are ignored. `UsedFallbackRankings` is raised only when a Top 25 rule was actually in play, so the UI warns only when the fallback could have affected the result; no poll at all is not a fallback.
D-033  2026-09-18  P0-06  The scheduler's catch-up window is 60 minutes (`Jobs__CatchUpMinutes`), applied from the job's newest recorded occurrence or, for a job with no history, from 60 minutes before now.
       Rationale: a home server that was off overnight must not come back and replay a day of cron. Each tick asks Cronos for the occurrences in `(max(lastRecordedOccurrence, now - CatchUpMinutes), now]`, so a short outage is made good and a long one is not. The window is deliberately also applied on a cold start, which is what makes "a job scheduled for a past minute runs once" true on a fresh database and is what the P0-06 card's first Done-when case tests; the visible cost is that a brand-new database logs up to twelve `HeartbeatJob` runs on first boot, once, which is cheap proof the scheduler works. The corollary every later phase inherits: **a scheduled job must be idempotent**, because the same occurrence can be offered again after a crash between the claim and the finish, and because `JobRuns` only proves a run was *claimed*, not that it completed.

D-034  2026-09-18  P0-06  The card's `IOneShotScheduler` ships as `IOneShotJob` (`Name`, `GetDueAsync(nowUtc, ct)`, `RunAsync(occurrence, ct)`), deduplicated as `JobRuns(JobName = "{Name}:{Key}", ScheduledForUtc = DueUtc)`.
       Rationale: the card requires one-shots to be "persisted by reading target times from the DB each minute rather than in-memory timers", so nothing is ever scheduled, queued, or held - there is no scheduler to implement, only a source the tick interrogates. P4-02's `LockWeekJob` and P7-03's `SaturdayReminderOneShot` *are* jobs, and naming their interface `...Scheduler` would have read backwards at every implementation site. The tick, not the source, decides that "due" excludes the future, so a source can answer the naive query (`LockAtUtc <= now AND LockedUtc IS NULL`) and stay stateless. Putting `DueUtc` in `ScheduledForUtc` rather than a synthetic per-week key is what makes a moved lock time a genuinely new occurrence that runs again, satisfying `04` section 11's "recomputed whenever `LockAtUtc` changes"; the cost is that `Name` + `:` + `Key` must fit `JobRuns.JobName`'s 60 characters, which the tick checks and logs rather than truncating into a silent collision.

D-035  2026-09-18  P0-06  `/api/admin/*` is scoped by `RequireAnyLeagueCommissioner()` (`Api/Auth/AdminAuthorizationExtensions.cs`), which answers 403 - not 404 - to a signed-in caller who commissions no league.
       Rationale: `03-API-Contracts.md` scopes data admin to "Commish (any league)", and the route carries no `{leagueId}`, so `LeagueMembershipEndpointFilter` and its deliberate 404 do not apply. The 404-instead-of-403 rule exists to keep league ids unprobeable; `/api/admin/data-status` names no league, so there is nothing to conceal and a plain 403 is the honest answer. Consequence for tests: the mandatory four-case `AuthMatrix` does not fit this group, and `AdminDataStatusTests` asserts anonymous 401 / non-commissioner 403 / commissioner 200 directly instead.
D-036  2026-09-18  P2-05  `NcaafPickEm.Infrastructure` takes a project reference on `tests/NcaafPickEm.Fixtures`; `IReferenceDataProvider`/`ILiveScoreProvider` and their provider-neutral records live in `Infrastructure/Providers`; `AddInfrastructure` gains an `IHostEnvironment` parameter.
       Rationale: the fixture providers (`FixtureReferenceDataProvider`, `FixtureLiveScoreProvider`) need to read the embedded JSON in the Fixtures classlib at runtime, not just in tests — a fixture-backed dev box and P8-03's Simulate page both run inside the Api process. `Fixtures` has no test-framework dependency (plain classlib, embedded JSON + a loader), so the direction (`Infrastructure -> tests/Fixtures`) is unusual layering but not a real violation: nothing test-only leaks into it. `IReferenceDataProvider`/`ILiveScoreProvider` model CFBD's and ESPN's real shapes per `Implementation/spikes/providers.md` so P2-02/P2-03's real providers are a straight mapping exercise, not a redesign. `Providers:ReferenceData`/`Providers:LiveScores` missing is Fixture only in Development/Testing; anywhere else `AddInfrastructure` throws at startup with a clear message, which needed `IHostEnvironment` added to its signature (`Program.cs` updated to pass `builder.Environment`) since the previous signature had no way to distinguish environments.
       Also: `GET /auth/dev-login?user=<name>` and `POST/GET /api/admin/fixture/snapshot` are mapped whenever `IsDevelopment() || IsEnvironment("Testing")` (matching the existing Diagnostics-probe precedent in `EndpointMapping`), not Development alone, so `DevLoginTests` and the fixture admin tests can exercise the real routes through `ApiFactory` instead of a parallel test-only endpoint. Dev-login signs in with `ExternalSignInService.CreatePrincipal`, the identical claims a real Google login produces, against `Users.GoogleSubject = "fixture:{name}"` rows `FixtureSeeder` creates.
       P0-05 (`ISeasonWeekSource`, `FixtureSeasonWeekSource`, `SeasonCalendar`) landed on `main` before this task finished; this branch merged `main` and uses those types directly rather than defining its own, per the orchestrator's guidance. Week 7 of 2026 (this task's sample week) is confirmed as the week whose Saturday is 2026-10-17, matching `FixtureSeasonWeekSource`'s Week 1 anchor of Saturday 2026-09-05.

D-037  2026-09-18  P1-01  A member removed from a league who accepts a fresh invite reactivates their existing `Memberships` row (clears `RemovedUtc`, resets `Role` to Member, `JoinedUtc`/`JoinedWeek` to now/current week) instead of inserting a second row.
       Rationale: the unique index on `Memberships (LeagueId, UserId)` makes a second row for the same pair impossible outright, so there is no other option that keeps the schema as specified. Their `DisplayNameOverride` is left as-is (not cleared) — a returning member's old nickname is not surprising, and clearing it would risk an immediate uniqueness re-check the accept flow does not otherwise need. `InviteService.AcceptAsync` looks up the membership by `(LeagueId, UserId)` regardless of `RemovedUtc` before deciding whether to reactivate or insert.

D-038  2026-09-18  P1-01  `InviteService` resolves `InviteState` in this priority when several could apply: Revoked > Expired > AlreadyMember > Full > Valid.
       Rationale: `03-API-Contracts.md` lists the five states but not an order, and a revoked-and-expired invite (or one a caller who is already a member also finds full) needs one deterministic answer. Revoked wins first because it is the only state a commissioner chose on purpose; AlreadyMember outranks Full because "you are already in" is more specific and more useful to show than "the league is full" when both happen to be true (e.g. the league filled up after the caller joined and somehow re-opened the invite).

D-039  2026-09-18  P1-01  `PUT /api/leagues/{leagueId}/members/me/display-name` returns the caller's own updated `MemberRow`, not a `LeagueDetail` or `204 No Content`.
       Rationale: `03-API-Contracts.md` names the request body but not the response shape for this route. `MemberRow` is the smallest type that already carries the effective name (`MemberNameProjection`), role, and joined-week the caller just changed or left alone, so the client can update its own roster row from the response without a second `GET .../members` round trip.

D-040  2026-09-18  P1-01  `RequireLeagueMember()`/`RequireLeagueCommissioner()` are applied per endpoint inside `/api/leagues/{leagueId:guid}`, not once to a parent route group that every league-scoped route nests under.
       Rationale: `LeagueMembershipEndpointFilter` re-queries the membership every time it runs; nesting a commissioner-only group under an already-member-scoped parent group would run the filter twice (member check, then commissioner check) on every commissioner route for no benefit — the commissioner check already implies membership. Applying the right one directly to each `MapGet`/`MapPut`/etc. call keeps it to one query per request and one filter per route, at the cost of writing `.RequireLeagueMember()` or `.RequireLeagueCommissioner()` on every line instead of once on the group.

D-041  2026-09-18  P1-01  `AuthMatrix.RunAsync` gained an optional `commissionerMethod` parameter, defaulting to `method`.
       Rationale: the harness's original two-route matrix assumed the member and commissioner routes answered the same HTTP method (true of the P0-03 diagnostics probes, both GET). The real routes do not: `GET /api/leagues/{leagueId}` (member) pairs with `PUT /api/leagues/{leagueId}/settings` (commissioner). Adding a second, optional method parameter keeps every existing call to `RunAsync` unchanged and lets `AuthMatrixTests` exercise the real pair without a second harness.

D-042  2026-09-18  P1-01  API tests that need a deterministic "current week" use a second `ApiFactory` per run, `ApiTestFixture.PinnedFactory`, whose `TimeProvider` is fixed at 2026-10-14T12:00Z (week 7) rather than the machine clock.
       Rationale: `05-Conventions.md`/`AGENT-NOTES.md` ask for exactly this hook and none existed yet. `AddInfrastructure`'s `TryAddSingleton(TimeProvider.System)` means a plain `AddSingleton` registered afterward in `ConfigureTestServices` wins, since `WebApplicationFactory.ConfigureWebHost`'s `ConfigureTestServices` callback runs after the app's own `ConfigureServices`. `PinnedFactory` shares the run's single throwaway database with `Factory`/`CookieFactory` (a second `WebApplicationFactory` against the same connection string boots its own host and service provider, but `SqlTestDatabase` already migrated it and `Database__MigrateOnStartup` is off, so nothing races). Documented in `AGENT-NOTES.md` for P1-02/P1-03 and any later task whose assertions depend on "now".

D-043  2026-09-18  P1-02  P1-02 builds against `Web/Services/ILeaguesApi` and `ISeasonsApi` rather than injecting `HttpClient` directly in pages; `LeaguesApiException` carries the ProblemDetails title, and invite accept's 409 body is modeled as `InviteAcceptOutcome` (an expected outcome, not an exception).
       Rationale: P1-01 (the real endpoints) is still in flight, so pages need a seam a fake can sit behind without touching every page again once the real service lands. `LeagueDetail`/`InvitePreview`/etc. are P1-01's final DTOs (03-API-Contracts.md), unchanged by this task.

D-044  2026-09-18  P1-02  A Development-only fake (`Web/Services/Fakes/FakeLeaguesApi`, `FakeSeasonsApi`) is selected only when both `DEBUG` and the compile constant `USE_FAKE_API` are defined; `DependencyInjection.AddWebServices` registers the real `LeaguesApi`/`SeasonsApi` otherwise.
       Rationale: the task card asked for a switch that "can never ship in Release." Gating on `DEBUG` alone would already exclude Release (which does not define `DEBUG`), but pairing it with a second, unusual constant name means a stray `-p:DefineConstants=USE_FAKE_API` on a Release publish still compiles in the real client, since Release publishes do not define `DEBUG`. `FakeLeaguesApi` seeds one sample league ("The Family League", 2026, week 7, 5 members, Michael commissioner, one active invite code `FAMILY7`); the initial URL's `?emptyState=1` seeds no league (empty-picker screenshot) and `?asMember=1` makes Alyson the caller instead of Michael (member-view screenshot), both read once at construction via `NavigationManager.Uri` since Blazor WebAssembly has exactly one DI scope for the app's lifetime.
       Screenshots for the DoD were produced by temporarily loosening the guard to a private, uncommitted `USE_FAKE_API_SCREENSHOT_TEMP` constant (no `DEBUG` requirement) and running `dotnet publish src/NcaafPickEm.Api -c Release -p:DefineConstants=USE_FAKE_API_SCREENSHOT_TEMP`, then serving that publish output and driving headless Edge over the Chrome DevTools Protocol at 375x812 (the P0-04 spike's recipe). The Blazor WebAssembly *standalone dev server* (`dotnet run` on `NcaafPickEm.Web` alone) was tried first and rejected: in this environment it 404s its own freshly-built `_framework/*.wasm`/`.pdb` files against a `blazor.boot.json` that references the right file names, which SRI then reports as an integrity failure and Blazor treats as a fatal boot error — a dev-server-only defect, not a bug in this task's pages. The loosened constant and the publish output were never committed; the double-guarded `DEBUG && USE_FAKE_API` in `DependencyInjection.cs` is what ships.

D-045  2026-09-18  P1-02  `Components/LockTime` is the one reusable "lock time in local zone with Eastern hint" component; it renders `LockAtUtc.Value.ToLocalTime()` plus the server's `LockAtEasternDisplay` string, with a caller-supplied fallback (e.g. "No game set yet") when `LockAtUtc` is null.
       Rationale: per D-023 the client cannot produce a zone abbreviation itself, so the Eastern half is always the server's string, verbatim. Phases 4 (Picks) and 6 (Dashboard) are expected to reuse this component rather than re-implement the pairing.
D-046  2026-09-18  P2-03  Domain events are `IDomainEvent` (marker with an `init` `OccurredUtc`) in the owning feature folder, dispatched by a scoped `DomainEventDispatcher` after `SaveChangesAsync`; a handler that throws is logged and skipped, and the raising service also returns the events it raised.
       Rationale: `01-Architecture.md` asks for in-process events with no bus, so the whole thing is five small types and no MediatR: `DomainEventCollector` (scoped) queues, the service saves, then `IDomainEventDispatcher` resolves `IDomainEventHandler<TEvent>` for each event's runtime type out of the same scope and awaits them in order. Raising before the save and dispatching after it is what makes "what the event says is already true in the database" hold for every subscriber. Continuing past a failed handler is deliberate and is the reason the dispatcher exists at all rather than a direct call: Feature 11's push sender must never be able to stop Feature 06's scoring, and the nightly full recompute is the real safety net for a lost event. `OccurredUtc` is an `init` property rather than a constructor parameter so `GameWentFinal(GameId, SeasonYear, Week, WinnerTeamId)` keeps the short signature its consumers read while the application service stamps it from `TimeProvider`; the Domain project may not read a clock. `LiveScoreApplyService` additionally returns the raised events in its result so the poller and its tests can assert on them without subscribing - subscribing is for reacting, not for observing.

D-047  2026-09-18  P2-03  `LiveScoreUpdate` gains `ProviderSource Source` (defaulted to `Espn`); the CFBD fallback identifies games by `CfbdGameId` in `SourceEventId` and carries no team names. `LiveScoreSource` (Fixture | Espn | Cfbd) is a separate Infrastructure enum from `Shared.Enums.ProviderSource`.
       Rationale: `IReferenceDataProvider.GetGamesAsync` returns CFBD's `Game`, which has team *ids* and no school names, so the fallback physically cannot fill the name fields the ESPN matcher works on - and it does not need to, because CFBD is the source of truth for `Games.CfbdGameId` and that is a natural key. The matcher therefore needs to know which provider an update came from, and `UnmatchedGames.Source` wants the same value. Adding it as a trailing parameter with a default keeps every existing construction site (including P2-05's `FixtureLiveScoreProvider`, whose payloads are ESPN-shaped) compiling untouched. `LiveScoreSource` is deliberately not `ProviderSource`: that enum names the two real providers a stored row is attributed to, and "Fixture" is neither - putting it there would leak a dev-only value into `TeamAliases.Source` and `UnmatchedGames.Source`.

D-048  2026-09-18  P2-03  An unrecognized ESPN `status.type.name` whose `state` is `post` without `type.completed` maps to InProgress, not to "leave the status unchanged" as D-012 proposed.
       Rationale: D-012's fallback is right in every other case and is implemented verbatim (`pre` -> Scheduled, `in` -> InProgress, `post` + completed -> Final, logged once per unknown name). "Leave unchanged" is the one branch a provider-neutral `LiveScoreUpdate` cannot express: the record carries a non-nullable `GameStatus`, and the alternative - dropping the event - would also throw away the score, which is the thing the poll exists to collect. InProgress is the safe encoding of "not scheduled and not over": it can never fire `GameWentFinal`, the apply service never regresses a game that is already Final, and the next poll corrects it. `04` section 9 is rewritten to say this, along with the `STATUS_END_OF_PERIOD` spelling D-012 flagged.

D-049  2026-09-18  P2-03  The matcher's key is the team pair within a candidate window of the requested Eastern date *and the day before*; matching happens before any FBS/FCS filtering, and a swapped-sides match is reported so the caller crosses the scores over.
       Rationale: `04` section 9 made `KickoffEasternDate` part of the match key, which is redundant once the caller has already restricted the candidates to a window, and actively wrong for the post-midnight case the fixture week exists to exercise - a poller tick at 01:45 ET on Sunday asks for Sunday while ESPN still files the game under Saturday. Widening the window to two days is safe because no team plays twice in two days, so the pair stays unique; the alternative (matching on the update's own kickoff date) breaks the moment a game is moved. Matching before filtering fixes a real bug in the naive reading of D-012: an FCS opponent that is genuinely on our schedule (CFBD ingests those games, and `05-Conventions.md` requires two of them in the fixture week) would otherwise never receive a live score, because classification is only meant to decide what to do with an event we could *not* place. The swapped flag exists because ESPN labels one side "home" even at a neutral site and does not always agree with CFBD; matching the pair but writing the provider's home score into our home column would silently invert a result. Ambiguity - two candidate games with the same pair, or an abbreviation two schools share - resolves to no match at all, because a visible unmatched row is cheap and a wrong match scores the wrong game.

D-050  2026-09-18  P2-03  Status transitions are forward-only (Postponed/Cancelled excepted, in both directions), and a row is stamped only when something actually changed.
       Rationale: the story's guarantee is that a game is scored when the feed says Final and not before; nothing says what to do when a later payload contradicts a Final. Scoring has already run by then, so a stale or glitched payload must not walk the status back - and since its scores are stale too, the whole update is skipped rather than half-applied. Scheduled after InProgress is the same class of error. Postponement is the deliberate exception, because it can legitimately strike at any point and can be undone, which is also what makes `GameScheduleChanged` fire in both directions for P3-04. Not stamping `LastScoreUpdateUtc` on a no-op is what makes "apply the same snapshot twice and nothing happens" literally true rather than nearly true, which matters because the poller re-applies the same finished snapshot every five minutes until its window closes.

D-051  2026-09-18  P2-03  `tests/NcaafPickEm.Fixtures/Real/*.json` is embedded under its own resource prefix and read with `FixtureLoader.ReadRealText`, rather than copied into `Data/` as the P2-05 csproj comment suggested.
       Rationale: the captures are now inputs, not only documentation - `EspnScoreboardParserTests` parses the two scoreboards, and `TeamAliasSeed` reads `team-aliases-draft.json` at runtime for P2-04's teams refresh. Copying them into `Data/` would duplicate 45 KB of payload that must then be kept in step with the spike's captures by hand, and widening the `Data/` glob would put real captures into `FixtureLoader.Names`, which is the synthetic sample week's inventory. A second prefix costs one property and two methods and keeps both lists honest.

D-052  2026-09-18  P2-03  `IProviderCallRecorder` / `ProviderCallRecorder` are defined here, as a singleton that writes its `ProviderCalls` row in a scope of its own; P2-02 owns the type, and the orchestrator reconciles the two at merge if P2-02 shipped a different shape.
       Rationale: the card requires every ESPN call to be recorded and P2-02 had not landed on `main` when this task finished, so the choice was to invent the seam or leave the requirement unmet. The signature is the one the orchestrator specified, `RecordAsync<T>(provider, operation, call, ct)`. Singleton with its own scope rather than scoped with an injected `AppDbContext`, for two reasons: the Saturday poller is a `BackgroundService` and would otherwise have to hand a scope to every provider, and an audit row that shares the caller's unit of work disappears if the caller rolls back - the record of a call that was really made must survive. A failure to write the row is logged and swallowed, since losing the audit must never turn a working provider call into a failed one.

D-053  2026-09-18  P2-02  Kiota packages for `CollegeFootballData` 5.27.1 are pinned at 2.1.2 in `Directory.Packages.props`, not the 1.19.0 floor the spike named.
       Rationale: 1.19.0 carries a known high-severity advisory (GHSA-7j59-v9qr-6fq9) that NuGet's `NU1903` audit refuses to restore against under this repo's warnings-as-errors build. 2.1.2 is the latest release of every `Microsoft.Kiota.*` package the client depends on and satisfies the package's own `>= 1.19.0` floor; restore and build are clean against it.

D-054  2026-09-18  P2-02  `CfbdMapping.MapConference` maps `Conferences.Name` from CFBD's `name` field ("ACC"), not `shortName` ("Atlantic Coast Conference").
       Rationale: the spike flagged this as open. The real captures settle it: `Team.conference` ("Sun Belt") and ESPN's `groups.shortName` both match CFBD's `name`, not `shortName` — confirmed against `cfbd-teams-fbs.json` and `cfbd-conferences-2025.json`. Mapping from `shortName` would make every `Games`/`Teams` join on the conference string fail silently.

D-055  2026-09-18  P2-02  `SeasonWeeks.StartUtc`/`EndUtc` store `SeasonCalendar.WeekWindow` of the Saturday inside CFBD's own calendar week, not CFBD's Monday-03:00-ET-to-next-Monday-02:59-ET boundary verbatim.
       Rationale: the spike (D-013) left this open pending P2-02. `04-Domain-Algorithms.md` section 1's lock algorithm, `SeasonWeek.Contains`, and every existing week-window consumer (`FixtureSeasonWeekSource`, `SeasonEndpointTests`) are already built against the Sunday-through-Saturday Eastern convention; storing CFBD's own boundary would require a second, incompatible notion of "week window" everywhere else in the codebase. `Infrastructure/Providers/Cfbd/CfbdCalendarNormalization` finds the one Saturday inside CFBD's window (there is always exactly one, since the window is 7 days anchored to a fixed hour) and calls `SeasonCalendar.WeekWindow` on it. `IsRegularSeason` is `seasonType == "regular"` case-insensitively, since CFBD's own enum stringifies as `"Regular"`.

D-056  2026-09-18  P2-02  CFBD's `Game` has no status field (confirmed again live). `ReferenceDataIngestService.IngestScheduleAsync` treats a previously known `Scheduled`/`InProgress` game absent from a fresh week payload as `Postponed`, and a `Postponed` game that reappears as `Scheduled` again. A `Completed=true` game with both point totals is promoted straight to `Final` (with those scores) when it is not already `Final`, and no other live-score field (`Status` otherwise, `HomeScore`/`AwayScore` otherwise, `Period`, `Clock`) is ever touched by this ingest. A refresh that returns fewer than half of a week's previously known games is refused entirely (`LastError`, nothing written) rather than treated as a shrunken schedule.
       Rationale: the spike (D-012) already established there is no status field to read; this is the disappearance-based detection it called for, plus the guard the card's "prior data untouched" requirement needs against a truncated or mid-fetch-failed CFBD response being mistaken for a mass postponement. P2-03's live poller owns `Status`/scores/`Period`/`Clock` once a game is matched to ESPN; this ingest must never fight it over the same fields except the one-way `Completed`-promotion CFBD alone can still usefully offer when ESPN has not run yet.

D-057  2026-09-18  P2-02  `ReferenceDataIngestService.IngestCalendarAsync` records its `DataRefreshStatus` under `RefreshDataType.Schedule`, the same slice `IngestScheduleAsync` uses; there is no separate `Calendar` value.
       Rationale: `RefreshDataType` (P0-02) has five values — Teams, Schedule, Rankings, Lines, Scores — matching the card's admin refresh route, not six. Adding a sixth is a needless enum/schema surface for a slice (`SeasonWeeks`) that is schedule-domain data by any reasonable reading, and the data status page already groups by slice, not by table. The tradeoff: the page's "Schedule" row reports whichever of {calendar, games} ran more recently, which is acceptable since both usually run back-to-back in the same job.

D-062  2026-09-18  P2-02  `CfbdReferenceDataProvider` and `IProviderCallRecorder`/`ProviderCallRecorder` adopt P2-03's shape (singleton, its own scope per call) rather than the scoped-with-shared-`AppDbContext` recorder this task built independently before the merge; this task's `CountThisMonthAsync(ProviderSource, ct)` is added onto P2-03's class as an additive method, and its `ApiException.ResponseStatusCode` recognition is folded into the same status-code check alongside P2-03's `HttpRequestException.StatusCode`.
       Rationale: P2-02 and P2-03 both needed a provider-call recorder and neither had landed on `main` first (D-052 anticipated exactly this and left the reconciliation to the orchestrator). P2-03's shape wins because it is usable from a singleton `BackgroundService` poller without a scope hand-off, which P2-02's scoped design could not do; `CfbdReferenceDataProvider` becomes singleton to match (it had no other scoped dependency once the recorder is singleton).
D-058  2026-09-18  P1-03  `PUT /api/me` rejects (409) a global-name rename that would collide with another active member's *effective* name in any league where the caller has no per-league override; per-league uniqueness (`PUT .../members/me/display-name`) is unchanged.
       Rationale: Feature 08 says "Display names must be unique within a league" but is silent on whether a *global* rename may create that collision indirectly. The card's orchestrator guidance picks the stricter of the two options: reject rather than silently allow two members to share an effective name in a league until one of them notices and sets an override. Only leagues where the caller currently has no override are checked (`LeagueService.ListGlobalNameCollisionsAsync`) — a caller already shielded by an override in a league cannot be blocked by that league's roster, and the check is symmetric with `MemberNameProjection`: it is the *other* member's effective name (their override, if any, else their global name) that must not match. The error message lists every colliding league by name so the caller knows to either pick a different global name or set a nickname there instead of guessing which league objected.

D-059  2026-09-18  P3-05  `Web/Services/GameSetsApi` and `PointRulesApi` (plus their `Fakes/` counterparts) follow P1-02's client seam exactly (D-043/D-044): typed interfaces, `LeaguesApiException` reused as the one client-side API exception across every feature (not renamed, despite the name), and a shared `internal static ApiResponses` helper (`Web/Services/ApiResponses.cs`) that extracts `LeaguesApi`'s ProblemDetails-parsing/read helpers so the new clients do not need to edit `LeaguesApi.cs` (a different task's file) to reuse them.
       Rationale: `05-Conventions.md`'s folder-ownership table gives `Pages/<Feature>/` to the frontend task but says nothing about a second task reaching into another task's already-merged `Services/*.cs` file; extracting the shared logic avoids a merge conflict with any concurrent Leagues-page work (P1-03) while keeping one error-handling path. `LeaguesApiException` keeps its P1-02 name rather than being renamed to something generic (e.g. `ApiException`) because every page already imports it by that name and a rename would touch files outside this task's ownership for a cosmetic reason only.

D-060  2026-09-18  P3-05  Both new fakes (`FakeGameSetsApi`, `FakePointRulesApi`) share one scoped `Fakes/FakeGameSetStore`, seeded from the "Week 7, 2026" fixture's actual team names, conferences, kickoffs, and AP ranks (hand-transcribed into the store, not read from `tests/NcaafPickEm.Fixtures` at runtime — the Web project has no reference to the Fixtures test project and P2-05 owns the fixture-provider Infrastructure wiring). A `?locked=1` query flag on the initial URL marks week 7 locked, read once at construction exactly like `FakeLeaguesApi`'s `?emptyState=1`/`?asMember=1` (D-044) since Blazor WebAssembly has one DI scope per session.
       Rationale: the task card asked for fakes "seeded with the fixture week 7 games... so you can build and screenshot before P3-03 lands," and a `?locked=1` flag "to render the locked state." Sharing one store (rather than one store per fake) is what makes a point override made on "This week's games" visible on the member week view without a real backend, matching what the real API would do.

D-061  2026-09-18  P3-05  `Components/GameRow` renders a game as two stacked rows (matchup + kickoff on top; point badge + trailing action on the bottom), not one single row with the matchup, meta, and trailing action side by side.
       Rationale: found while screenshotting "This week's games" at 375px — team names like "Michigan" and "Georgia" were clipped to "Michi"/"Georgi" once ranks, a kickoff time, a point badge, "Edit points", and a remove button all shared one 375px-wide flex row. Stacking fixes it without shrinking the tap targets (05-Conventions.md's 44px minimum) or hiding any control. `Trailing` stays a `RenderFragment` so Phases 4-6 can still drop in a pick button, a stepper, or nothing.

D-063  2026-09-18  P3-03  Manual add/remove (`POST`/`DELETE .../gameset/games[/{gameId}]`) return a `WeekGameSetResponse`, the same body `generate` returns, rather than a bare `GameSetGameDto` or `204`.
       Rationale: `03-API-Contracts.md` names the request bodies for these two routes but leaves the response unspecified. A single-game DTO would still leave the caller re-fetching the whole set to update `LockAtUtc` (a first manual add on an empty week sets it) and the rest of the games list; returning the same shape `generate` and `GET .../gameset` use keeps the UI's "apply a mutation, replace the page state" pattern identical across all four routes.

D-064  2026-09-18  P3-03  `Infrastructure/Services/WeekRangeGuard.EnsureWeekInRangeAsync` is the one check every week-scoped game-set/point-rule route runs first; a week outside the league's `FirstWeek..LastWeek` is `GameSetRuleViolationCode.WeekOutOfRange` -> 404, not 400.
       Rationale: the card asks for one shared helper so the eight-plus week routes across `GameSetEndpoints`/`PointRuleEndpoints` cannot drift on this. 404 (not 400) matches `05-Conventions.md`'s "non-member access is 404, do not reveal existence" spirit extended to weeks: a week the league does not play is a resource that does not exist for that league, the same way a non-member's league does not exist for them.

D-065  2026-09-18  P3-03  `PUT .../gameset-rules` (default and week-override) and `PUT .../point-rules` save only; they never call `generate`/re-resolve the game selection themselves. `PUT .../point-rules` and `PUT .../weeks/{week}/gameset/games/{gameId}/points` *do* re-resolve `ResolvedPointValue` on every unlocked week immediately, because that number has no separate "apply" step in the UI.
       Rationale: Feature 02's story has an explicit "Save and generate" action and a separate Tuesday auto-regeneration job (P3-04); saving rules without regenerating lets a commissioner stage several rule changes before committing to a game list, and matches `GameSetGenerator.Generate`/`Preview` already being separate from persistence. Point values have no such staging concept in the story - a point rule or override is meant to take effect the moment it is saved - so `PointRuleService.ReplaceRulesAsync`/`SetOverrideAsync` call `ReResolveUnlockedWeeksAsync`/`PointValueRecalculator` inline. `LeagueService.UpdateSettingsAsync` does the same when `DefaultPointValue` changes, for the same reason.

D-066  2026-09-18  P3-03  Game-set rule shape validation (a conference rule needs an FBS conference, a team rule needs an FBS team) lives in `GameSetService` as a hand-rolled DB-backed check (`GameSetRuleConfigurationException`, mapped to the same `ValidationProblem` shape `ValidationFilter<T>` produces), not as a `PointRuleValidation`-style pure `Domain` function or a FluentValidation validator.
       Rationale: unlike `PointRuleValidation` (which validates value ranges and priority uniqueness with no DB lookups), "is this conference/team actually FBS" needs a database read, which the `Domain` project may never do (05-Conventions.md) and which `ValidationFilter<T>`'s synchronous, DI-resolved `IValidator<T>` was judged not obviously worth adding an async EF-backed FluentValidation validator for on a Sonnet card. The error dictionary keys (`Rules[i].ConferenceId`/`TeamId`) match `PointRuleError.Field`'s convention so both mistakes read the same way in the client.

D-067  2026-09-18  P3-03  `GameSetService`/`PointRuleService` raise `GameAddedToSet`/`GameRemovedFromSet` (`Domain/GameSets/Events/`) through the real P2-03 `DomainEventCollector`/`IDomainEventDispatcher` (merged mid-task); no handler is registered yet, so today they are dispatched to nobody. P3-04/P4-04/P7-03 register handlers with `AddDomainEventHandler<TEvent, THandler>()` when they need to react.

D-068  2026-09-18  P3-03  `PointRuleDto` gains `ConferenceName`/`TeamName` (trailing, defaulted `null`) as read-only display echoes, matching `GameSetRuleDto`'s existing `ConferenceName`/`TeamName` fields; `PointRuleService.GetRulesAsync` fills them from `PointRule.Conference`/`PointRule.Team`.
       Rationale: P3-05 (UI) proposed this while building against the contract in parallel; adding it is a cheap, additive, backward-compatible change owned by this task (03-API-Contracts.md gives `Shared/Contracts/Points/` to P3-03) and saves the UI a second lookup to render a rule's conference/team name.

D-069  2026-09-18  P2-02-review  The reference data ingest fetches CFBD teams and conferences for **every division** (`GET /teams?year=`, `GET /conferences?year=`), not the FBS-only `GET /teams/fbs` and `classification=fbs` P2-02 shipped, so FCS and lower schools exist in `Teams`/`Conferences` with their real `Classification` and FBS-vs-FCS games are stored in `Games`.
       Rationale: `GET /games?classification=fbs` returns games *involving* an FBS team, so an FBS-vs-FCS game (the real capture has Indiana vs Indiana State, `awayClassification: "fcs"`) carries an FCS away side. `ReferenceDataIngestService.IngestScheduleAsync` skips any game whose home or away CFBD id is missing from `Teams`, so an FBS-only teams list silently dropped roughly 10-15 games a week in September, and `TeamClassification.Fcs` was reachable only from fixture data. Eligibility for a game set stays where it belongs, in the generator (both teams FBS, 04-Domain-Algorithms.md section 2); this decision is only about what reference data exists. It also gives P2-03's matcher the rows its "ignore silently when either team is a known FCS school" rule (the spike's follow-up 8) needs, instead of filling `UnmatchedGames` with Howard-at-Indiana noise. The cost is a larger `Teams` table (~900 rows rather than ~136) and conference rows for divisions this product never schedules; `MapClassification` still folds `ii`, `ii/iii`, `iii` and null into `TeamClassification.Other`.

D-070  2026-09-19  P7-01  Retry state for a failed push lives in a new `PushRetries` table (migration `Phase7_01_PushRetries`), not in `NotificationLog` and not in memory.
       Rationale: the policy is "retry 3 times over 15 minutes", which has to survive a restart, so an in-process `Task.Delay` is out. `NotificationLog` cannot hold it either: a retry has to replay the identical payload and TTL, and the only free column is `Error nvarchar(500)` - stuffing JSON in there would be lossy and would fight the column's real job. `PushRetries(Id, SubscriptionId, NotificationLogId, PayloadJson, TtlSeconds, Attempt, NextAttemptUtc, CreatedUtc)` is eight columns, drains to empty, and lets `PushRetryJob : IOneShotJob` reuse the P0-06 scheduler exactly as the week lock does. A unique index on `(NotificationLogId, SubscriptionId)` means a notification can queue at most one retry per device. Backoff is +1, +5, +15 minutes from the first failure (`PushRetryPolicy`).

D-071  2026-09-19  P7-01  `NotificationLog` holds one row per notification (user, league, week, type), not one per device, and its `Result` is the best outcome across the member's devices: Sent > Failed > Expired > Skipped.
       Rationale: the card says "every attempt logged to NotificationLog", but the filtered unique index on `(UserId, LeagueId, Week, Type)` permits exactly one row per member per week for the three reminder types - a member with a phone and a laptop physically cannot have two Friday-reminder rows. The row is therefore the record of the notification, not of a delivery; per-device detail goes to the structured log. The row is inserted *before* anything is sent, so the duplicate-key violation is itself the once-per-week check (a duplicate = `Skipped`, nothing sent) and a crash mid-send leaves an honest `Skipped` rather than a claim of delivery. A row marked `Failed` while retries are outstanding carries "Retry N of 3 is due at ..." in `Error` and is promoted to `Sent` if a retry succeeds; a row that is already `Sent` is never downgraded.

D-072  2026-09-19  P7-01  TTL is 1 hour for FridayReminder / CommissionerSummary / SaturdayReminder and 4 hours for GamesAdded / GameRemoved.
       Rationale: the card fixes one hour for the reminders - a "you still have picks left" nudge delivered after lock is worse than none. The two event notifications are not tied to a deadline that close: "3 games were added to week 7" stays actionable until Saturday's lock, so a phone that was offline for two hours should still get it. `PushTtl.For(type)` is the single place this is decided; `NotificationService.SendToUserAsync` takes an optional override.

D-073  2026-09-19  P7-01  Missing or invalid `Push__*` configuration is not a startup failure. `VapidConfiguration` validates the pair once at registration; when it does not validate the app boots with `NullPushSender`, `GET /api/push/vapid-public-key` answers **503** `ProblemDetails`, and every send is logged `Failed` with the reason.
       Rationale: the committed templates ship `<dev-vapid-public-key>` placeholders, and nothing else in the app needs push to work. Validating through the library's own `VapidHelper.Validate*` means a placeholder is rejected exactly like a missing key rather than exploding on the first send. 503 rather than 500 or 404 tells the client "this capability is off right now", which is precisely what the P7-02 settings page needs to render.

D-074  2026-09-19  P7-01  A push endpoint is looked up by hashing it in C# (`PushEndpointHash.Compute`, SHA-256 over UTF-16LE) and comparing against the persisted computed column, not by `Endpoint == request.Endpoint`.
       Rationale: D-017 put the unique index on `EndpointHash` because `nvarchar(2048)` is too wide for an index key; comparing on `Endpoint` would scan. SQL Server's `HASHBYTES('SHA2_256', [Endpoint])` over an `nvarchar` hashes the UTF-16LE bytes, which is exactly `SHA256.HashData(Encoding.Unicode.GetBytes(...))`. The risk of the two drifting is covered by `PushSubscriptionTests.GivenAStoredSubscription_WhenComparingHashes_ThenTheComputedColumnMatchesTheClient`, which asserts them equal against a real SQL Server.

D-075  2026-09-19  P7-01  A push service answering 404/410 **or** subscription keys the library cannot encrypt for both count as "gone": the subscription is deleted and the row logged `Expired`. `POST /api/push/subscriptions` re-owns an endpoint that is already stored against another user; `DELETE` only touches the caller's own.
       Rationale: `InvalidEncryptionDetailsException` means the stored `p256dh`/`auth` can never work, so retrying it three times is pure waste and leaves a dead row forever. Re-owning matches how browsers behave: the endpoint identifies the *browser*, not the account, so a shared family tablet that signs out and back in as somebody else must stop pushing the previous member's reminders to it. Unsubscribe is scoped to the caller (another account's device is a no-op 204) because unsubscribing is destructive and subscribing is not. Deleting a subscription first nulls `NotificationLog.SubscriptionId` on the rows that point at it - every FK is `Restrict` (D-018) and the history is worth keeping.

D-076  2026-09-19  P7-01  VAPID keys are generated by a hidden `generate-vapid` first argument on the Api (`dotnet run --project src/NcaafPickEm.Api -- generate-vapid`), wrapped by `deploy/generate-vapid.ps1`; the keys are printed and never written to disk.
       Rationale: the alternative was a separate console project just to call one library method. The flag returns before the host builder is created, so it needs no configuration, no database and no port - and it is unit-testable as `VapidKeyGenerator.Generate()`. Note for anyone editing the script: adding `--verbosity quiet --nologo` to that `dotnet run` makes the SDK swallow the argument after `--` and start a web host instead, so the invocation is kept minimal and the `Push__*` lines are filtered out of the build output.
D-077  2026-09-18  P2-04  A refresh job resolves "the current season" as the calendar year a college football season starts in (Eastern month >= July -> that year, else the year before), via `RefreshJobSupport.CurrentSeasonYear(nowUtc)`, since no global "current season" configuration exists yet (each `League` carries its own `SeasonYear`).
       Rationale: the six refresh jobs and the manual `POST /api/admin/refresh/{dataType}` endpoint all need "what season is it" before they can ask `ISeasonWeekSource` for the current week, and inventing per-league resolution would multiply one calendar refresh into one per league for no product benefit — the schedule, rankings and lines tables are season-wide, not per-league. A later phase that needs multi-season support replaces this one function.

D-078  2026-09-18  P2-04  A cron job that needs more than one Eastern time (`ScheduleRefreshJob`/`ScheduleRefreshDailyJob`, `RankingsRefreshEveningJob`/`RankingsRefreshTuesdayJob`) is two thin `IScheduledJob` classes with distinct `Name`s and cron strings, both delegating to one shared runner (`ScheduleRefreshRunner`, `RankingsRefreshRunner`) that does the actual ingest call.
       Rationale: `JobRuns(JobName, ScheduledForUtc)` is the whole idempotency mechanism (D-005), so one job cannot carry two unrelated cron occurrences without either losing catch-up granularity or risking a collision; Cronos does support a day-of-week list (`RankingsRefreshEveningJob`'s `0 20 * * 0,1` covers Sunday and Monday in one expression), used wherever the hour is shared. `CalendarRefreshJob` is not a fifth job: it is folded into `ScheduleRefreshRunner.RunAsync` ahead of the schedule ingest, matching D-057's "calendar lives under Schedule" for the same reason the data status page has one row, not two.

D-079  2026-09-18  P2-04  `POST /api/admin/refresh/{dataType}` writes its `AuditLog` row against the caller's first active commissioner membership (ordered by `JoinedUtc`), not a league taken from the request, since the route carries no `{leagueId}` and `AuditLog.LeagueId` is non-nullable.
       Rationale: `RequireAnyLeagueCommissioner()` already guarantees at least one such membership exists. The action is not really about that league — it is a provider-wide refresh — but the schema (02-Data-Model.md) ties every audit row to a league for the league's own audit trail to read, and adding a nullable league id to accommodate one action would be a schema change for a cosmetic mismatch. Commissioners of multiple leagues will see the entry attributed to whichever league they joined first; that is disclosed here rather than silently guessed.

D-080  2026-09-18  P2-04  `DataStatusResponse` gains `ActiveLiveScoreSource`, `ScoresMayBeStale` and `NeedsReview` as new, defaulted trailing parameters rather than repurposing the existing `LiveScoreSource` field to mean "active".
       Rationale: `AdminDataStatusTests` (P0-06) already asserts `LiveScoreSource == "Fixture"` reading it as the *configured* value from `Providers:LiveScores`; changing its meaning to `ILiveScoreHealth.ActiveSource` would silently pass that test in Fixture mode (where configured and active are always equal) while being wrong for Espn/Cfbd. Additive fields with defaults keep every existing caller and test compiling and correct; `NeedsReview` defaults to `[]` via an `init` accessor so a null passed positionally never leaks onto the wire.

D-081  2026-09-18  P2-04  `ReferenceDataIngestService` is registered unconditionally in `AddInfrastructure` (a plain `TryAddScoped`, after `RegisterReferenceDataProvider`), not only inside the `Cfbd` branch P2-02 wired it from.
       Rationale: the card requires the refresh jobs to be registered "regardless of provider," and the service's own doc comment already says it "ingests reference data from any `IReferenceDataProvider`" — it has no CFBD-specific dependency once constructed. `TryAddScoped` makes the Cfbd branch's own registration (still present, for its own clarity) a no-op rather than a duplicate, so Fixture-mode development and every existing test keep working through the same seam a real deployment uses.
D-082  2026-09-18  P3-04  `PUT .../weeks/{week}/gameset-rules` also calls `GameSetService.GenerateAsync` in the same request when `{week}` is the league's current week right now (`SeasonCalendar.CurrentWeekAt`, clamped to `FirstWeek..LastWeek`), superseding D-065 for that one route only; every other week, and both `PUT .../gameset-rules` (the league default) and `PUT .../point-rules`, stay save only exactly as D-065 says.
       Rationale: Feature 02's "current week" guarantee ("members always have a set") is undermined if a commissioner edits this week's rules and sees no effect until the next click or the Tuesday job. `GenerateAsync`'s own diffing already makes a second, redundant `generate` call (the commissioner UI's existing "Save and generate" two-call flow, D-060's fakes) a no-op, so nothing needed to change on the client; `WeekOverrideRegenerateTests` proves the idempotency. A locked current week never reaches this path (`ReplaceWeekRulesAsync` already refuses before saving), so the only new refusal this can surface is `ExceedsMax`, propagated exactly as `POST .../generate` already does.

D-083  2026-09-18  P3-04-review  `GameSetService.GenerateAsync`'s per-added-row `Games` lookup is one batched `Where(g => newGameIds.Contains(g.Id))` query instead of one `FirstAsync` per row; `GetWeekGameSetAsync`'s ordering gets `.ThenBy(row => row.GameId)` after `KickoffUtc` for a stable tie-break; `GameSetService`/`PointRuleService` take an `ILogger<T>` and log Information on generate/add/remove/rules-replace/override-set, matching P1-01's services; `RegenerateGameSetsJob`/`EnsureCurrentWeekSetsJob` catch `DbUpdateException` (the `UQ(LeagueId, Week)`/`UQ(WeekGameSetId, GameId)` races) as "another writer won", clearing the change tracker and logging Information rather than failing the run.
       Rationale: orchestrator-flagged P3-03 review follow-ups folded into P3-04 since it was already editing `GameSetService` for the same feature; each is additive and covered by the existing test suite (no new tests needed - the batched query and ordering change are equivalent-output refactors, and the `DbUpdateException` paths are defensive for a race no single-process test run can force deterministically).

D-084  2026-09-19  P4-01  `MyPickGameDto` composes the shared game projection (`MyPickGameDto { Game: GameSetGameDto, MyTeamId?, IsNewSinceSubmit }`) instead of repeating `GameSetGameDto`'s fields and adding two.
       Rationale: `03-API-Contracts.md` wrote the shape as "`GameSetGameDto` + `{ MyTeamId?, IsNewSinceSubmit }`", which reads either way. Composition keeps one definition of a game — every field P5/P6 later adds to `GameSetGameDto` reaches the picks page for free — and matches `DashboardGameDto { Game: GameSetGameDto, ... }`, which the same contract file already defines that way. The client cost is one extra `.Game.` hop. The contract file now states the shape explicitly so P4-03 builds against it.

D-085  2026-09-19  P4-01  `WeekGameSetGames` gains `AddedUtc` (migration `Phase4_01_GameSetGameAddedUtc`), set on insert and again when a removed row is re-added; "pressed Submit since the last time TotalCount increased" is `SubmittedUtc >= max(AddedUtc)` over the week's active games.
       Rationale: `04-Domain-Algorithms.md` section 4 defines Submitted against "the last time TotalCount increased", and nothing in the schema recorded that instant. The alternative was to make the revert a side effect of P4-04's `GameAddedToSet` handler flipping the row to InProgress — but then the rule would hold only while that handler ran, a missed or failed event would leave a member wrongly Submitted, and `SubmissionStatusCalculator` could not be a pure function of the week's state. With the column the status recomputes correctly from source rows at any time, the handler becomes an optimisation plus the notification hook, and `IsNewSinceSubmit` on the picks page falls out of the same comparison. Existing rows backfill to `0001-01-01`, i.e. "was in the set from the start", which never reverts anybody.

D-086  2026-09-19  P4-01  `MyPicksResponse.IsLocked` means "picks are frozen right now" — `LockedUtc != null` **or** `nowUtc >= LockAtUtc` — which is broader than `WeekGameSetResponse.IsLocked`, where it still means only "the lock job has run".
       Rationale: section 4 makes the server reject picks from `LockAtUtc` onwards whether or not P4-02's job has run yet (a job that runs a minute late must not open a minute of picking). The picks page has to go read-only at exactly the instant the server stops accepting picks, or the member taps a team and gets a 409. The game-set DTO keeps the narrow meaning because its consumers (commissioner configuration) care about the frozen-snapshot state the job writes, not about whether picking is open. Both meanings are documented in the contract and in the DTOs' XML docs.

D-087  2026-09-19  P4-01  The `{gameId}` in `PUT .../picks/me/{gameId}` is the `Games.Id`, not the `WeekGameSetGames.Id`, and a `PickRuleViolation` (own exception + code enum, mapped by `Api/Endpoints/PickRuleViolationResults`) carries every picks refusal, with the code name as the `ProblemDetails` title: `WeekNotCurrent` 409, `Locked` 409, `GameNotActive` 409, `IncompletePicks` 409 (+ `count`), `NoGamesInSet` 409, `GameNotInSet` 404, `TeamNotInGame` 400, `PicksNotVisible` 403.
       Rationale: `03-API-Contracts.md`'s conventions define `{gameId:guid}` as "the `Games.Id`, not provider IDs", and `GameSetEndpoints`' remove and point-override routes already use it that way, so the picks route matches rather than introducing a second kind of id into the same URL space. A separate violation type (rather than stretching `GameSetRuleViolation`) keeps the two features' code sets from colliding — `Locked` means the same thing in both, but `GameNotEligible`/`ExceedsMax` are meaningless for picks and `TeamNotInGame`/`PicksNotVisible` are meaningless for configuration. Week-in-range stays `WeekRangeGuard`'s `GameSetRuleViolation(WeekOutOfRange)` (D-064), so picks handlers catch both types.

D-088  2026-09-19  P4-01  Picks are accepted only for the current week: a week inside the league's range that is not current is 409 `WeekNotCurrent` for set-pick and submit, in the past and in the future alike, while `GET .../picks/me` stays 200 and read-only for any week in range.
       Rationale: Feature 04 says "members can only ever pick from the current week's set; past weeks are read-only" and Feature 13 says Sunday 00:00 ET ends picking for the week that just finished even while its games are still being scored. The card names only older weeks; refusing future weeks the same way follows from the same sentence (a week that has not started is not "the current week") and stops a league whose commissioner generated next week's set early from being picked against. 404 was rejected for the not-current case because the week does exist for this league and is readable — 404 is reserved for a week outside `FirstWeek..LastWeek` (D-064).

D-089  2026-09-18  P2-04 review  `POST /api/admin/unmatched/{id}/resolve` answers **409** when one of the unmatched row's raw names is already a `TeamAliases(Source, Alias)` row belonging to a *different* team, and writes nothing (the row stays unresolved).
       Rationale: `IX_TeamAliases_Source_Alias` is unique across teams, so the original "does this team already have this alias?" check let a realistic mis-click (resolving against the wrong game, or a raw name a previous resolve already claimed) reach the database as a unique-index violation and surface as a 500. The commissioner is the only one who can say which of the two claims is wrong, so the conflict is reported rather than guessed at by repointing the alias or silently skipping it. The check also consults the change tracker, because the home and away inserts share one `SaveChangesAsync`.

D-090  2026-09-18  P2-04 review  The Saturday poller resolves "which week am I polling" at the window's **Saturday evening**, not at `nowUtc`, whenever the Eastern clock reads Sunday before 03:00 (`SaturdayPoller.GameDayInstant`), and polls the window's **Saturday** Eastern date for the whole window rather than "today" (`PollerDecision.SaturdayEastern`).
       Rationale: a season week ends Saturday 23:59:59.999 ET (04 section 1) but the poller's window runs to 03:00 ET Sunday (04 section 10). Resolving the week at `nowUtc` dropped every still-playing set the moment the clock passed midnight, and asking ESPN for "today" asked for the Sunday scoreboard — between them, a 22:30 ET kickoff that goes final at 01:45 ET was never applied, which is exactly the case 04 section 9 exists to handle. Shifting the Eastern calendar date rather than subtracting three hours of UTC is what keeps this right on the November fall-back Sunday, where 02:00 ET happens twice.
D-091  2026-09-19  P7-03  `NotificationMessages` splits each Feature 11 catalog line into a short generic `Title` ("Pick reminder", "New games added", ...) and a `Body` that is the catalog sentence verbatim with its placeholders filled; the catalog itself specifies only one line of "Text" per entry.
       Rationale: `PushPayload` requires both a `Title` and a `Body` (P7-01), and the story's single "Text" string does not say how to split it. Putting the whole catalog sentence into `Body` unchanged, rather than dividing it across both fields, keeps "message texts equal the catalog" a literal string-equality assertion in `EventNotificationTests` instead of a reconstruction the reviewer has to trust; `Title` exists only to give the OS notification banner something short.

D-092  2026-09-19  P7-03  A league's "current week" for the two Friday jobs and the recipient queries is computed at call time from `ISeasonWeekSource.GetWeeksAsync(league.SeasonYear)` + `SeasonCalendar.CurrentWeekAt(nowUtc, weeks)`, grouped by `SeasonYear` across all leagues in one pass (`ReminderRecipients.CurrentUnlockedSetsAsync`), rather than reading a cached or stored "current week" value.
       Rationale: matches the pattern P1-01/P1-03 already use for the same lookup (`LeagueService`'s batched season-weeks-by-year query) and keeps "recipients evaluated at send time" literally true — nothing about which week is current is computed ahead of the job running. A league with no `WeekGameSets` row for that week, an already-locked week, or a week with no active games (`LockAtUtc == null`) is filtered out here in one place rather than three times in each job.

D-093  2026-09-19  P7-03  `GamesAddedNotificationHandler`'s recipient query treats a membership as "was Submitted" when its current `WeekSubmissions.Status == Submitted` **or** `SubmittedUtc` is non-null and earlier than the event's `OccurredUtc`, rather than assuming this handler runs before or after P4-04's handler on the same `GameAddedToSet` event.
       Rationale: domain event handlers for one event run in registration order (D-046), and P4-04 (in flight at the time this task landed) is expected to react to the same event by flipping a Submitted member to InProgress. Depending on which handler is registered first would make this handler's recipients silently wrong depending on registration order alone — an accident of `DependencyInjection.cs` line order, not a real business rule. Checking both the live status and the historical `SubmittedUtc` against the event's own timestamp makes the answer correct regardless of order, and costs nothing extra since `SubmittedUtc` is already read for every membership.

D-094  2026-09-19  P7-03  `SaturdayReminderOneShot.GetDueAsync` scans every unlocked `WeekGameSets` row across every league (`WHERE LockedUtc IS NULL AND LockAtUtc IS NOT NULL`) rather than restricting to a bounded time window, unlike `PushRetryJob`'s `Take(200)`/`OrderBy` bound.
       Rationale: the scheduler tick (D-034) already drops any occurrence whose `DueUtc` is in the future, so an unbounded scan only ever *runs* work that is actually due; the cost is reading a handful of rows per league per minute; a bounded window was judged unnecessary complexity at this product's scale (a home-server family league, not a multi-tenant SaaS) and is easy to add later if `WeekGameSets` ever grows large. `SaturdayOneShotTests` seeds its own leagues with `LockAtUtc` relative to `DateTimeOffset.UtcNow` rather than the Week 7, 2026 fixture calendar (which is a month in the future relative to the run) so due/not-due assertions are deterministic against the real clock a `SchedulerHarness` tick uses.
